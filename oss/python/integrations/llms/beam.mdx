---
title: "Beam 集成"
description: "使用 LangChain Python 与 Beam LLM 进行集成。"
---

调用 Beam API 包装器以部署并在云环境中对 gpt2 LLM 实例进行后续调用。需要安装 Beam 库并注册 Beam Client ID 和 Client Secret。通过调用包装器，将创建并运行模型实例，返回与提示相关的文本。随后可通过直接调用 Beam API 进行额外调用。

如果您还没有账户，请[创建一个账户](https://www.beam.cloud/)。从[仪表板](https://www.beam.cloud/dashboard/settings/api-keys)获取您的 API 密钥。

安装 Beam CLI

```python
!curl https://raw.githubusercontent.com/slai-labs/get-beam/main/get-beam.sh -sSfL | sh
```

注册 API 密钥并设置您的 beam client id 和 secret 环境变量：

```python
import os

beam_client_id = "<Your beam client id>"
beam_client_secret = "<Your beam client secret>"

# 设置环境变量
os.environ["BEAM_CLIENT_ID"] = beam_client_id
os.environ["BEAM_CLIENT_SECRET"] = beam_client_secret

# 运行 beam configure 命令
!beam configure --clientId={beam_client_id} --clientSecret={beam_client_secret}
```

安装 Beam SDK：

```python
pip install -qU  beam-sdk
```

**直接从 langchain 部署并调用 Beam！**

请注意，冷启动可能需要几分钟才能返回响应，但后续调用会更快！

```python
from langchain_community.llms.beam import Beam

llm = Beam(
    model_name="gpt2",
    name="langchain-gpt2-test",
    cpu=8,
    memory="32Gi",
    gpu="A10G",
    python_version="python3.8",
    python_packages=[
        "diffusers[torch]>=0.10",
        "transformers",
        "torch",
        "pillow",
        "accelerate",
        "safetensors",
        "xformers",
    ],
    max_length="50",
    verbose=False,
)

llm._deploy()

response = llm._call("Running machine learning on a remote GPU")

print(response)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/beam.mdx) 或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs)至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>