---
title: "Dataforseo 集成"
description: "使用 LangChain Python 集成 Dataforseo。"
---

>[DataForSEO](https://dataforseo.com/) 通过 API 提供全面的 SEO 和数字营销数据解决方案。

本页提供如何在 LangChain 中使用 DataForSEO 搜索 API 的说明。

## 安装与设置

获取 [DataForSEO API 访问登录名和密码](https://app.dataforseo.com/register)，并分别将其设置为环境变量（`DATAFORSEO_LOGIN` 和 `DATAFORSEO_PASSWORD`）。

```python
import os

os.environ["DATAFORSEO_LOGIN"] = "your_login"
os.environ["DATAFORSEO_PASSWORD"] = "your_password"
```


## 实用工具

`DataForSEO` 实用工具封装了 API。要导入此实用工具：

```python
from langchain_community.utilities.dataforseo_api_search import DataForSeoAPIWrapper
```

有关此封装的详细介绍，请参阅[此笔记本](/oss/python/integrations/tools/dataforseo)。

## 工具

您也可以将此封装作为工具加载，以便与智能体一起使用：

```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["dataforseo-api-search"])
```

这将加载以下工具：

```python
from langchain_community.tools import DataForSeoAPISearchRun
from langchain_community.tools import DataForSeoAPISearchResults
```

## 使用示例

```python
dataforseo = DataForSeoAPIWrapper(api_login="your_login", api_password="your_password")
result = dataforseo.run("Bill Gates")
print(result)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/dataforseo.mdx) 或[提交 issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    通过 MCP [将这些文档连接](/use-these-docs)到 Claude、VSCode 等，获取实时答案。
</Callout>
</div>
