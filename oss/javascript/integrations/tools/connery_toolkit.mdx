---
title: "Connery 工具包集成"
description: "使用 LangChain JavaScript 与 Connery 工具包集成。"
---

```typescript
import { ConneryService } from "@langchain/community/tools/connery";
import { ConneryToolkit } from "@langchain/community/agents/toolkits/connery";
import { ChatOpenAI } from "@langchain/openai";
import { initializeAgentExecutorWithOptions } from "@langchain/classic/agents";

// 指定你的 Connery Runner 凭据。
process.env.CONNERY_RUNNER_URL = "";
process.env.CONNERY_RUNNER_API_KEY = "";

// 指定 OpenAI API 密钥。
process.env.OPENAI_API_KEY = "";

// 指定你的电子邮件地址，以接收以下示例中的邮件。
const recepientEmail = "test@example.com";

// 使用 Connery Runner 中所有可用的操作创建一个 Connery 工具包。
const conneryService = new ConneryService();
const conneryToolkit = await ConneryToolkit.createInstance(conneryService);

// 使用 OpenAI Functions 代理，通过 Connery 工具包中的操作执行提示。
const llm = new ChatOpenAI({ model: "gpt-5.4-mini", temperature: 0 });
const agent = await initializeAgentExecutorWithOptions(
  conneryToolkit.tools,
  llm,
  {
    agentType: "openai-functions",
    verbose: true,
  }
);
const result = await agent.invoke({
  input:
    `用三句话简要总结网页 http://www.paulgraham.com/vb.html 的内容 ` +
    `并将其发送到 ${recepientEmail}。在邮件正文中包含该网页的链接。`,
});
console.log(result.output);
```

<Info>
Connery Action 是一个结构化工具，因此你只能在支持结构化工具的代理中使用它。
</Info>

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/tools/connery_toolkit.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>