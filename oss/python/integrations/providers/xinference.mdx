---
title: "Xorbits inference (Xinference) 集成"
description: "使用 LangChain Python 与 Xorbits inference (Xinference) 集成。"
---

本页演示如何将 [Xinference](https://github.com/xorbitsai/inference)
与 LangChain 一起使用。

`Xinference` 是一个功能强大且用途广泛的库，旨在提供 LLM、
语音识别模型和多模态模型服务，甚至可以在您的笔记本电脑上运行。
使用 Xorbits Inference，您可以轻松部署和提供您自己的或
最先进的内置模型，只需一个命令即可。

## 安装和设置

Xinference 可以通过 pip 从 PyPI 安装：

<CodeGroup>
```bash pip
pip install "xinference[all]"
```

```bash uv
uv add "xinference[all]"
```
</CodeGroup>

## LLM

Xinference 支持各种与 GGML 兼容的模型，包括 chatglm、baichuan、whisper、
vicuna 和 orca。要查看内置模型，请运行命令：

```bash
xinference list --all
```


### Xinference 包装器

您可以通过运行以下命令启动 Xinference 的本地实例：

```bash
xinference
```

您也可以在分布式集群中部署 Xinference。为此，首先在您要运行它的服务器上启动 Xinference supervisor：

```bash
xinference-supervisor -H "${supervisor_host}"
```


然后，在您要运行 Xinference worker 的其他每个服务器上启动 Xinference worker：

```bash
xinference-worker -e "http://${supervisor_host}:9997"
```

您也可以通过运行以下命令启动 Xinference 的本地实例：

```bash
xinference
```

一旦 Xinference 运行起来，将可以通过 CLI 或 Xinference 客户端访问用于模型管理的端点。

对于本地部署，端点将是 http://localhost:9997。


对于集群部署，端点将是 http://$\{supervisor_host\}:9997。


然后，您需要启动一个模型。您可以指定模型名称和其他属性，包括 model_size_in_billions 和 quantization。您可以使用命令行界面 (CLI) 来完成此操作。例如，

```bash
xinference launch -n orca -s 3 -q q4_0
```

将返回一个模型 uid。

用法示例：

```python
from langchain_community.llms import Xinference

llm = Xinference(
    server_url="http://0.0.0.0:9997",
    model_uid = {model_uid} # 将 model_uid 替换为启动模型时返回的模型 UID
)

llm(
    prompt="Q: where can we visit in the capital of France? A:",
    generate_config={"max_tokens": 1024, "stream": True},
)

```

### 用法

有关更多信息和详细示例，请参阅
[xinference LLM 示例](/oss/python/integrations/llms/xinference)

### 嵌入

Xinference 还支持嵌入查询和文档。请参阅
[xinference 嵌入示例](/oss/python/integrations/embeddings/xinference)
以获取更详细的演示。


### Xinference LangChain 合作伙伴包安装

使用以下命令安装集成包：

<CodeGroup>
```bash pip
pip install langchain-xinference
```

```bash uv
uv add langchain-xinference
```
</CodeGroup>

## 聊天模型

```python
from langchain_xinference.chat_models import ChatXinference
```

## LLM

```python
from langchain_xinference.llms import Xinference
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/xinference.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>