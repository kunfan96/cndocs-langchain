---
title: "Memorize 集成"
description: "使用 LangChain Python 与 Memorize 工具集成。"
---

通过无监督学习微调 LLM 本身以记忆信息。

此工具需要支持微调的 LLM。目前仅支持 `langchain.llms import GradientLLM`。

## 导入

```python
import os

from langchain.agents import create_agent, load_tools
from langchain_classic.chains import LLMChain
from langchain.memory import ConversationBufferMemory
from langchain_community.llms import GradientLLM
```

## 设置环境 API 密钥

确保从 Gradient AI 获取您的 API 密钥。您将获得 10 美元的免费额度用于测试和微调不同模型。

```python
from getpass import getpass

if not os.environ.get("GRADIENT_ACCESS_TOKEN", None):
    # 访问令牌位于 https://auth.gradient.ai/select-workspace
    os.environ["GRADIENT_ACCESS_TOKEN"] = getpass("gradient.ai access token:")
if not os.environ.get("GRADIENT_WORKSPACE_ID", None):
    # `ID` 列在 `$ gradient workspace list` 中
    # 登录后也会显示在 https://auth.gradient.ai/select-workspace
    os.environ["GRADIENT_WORKSPACE_ID"] = getpass("gradient.ai workspace id:")
if not os.environ.get("GRADIENT_MODEL_ADAPTER_ID", None):
    # `ID` 列在 `$ gradient model list --workspace-id "$GRADIENT_WORKSPACE_ID"` 中
    os.environ["GRADIENT_MODEL_ID"] = getpass("gradient.ai model id:")
```

可选：验证您的环境变量 `GRADIENT_ACCESS_TOKEN` 和 `GRADIENT_WORKSPACE_ID` 以获取当前部署的模型。

## 创建 `GradientLLM` 实例

您可以指定不同的参数，例如模型名称、生成的最大令牌数、温度等。

```python
llm = GradientLLM(
    model_id=os.environ["GRADIENT_MODEL_ID"],
    # # 可选：设置新凭据，默认使用环境变量
    # gradient_workspace_id=os.environ["GRADIENT_WORKSPACE_ID"],
    # gradient_access_token=os.environ["GRADIENT_ACCESS_TOKEN"],
)
```

## 加载工具

```python
tools = load_tools(["memorize"], llm=llm)
```

## 初始化代理

```python
agent = create_agent(
    model=llm,
    tools=tools,
    verbose=True,
)
```

## 运行代理

要求代理记忆一段文本。

```python
agent.invoke(
    {
        "input": "请详细记住以下事实：\nZara Tubikova 以惊人的灵巧性，仅用双脚在 20 秒内蒙眼解开了 4x4 魔方变体，创造了世界纪录。"
    }
)
```

```text
> 进入新的 AgentExecutor 链...
我应该记住这个事实。
操作：记忆
操作输入：Zara T
观察：训练完成。损失：1.6853971333333335
思考：我现在知道最终答案了。
最终答案：Zara Tubikova set a world

> 链结束。
```

```text
'Zara Tubikova set a world'
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/tools/memorize.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>