---
title: "Python REPL 集成"
description: "使用 LangChain Python 与 Python REPL 工具集成。"
---

有时，对于复杂的计算，与其让 LLM 直接生成答案，不如让 LLM 生成代码来计算答案，然后运行该代码以获取答案。为了轻松实现这一点，我们提供了一个简单的 Python REPL 来执行命令。

此接口仅返回打印的内容 - 因此，如果您想用它来计算答案，请确保让它打印出答案。

<Warning>
**Python REPL 可以在主机上执行任意代码（例如，删除文件、发出网络请求）。请谨慎使用。**

</Warning>

```python
from langchain.tools import tool
from langchain_experimental.utilities import PythonREPL
```

```python
python_repl = PythonREPL()
```

```python
python_repl.run("print(1+1)")
```

```text
Python REPL can execute arbitrary code. Use with caution.
```

```text
'2\n'
```

```python
# 您可以创建工具以传递给代理
@tool
def python_repl_tool(code: str) -> str:
    """一个 Python shell。

    使用此工具执行 python 命令。

    输入应为有效的 python 命令。

    如果您想查看某个值的输出，应使用 `print(...)` 将其打印出来。
    """
    return python_repl.run(code)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/tools/python.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>
