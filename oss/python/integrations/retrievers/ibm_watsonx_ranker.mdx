---
title: "IBM watsonx.ai 集成"
description: "使用 LangChain Python 与 IBM watsonx.ai 检索器集成。"
---

>`WatsonxRerank` 是 IBM [watsonx.ai](https://www.ibm.com/products/watsonx-ai) 基础模型的封装器。

本笔记本展示了如何在检索器中使用 [watsonx 的重排序端点](https://cloud.ibm.com/apidocs/watsonx-ai#text-rerank)。

## 概述

### 集成详情

| 类 | 包 | [JS 支持](https://js.langchain.com/docs/integrations/document_compressors/ibm/) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: | :---: |
| [`WatsonxRerank`](https://reference.langchain.com/python/integrations/langchain_ibm/WatsonxRerank/) | [`langchain-ibm`](https://reference.langchain.com/python/integrations/langchain_ibm/) | ✅ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-ibm?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-ibm?style=flat-square&label=%20) |

## 设置

要访问 IBM watsonx.ai 模型，您需要创建一个 IBM watsonx.ai 账户，获取 API 密钥，并安装 `langchain-ibm` 集成包。

### 凭证

下面的单元格定义了使用 watsonx 基础模型推理所需的凭证。

**操作：** 提供 IBM Cloud 用户 API 密钥。详情请参阅
[管理用户 API 密钥](https://cloud.ibm.com/docs/account?topic=account-userapikey&interface=ui)。

```python
import os
from getpass import getpass

watsonx_api_key = getpass()
os.environ["WATSONX_APIKEY"] = watsonx_api_key
```

此外，您还可以将其他密钥作为环境变量传递。

```python
import os

os.environ["WATSONX_URL"] = "your service instance url"
os.environ["WATSONX_TOKEN"] = "your token for accessing the CLOUD or CPD cluster"
os.environ["WATSONX_PASSWORD"] = "your password for accessing the CPD cluster"
os.environ["WATSONX_USERNAME"] = "your username for accessing the CPD cluster"
os.environ["WATSONX_INSTANCE_ID"] = "your instance_id for accessing the CPD cluster"
```

### 安装

LangChain IBM 集成位于 `langchain-ibm` 包中：

```python
!pip install -qU langchain-ibm
!pip install -qU langchain-community
!pip install -qU langchain_text_splitters
```

出于实验目的，请同时安装 `faiss` 或 `faiss-cpu` 包：

```python
!pip install -qU  faiss

# 或者（取决于 Python 版本）

!pip install -qU  faiss-cpu
```

用于打印文档的辅助函数

```python
def pretty_print_docs(docs):
    print(
        f"\n{'-' * 100}\n".join(
            [f"Document {i + 1}:\n\n" + d.page_content for i, d in enumerate(docs)]
        )
    )
```

## 实例化

### 设置基础向量存储检索器

让我们从初始化一个简单的向量存储检索器开始，并存储 2023 年国情咨文演讲（分块存储）。我们可以设置检索器以检索大量（20）文档。

初始化 `WatsonxEmbeddings`。更多详情请参阅 [WatsonxEmbeddings](/oss/python/integrations/embeddings/ibm_watsonx)。

**注意**：

- 为提供 API 调用的上下文，您必须添加 `project_id` 或 `space_id`。更多信息请参阅[文档](https://www.ibm.com/docs/en/watsonx-as-a-service?topic=projects)。
- 根据您预配服务实例的区域，使用[特定区域的认证 URL](https://ibm.github.io/watsonx-ai-python-sdk/setup_cloud.html#authentication) 之一。

在此示例中，我们将使用 `project_id` 和 Dallas url。

您需要指定用于嵌入的 `model_id`。所有可用模型可在[文档](https://ibm.github.io/watsonx-ai-python-sdk/fm_embeddings.html#EmbeddingModels)中找到。

```python
from langchain_ibm import WatsonxEmbeddings

wx_embeddings = WatsonxEmbeddings(
    model_id="ibm/slate-125m-english-rtrvr",
    url="https://us-south.ml.cloud.ibm.com",
    project_id="PASTE YOUR PROJECT_ID HERE",
)
```

```python
from langchain_community.document_loaders import TextLoader
from langchain_community.vectorstores import FAISS
from langchain_text_splitters import RecursiveCharacterTextSplitter

documents = TextLoader("../../how_to/state_of_the_union.txt").load()
text_splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=100)
texts = text_splitter.split_documents(documents)
retriever = FAISS.from_documents(texts, wx_embeddings).as_retriever(
    search_kwargs={"k": 20}
)

query = "What did the president say about Ketanji Brown Jackson"
docs = retriever.invoke(query)
pretty_print_docs(docs[:5])  # 打印前 5 个文档
```

```text
Document 1:

总统最严肃的宪法职责之一是提名某人在美国最高法院任职。

我在 4 天前就这样做了，当时我提名了巡回上诉法院法官凯坦吉·布朗·杰克逊。她是我国顶尖的法律人才之一，将继续延续布雷耶大法官卓越的遗产。
----------------------------------------------------------------------------------------------------
Document 2:

我与他们的家人交谈，并告诉他们，我们永远感激他们的牺牲，我们将继续他们的使命，恢复每个社区应得的信任和安全。

我长期致力于这些问题。

我知道什么有效：投资于犯罪预防和社区警察，他们将巡逻街区，了解社区，并能恢复信任和安全。

所以，让我们不要放弃我们的街道。或者在安全与平等司法之间做出选择。
----------------------------------------------------------------------------------------------------
Document 3:

他与乌克兰人民会面。

从泽连斯基总统到每一位乌克兰人，他们的无畏、勇气和决心激励着世界。

一群公民用身体阻挡坦克。从学生到退休人员，从教师到士兵，每个人都保卫着他们的家园。

正如泽连斯基总统在对欧洲议会的演讲中所说：“光明将战胜黑暗。”乌克兰驻美国大使今晚也在这里。
----------------------------------------------------------------------------------------------------
Document 4:

正如我去年所说，特别是对我们年轻的跨性别美国人，作为你们的总统，我将永远支持你们，这样你们就可以做自己，并发挥上帝赋予的潜力。

虽然我们似乎从未达成一致，但事实并非如此。去年我签署了 80 项两党法案成为法律。从防止政府关门到保护亚裔美国人免受仍然过于常见的仇恨犯罪，再到改革军事司法。
----------------------------------------------------------------------------------------------------
Document 5:

对所有美国人，我将如我所承诺的那样对你们诚实。一位俄罗斯独裁者入侵外国，在世界各地造成了代价。

我正在采取有力行动，确保我们制裁的痛苦针对俄罗斯经济。我将利用我们掌握的一切工具来保护美国企业和消费者。

今晚，我可以宣布，美国已与另外 30 个国家合作，从世界各地的储备中释放了 6000 万桶石油。
```

## 用法

### 使用 WatsonxRerank 进行重排序

现在让我们用 `ContextualCompressionRetriever` 包装我们的基础检索器。我们将添加一个 `WatsonxRerank`，它使用 watsonx 重排序端点对返回的结果进行重排序。
请注意，在 WatsonxRerank 中指定模型名称是强制性的！

```python
from langchain_ibm import WatsonxRerank

wx_rerank = WatsonxRerank(
    model_id="cross-encoder/ms-marco-minilm-l-12-v2",
    url="https://us-south.ml.cloud.ibm.com",
    project_id="PASTE YOUR PROJECT_ID HERE",
)
```

```python
from langchain_classic.retrievers.contextual_compression import ContextualCompressionRetriever

compression_retriever = ContextualCompressionRetriever(
    base_compressor=wx_rerank, base_retriever=retriever
)

compressed_docs = compression_retriever.invoke(
    "What did the president say about Ketanji Jackson Brown"
)
pretty_print_docs(compressed_docs[:5])  # 打印前 5 个压缩后的文档
```

```text
Document 1:

总统最严肃的宪法职责之一是提名某人在美国最高法院任职。

我在 4 天前就这样做了，当时我提名了巡回上诉法院法官凯坦吉·布朗·杰克逊。她是我国顶尖的法律人才之一，将继续延续布雷耶大法官卓越的遗产。
----------------------------------------------------------------------------------------------------
Document 2:

正如我去年所说，特别是对我们年轻的跨性别美国人，作为你们的总统，我将永远支持你们，这样你们就可以做自己，并发挥上帝赋予的潜力。

虽然我们似乎从未达成一致，但事实并非如此。去年我签署了 80 项两党法案成为法律。从防止政府关门到保护亚裔美国人免受仍然过于常见的仇恨犯罪，再到改革军事司法。
----------------------------------------------------------------------------------------------------
Document 3:

对所有美国人，我将如我所承诺的那样对你们诚实。一位俄罗斯独裁者入侵外国，在世界各地造成了代价。

我正在采取有力行动，确保我们制裁的痛苦针对俄罗斯经济。我将利用我们掌握的一切工具来保护美国企业和消费者。

今晚，我可以宣布，美国已与另外 30 个国家合作，从世界各地的储备中释放了 6000 万桶石油。
----------------------------------------------------------------------------------------------------
Document 4:

我与他们的家人交谈，并告诉他们，我们永远感激他们的牺牲，我们将继续他们的使命，恢复每个社区应得的信任和安全。

我长期致力于这些问题。

我知道什么有效：投资于犯罪预防和社区警察，他们将巡逻街区，了解社区，并能恢复信任和安全。

所以，让我们不要放弃我们的街道。或者在安全与平等司法之间做出选择。
----------------------------------------------------------------------------------------------------
Document 5:

他与乌克兰人民会面。

从泽连斯基总统到每一位乌克兰人，他们的无畏、勇气和决心激励着世界。

一群公民用身体阻挡坦克。从学生到退休人员，从教师到士兵，每个人都保卫着他们的家园。

正如泽连斯基总统在对欧洲议会的演讲中所说：“光明将战胜黑暗。”乌克兰驻美国大使今晚也在这里。
```

## 在链中使用

您当然可以在 QA 管道中使用此检索器。

初始化 `ChatWatsonx`。更多详情请参阅 [ChatWatsonx](/oss/python/integrations/chat/ibm_watsonx)。

```python
from langchain_ibm import ChatWatsonx

wx_chat = ChatWatsonx(
    model_id="meta-llama/llama-3-1-70b-instruct",
    url="https://us-south.ml.cloud.ibm.com",
    project_id="PASTE YOUR PROJECT_ID HERE",
)
```

```python
from langchain_classic.chains import RetrievalQA

chain = RetrievalQA.from_chain_type(llm=wx_chat, retriever=compression_retriever)
```

```python
chain.invoke(query)
```

```text
{'query': 'What did the president say about Ketanji Brown Jackson',
 'result': '总统说，他已提名巡回上诉法院法官凯坦吉·布朗·杰克逊在美国最高法院任职，并将她描述为“我国顶尖的法律人才之一”，将继续延续布雷耶大法官卓越的遗产。'}
```

---

## API 参考

有关所有 `WatsonxRerank` 功能和配置的详细文档，请访问 [API 参考](https://reference.langchain.com/python/integrations/langchain_ibm/WatsonxRerank/)。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs) 通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/retrievers/ibm_watsonx_ranker.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>