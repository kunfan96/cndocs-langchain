---
title: "Banana 集成"
description: "使用 LangChain Python 与 Banana LLM 进行集成。"
---

[Banana](https://www.banana.dev/about-us) 专注于构建机器学习基础设施。

本示例介绍如何使用 LangChain 与 Banana 模型进行交互。

```python
## 安装用于该集成的所需 langchain 包
pip install -qU  langchain-community
```

```python
# 安装包 https://docs.banana.dev/banana-docs/core-concepts/sdks/python
pip install -qU  banana-dev
```

```python
# 获取新令牌：https://app.banana.dev/
# 发起 Banana.dev API 调用需要三个参数：
# * 团队 API 密钥
# * 模型的唯一标识符（key）
# * 模型的 URL 路径别名（slug）

import os

# 您可以从主仪表板获取此信息
# 位于 https://app.banana.dev
os.environ["BANANA_API_KEY"] = "YOUR_API_KEY"
# 或者
# BANANA_API_KEY = getpass()
```

```python
from langchain_classic.chains import LLMChain
from langchain_community.llms import Banana
from langchain_core.prompts import PromptTemplate
```

```python
template = """Question: {question}

Answer: Let's think step by step."""

prompt = PromptTemplate.from_template(template)
```

```python
# 这两项均可在您的模型详情页中找到
# 网址为 https://app.banana.dev
llm = Banana(model_key="YOUR_MODEL_KEY", model_url_slug="YOUR_MODEL_URL_SLUG")
```

```python
llm_chain = LLMChain(prompt=prompt, llm=llm)
```

```python
question = "What NFL team won the Super Bowl in the year Justin Beiber was born?"

llm_chain.run(question)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/banana.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>