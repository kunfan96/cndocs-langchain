---
title: "Near 区块链集成"
description: "使用 LangChain Python 集成 Near 区块链文档加载器。"
---

本 notebook 旨在提供一种测试 LangChain 文档加载器在 Near 区块链上功能的方法。

此加载器目前支持：

* 从 NFT 智能合约（NEP-171 和 NEP-177）中以文档形式加载 NFT
* Near 主网、Near 测试网（默认为主网）
* Mintbase 的 Graph API

如果社区认为该加载器有价值，可以对其进行扩展。具体而言：

* 可以添加更多 API（例如与交易相关的 API）

此文档加载器需要：

* 一个免费的 [Mintbase API Key](https://docs.mintbase.xyz/dev/mintbase-graph/)

输出格式如下：

* pageContent= 单个 NFT
* metadata=\{'source': 'nft.yearofchef.near', 'blockchain': 'mainnet', 'tokenId': '1846'\}

## 将 NFT 加载到文档加载器

```python
# get MINTBASE_API_KEY from https://docs.mintbase.xyz/dev/mintbase-graph/

mintbaseApiKey = "..."
```

### 选项 1：以太坊主网（默认 BlockchainType）

```python
from MintbaseLoader import MintbaseDocumentLoader

contractAddress = "nft.yearofchef.near"  # Year of chef contract address


blockchainLoader = MintbaseDocumentLoader(
    contract_address=contractAddress, blockchain_type="mainnet", api_key="omni-site"
)

nfts = blockchainLoader.load()

print(nfts[:1])

for doc in blockchainLoader.lazy_load():
    print()
    print(type(doc))
    print(doc)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/document_loaders/mintbase.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接](/use-these-docs)到 Claude、VSCode 等，获取实时答案。
</Callout>
</div>
