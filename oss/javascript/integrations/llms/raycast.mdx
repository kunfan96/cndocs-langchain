---
title: "RaycastAI 集成"
description: "使用 LangChain JavaScript 与 RaycastAI LLM 集成。"
---

> **注意：** 这是一个社区构建的集成，并非由 Raycast 官方支持。

您可以在 [Raycast 环境](https://developers.raycast.com/api-reference/ai) 中使用 LangChain 的 RaycastAI 类，以利用 LangChain 的功能增强您的 Raycast 扩展。

- 截至 2023 年 8 月，RaycastAI 类仅在 Raycast 环境中可用，并且仅对 [Raycast Pro](https://www.raycast.com/pro) 用户开放。更多信息，请参阅[如何创建 Raycast 扩展](https://developers.raycast.com/)。

- 每个 Raycast Pro 用户的速率限制约为每分钟 10 个请求。如果超出此限制，您将收到错误。您可以通过向 `RaycastAI` 构造函数传递 `rateLimitPerMinute` 来设置所需的每分钟请求数限制，如示例所示，因为此速率限制将来可能会更改。

<Tip>
有关安装 LangChain 包的一般说明，请参阅[此部分](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```
```ts
import { RaycastAI } from "@langchain/community/llms/raycast";

import { Tool } from "@langchain/core/tools";

const model = new RaycastAI({
  rateLimitPerMinute: 10, // 默认值为 10，因此您可以省略此行
  model: "<model_name>",
  creativity: 0, // `creativity` 是 Raycast 使用的一个术语，等同于其他一些 LLM 中的 `temperature`
});
```

## 相关内容


- [模型指南](/oss/javascript/langchain/models)

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/llms/raycast.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>