---
title: "MistralAI 集成"
description: "使用 LangChain JavaScript 与 MistralAI LLM 集成。"
---

<Warning>
您当前正在查看的页面记录了将 Mistral 模型用作文本补全模型的用法。Mistral 上许多流行的模型是[聊天补全模型](/oss/javascript/langchain/models)。

您可能正在寻找[此页面](/oss/javascript/integrations/chat/mistral/)。
</Warning>

<Tip>
**想在本地运行 Mistral 的模型？请查看我们的 [Ollama 集成](/oss/javascript/integrations/chat/ollama)。**
</Tip>

[Mistral AI](https://mistral.ai/) 是一个为其强大的[开源模型](https://docs.mistral.ai/getting-started/models/)提供托管服务的平台。

本文档将帮助您开始使用 LangChain 的 MistralAI 补全模型（LLM）。有关 `MistralAI` 功能和配置选项的详细文档，请参阅 [API 参考](https://reference.langchain.com/javascript/langchain-mistralai/MistralAI)。

## 概述

### 集成详情

| 类 | 包 | 本地 | 可序列化 | Python 支持 | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [`MistralAI`](https://reference.langchain.com/javascript/langchain-mistralai/MistralAI) | [`@langchain/mistralai`](https://www.npmjs.com/package/@langchain/mistralai) | ❌ | ✅ | ❌ | ![NPM - Downloads](https://img.shields.io/npm/dm/@langchain/mistralai?style=flat-square&label=%20&) | ![NPM - Version](https://img.shields.io/npm/v/@langchain/mistralai?style=flat-square&label=%20&) |

## 设置

要访问 MistralAI 模型，您需要创建一个 MistralAI 账户，获取 API 密钥，并安装 `@langchain/mistralai` 集成包。

### 凭证

前往 [console.mistral.ai](https://console.mistral.ai/) 注册 MistralAI 并生成 API 密钥。完成此操作后，设置 `MISTRAL_API_KEY` 环境变量：

```bash
export MISTRAL_API_KEY="your-api-key"
```

如果您想自动跟踪模型调用，也可以通过取消注释以下内容来设置您的 [LangSmith](/langsmith/home) API 密钥：

```bash
# export LANGSMITH_TRACING="true"
# export LANGSMITH_API_KEY="your-api-key"
```

### 安装

LangChain MistralAI 集成位于 `@langchain/mistralai` 包中：

<CodeGroup>
```bash npm
npm install @langchain/mistralai @langchain/core
```
```bash yarn
yarn add @langchain/mistralai @langchain/core
```
```bash pnpm
pnpm add @langchain/mistralai @langchain/core
```
</CodeGroup>

## 实例化

现在我们可以实例化我们的模型对象并生成聊天补全：

```typescript
import { MistralAI } from "@langchain/mistralai"

const llm = new MistralAI({
  model: "codestral-latest",
  temperature: 0,
  maxTokens: undefined,
  maxRetries: 2,
  // 其他参数...
})
```

## 调用

```typescript
const inputText = "MistralAI 是一家 AI 公司，"

const completion = await llm.invoke(inputText)
completion
```

```text
 开发了 Mistral 7B，这是一个开源且可用于商业用途的大型语言模型（LLM）。Mistral 7B 是一个拥有 70 亿参数的模型，它在一个多样化且高质量的数据集上进行训练，并经过微调以在各种任务上表现良好，包括文本生成、问答和代码解释。

MistralAI 以宽松的许可证提供 Mistral 7B，允许任何人将该模型用于商业目的而无需支付任何费用。这使得 Mistral 7B 成为希望利用大型语言模型能力而无需承担高昂成本的企业和组织的热门选择。

Mistral 7B 在多样化且高质量的数据集上进行训练，这使其能够在各种任务上表现良好。它经过微调以生成连贯且上下文相关的文本，并且已被证明能够回答复杂问题和解释代码。

Mistral 7B 也是一个高效的模型，能够快速处理文本。这使其非常适合需要实时响应的应用程序，例如聊天机器人和虚拟助手。

总的来说，Mistral 7B 是一个强大且多功能的大型语言模型，开源且可用于商业用途。它在各种任务上的表现、效率以及宽松的许可证使其成为希望利用大型语言模型能力的企业和组织的热门选择。
```

## 钩子

Mistral AI 支持三个事件的自定义钩子：beforeRequest、requestError 和 response。每种钩子类型的函数签名示例如下：

```typescript
const beforeRequestHook = (req: Request): Request | void | Promise<Request | void> => {
    // 在 Mistral 处理请求之前运行的代码
};

const requestErrorHook = (err: unknown, req: Request): void | Promise<void> => {
    // 在 Mistral 处理请求时发生错误时运行的代码
};

const responseHook = (res: Response, req: Request): void | Promise<void> => {
    // 在 Mistral 发送成功响应之前运行的代码
};
```

要将这些钩子添加到聊天模型，可以将它们作为参数传递，它们会自动添加：

```typescript
import { ChatMistralAI } from "@langchain/mistralai"

const modelWithHooks = new ChatMistralAI({
    model: "mistral-large-latest",
    temperature: 0,
    maxRetries: 2,
    beforeRequestHooks: [ beforeRequestHook ],
    requestErrorHooks: [ requestErrorHook ],
    responseHooks: [ responseHook ],
    // 其他参数...
});
```

或者在实例化后手动分配并添加它们：

```typescript
import { ChatMistralAI } from "@langchain/mistralai"

const model = new ChatMistralAI({
    model: "mistral-large-latest",
    temperature: 0,
    maxRetries: 2,
    // 其他参数...
});

model.beforeRequestHooks = [ ...model.beforeRequestHooks, beforeRequestHook ];
model.requestErrorHooks = [ ...model.requestErrorHooks, requestErrorHook ];
model.responseHooks = [ ...model.responseHooks, responseHook ];

model.addAllHooksToHttpClient();
```

方法 `addAllHooksToHttpClient` 会清除所有当前添加的钩子，然后再将整个更新的钩子列表分配给模型，以避免钩子重复。

可以一次移除一个钩子，也可以一次性清除模型中的所有钩子。

```typescript
model.removeHookFromHttpClient(beforeRequestHook);

model.removeAllHooksFromHttpClient();
```

---

## API 参考

有关所有 `MistralAI` 功能和配置的详细文档，请前往 [API 参考](https://reference.langchain.com/javascript/langchain-mistralai/MistralAI)。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/llms/mistral.mdx) 或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>