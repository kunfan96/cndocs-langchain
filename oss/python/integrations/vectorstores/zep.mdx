---
title: "Zep 集成"
description: "使用 LangChain Python 与 Zep 向量存储进行集成。"
---

> 从聊天历史中回忆、理解并提取数据。为个性化 AI 体验提供动力。

> [Zep](https://www.getzep.com) 是 AI 助手应用的长期记忆服务。
> 借助 Zep，您可以为 AI 助手提供回忆过去对话的能力，无论对话多么久远，
> 同时还能减少幻觉、延迟和成本。

> 对 Zep Cloud 感兴趣？请参阅 [Zep Cloud 安装指南](https://help.getzep.com/sdks) 和 [Zep Cloud 向量存储示例](https://help.getzep.com/langchain/examples/vectorstore-example)

## 开源安装与设置

> Zep 开源项目：[https://github.com/getzep/zep](https://github.com/getzep/zep)
>
> Zep 开源文档：[https://docs.getzep.com/](https://docs.getzep.com/)

您需要使用 `pip install -qU langchain-community` 安装 `langchain-community` 才能使用此集成。

## 用法

在下面的示例中，我们使用 Zep 的自动嵌入功能，该功能使用低延迟嵌入模型在 Zep 服务器上自动嵌入文档。

## 注意

- 这些示例使用 Zep 的异步接口。通过从方法名称中移除 `a` 前缀来调用同步接口。
- 如果您传入 [`Embeddings`](https://reference.langchain.com/python/langchain-core/embeddings/embeddings/Embeddings) 实例，Zep 将使用此实例来嵌入文档，而不是自动嵌入。
  您还必须将文档集合设置为 `isAutoEmbedded === false`。
- 如果您将集合设置为 `isAutoEmbedded === false`，则必须传入 [`Embeddings`](https://reference.langchain.com/python/langchain-core/embeddings/embeddings/Embeddings) 实例。

## 从文档加载或创建集合

```python
from uuid import uuid4

from langchain_community.document_loaders import WebBaseLoader
from langchain_community.vectorstores import ZepVectorStore
from langchain_community.vectorstores.zep import CollectionConfig
from langchain_text_splitters import RecursiveCharacterTextSplitter

ZEP_API_URL = "http://localhost:8000"  # 这是您的 Zep 实例的 API 地址
ZEP_API_KEY = "<optional_key>"  # 您的 Zep 实例的可选 API 密钥
collection_name = f"babbage{uuid4().hex}"  # 唯一的集合名称。仅限字母数字

# 如果我们要创建新的 Zep 集合，则需要集合配置
config = CollectionConfig(
    name=collection_name,
    description="<optional description>",
    metadata={"optional_metadata": "associated with the collection"},
    is_auto_embedded=True,  # 我们将让 Zep 使用其低延迟嵌入器嵌入我们的文档
    embedding_dimensions=1536,  # 这应该与您为 Zep 配置的模型相匹配。
)

# 加载文档
article_url = "https://www.gutenberg.org/cache/epub/71292/pg71292.txt"
loader = WebBaseLoader(article_url)
documents = loader.load()

# 将其拆分为块
text_splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=0)
docs = text_splitter.split_documents(documents)

# 实例化 VectorStore。由于集合在 Zep 中尚不存在，
# 它将被创建并填充我们传入的文档。
vs = ZepVectorStore.from_documents(
    docs,
    collection_name=collection_name,
    config=config,
    api_url=ZEP_API_URL,
    api_key=ZEP_API_KEY,
    embedding=None,  # 我们将让 Zep 使用其低延迟嵌入器嵌入我们的文档
)
```

```python
# 等待集合嵌入完成


async def wait_for_ready(collection_name: str) -> None:
    import time

    from zep_python import ZepClient

    client = ZepClient(ZEP_API_URL, ZEP_API_KEY)

    while True:
        c = await client.document.aget_collection(collection_name)
        print(
            "嵌入状态: "
            f"{c.document_embedded_count}/{c.document_count} 个文档已嵌入"
        )
        time.sleep(1)
        if c.status == "ready":
            break


await wait_for_ready(collection_name)
```

```text
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 0/401 个文档已嵌入
嵌入状态: 401/401 个文档已嵌入
```

## 对集合进行相似性搜索查询

```python
# 查询它
query = "我们太阳系的结构是什么？"
docs_scores = await vs.asimilarity_search_with_relevance_scores(query, k=3)

# 打印结果
for d, s in docs_scores:
    print(d.page_content, " -> ", s, "\n====\n")
```

```text
两个主要行星的位置（对于航海者来说这些是最
必要的），木星和土星，每个都需要不少于
一百一十六个表格。然而，不仅需要
预测这些天体的位置，而且同样需要
将木星的四颗卫星的运动制成表格，以预测
它们进入其阴影的确切时间，以及它们的阴影
穿过其圆面的时间，以及它们被遮挡的时间  ->  0.9003241539387915
====

为导航（在此术语的广义上）提供超过一小部分的帮助，而通过
更轻松、更快速、更经济地计算和打印表格，它可能被用于
提供这种帮助。

确定行星位置所需的表格与太阳、月亮和星星所需的表格
同样必要。当我们说明时，可以形成对这些表格的数量和复杂性的
一些概念  ->  0.8911165633479508
====

如此应用的符号方案立即暗示了
它作为表达动物系统结构、操作和循环的工具
所必须具备的优势；我们毫不怀疑它足以
实现这一目的。不仅人体和动物身体固体部分的
机械连接，而且包括肌肉、皮肤、膜等较软部分的
结构和操作，其性质、运动、  ->  0.8899750214770481
====
```

## 按 MMR 重新排序搜索集合

Zep 提供原生、硬件加速的 MMR 重新排序搜索结果。

```python
query = "我们太阳系的结构是什么？"
docs = await vs.asearch(query, search_type="mmr", k=3)

for d in docs:
    print(d.page_content, "\n====\n")
```

```text
两个主要行星的位置（对于航海者来说这些是最
必要的），木星和土星，每个都需要不少于
一百一十六个表格。然而，不仅需要
预测这些天体的位置，而且同样需要
将木星的四颗卫星的运动制成表格，以预测
它们进入其阴影的确切时间，以及它们的阴影
穿过其圆面的时间，以及它们被遮挡的时间
====

如此应用的符号方案立即暗示了
它作为表达动物系统结构、操作和循环的工具
所必须具备的优势；我们毫不怀疑它足以
实现这一目的。不仅人体和动物身体固体部分的
机械连接，而且包括肌肉、皮肤、膜等较软部分的
结构和操作，其性质、运动、
====

阻力，节省时间，协调机制，并赋予
整个机械动作以最大的实际完美性。

通过机械装置系统实现此处
试图描述的结果，仅是
在此机械中采用的一种权宜之计；--尽
管其动作如此完美，在任何普通情况下，它们都会被视为
以几乎多余的程度实现了预期的目标
====
```

# 按元数据过滤

使用元数据过滤器缩小结果范围。首先，加载另一本书：“福尔摩斯探案集”

```python
# 让我们向现有集合添加更多内容
article_url = "https://www.gutenberg.org/files/48320/48320-0.txt"
loader = WebBaseLoader(article_url)
documents = loader.load()

# 将其拆分为块
text_splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=0)
docs = text_splitter.split_documents(documents)

await vs.aadd_documents(docs)

await wait_for_ready(collection_name)
```

```text
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 401/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 901/1691 个文档已嵌入
嵌入状态: 1401/1691 个文档已嵌入
嵌入状态: 1401/1691 个文档已嵌入
嵌入状态: 1401/1691 个文档已嵌入
嵌入状态: 1401/1691 个文档已嵌入
嵌入状态: 1691/1691 个文档已嵌入
```

我们看到来自两本书的结果。注意 `source` 元数据

```python
query = "他对天文学感兴趣吗？"
docs = await vs.asearch(query, search_type="similarity", k=3)

for d in docs:
    print(d.page_content, " -> ", d.metadata, "\n====\n")
```

```text
或间接地，为此目的。但除此之外，还有大量
专门用于天文学的表格同样是必不可少的。天文学家
关于天体位置和运动的预测是
航海者能够推进其技艺的手段，也是唯一的手段。通过这些，他能够
发现他的船与赤道线的距离，以及他的  ->  {'source': 'https://www.gutenberg.org/cache/epub/71292/pg71292.txt'}
====

拥有所有可能对其工作有用的知识，
而我已努力在我的情况下做到这一点。如果我没记错的话，
您有一次，在我们友谊的早期，以非常精确的方式
定义了我的界限。”

“是的，”我笑着回答。“那是一份独特的文件。哲学、
天文学和政治被标记为零，我记得。植物学
可变，地质学关于任何地区的泥渍  ->  {'source': 'https://www.gutenberg.org/files/48320/48320-0.txt'}
====

天文学及其相关科学，以及依赖它们的各种艺术。
在这些领域中，计算没有比天文学
特别需要的更繁琐的了；--在这些领域中，预备设施
没有比这更必要的了；--在这些领域中，错误
没有比这更有害的了。实践天文学家
在其追求中被打断，并从其观察任务中分心，
因为计算的繁琐劳动，或者他观察的勤奋
因缺乏更强大的勤奋而变得无效  ->  {'source': 'https://www.gutenberg.org/cache/epub/71292/pg71292.txt'}
====
```

现在，我们设置一个过滤器

```python
filter = {
    "where": {
        "jsonpath": (
            "$[*] ? (@.source == 'https://www.gutenberg.org/files/48320/48320-0.txt')"
        )
    },
}

docs = await vs.asearch(query, search_type="similarity", metadata=filter, k=3)

for d in docs:
    print(d.page_content, " -> ", d.metadata, "\n====\n")
```

```text
拥有所有可能对其工作有用的知识，
而我已努力在我的情况下做到这一点。如果我没记错的话，
您有一次，在我们友谊的早期，以非常精确的方式
定义了我的界限。”

“是的，”我笑着回答。“那是一份独特的文件。哲学、
天文学和政治被标记为零，我记得。植物学
可变，地质学关于任何地区的泥渍  ->  {'source': 'https://www.gutenberg.org/files/48320/48320-0.txt'}
====

光线照在他坚毅的鹰钩鼻特征上。他就这样坐着，当我
打瞌睡时，他就这样坐着，当一声突然的惊呼让我
醒来时，他也这样坐着，我发现夏日的阳光照进了房间。
烟斗仍在他唇间，烟雾仍向上缭绕，
房间里充满了浓密的烟草雾气，但前一晚
我看到的那堆烟丝已荡然无存。

“醒了吗，华生？”他问道。

“是的。”

“早上开车兜风怎么样？”  ->  {'source': 'https://www.gutenberg.org/files/48320/48320-0.txt'}
====

“我瞥了一眼桌上的书，尽管我不懂德语，
但我能看出其中两本是科学论文，
其余的是诗集。然后我走到窗边，
希望能瞥见乡村景色，但一扇橡木百叶窗，
被重重地闩住，折叠在窗户上。这是一座
异常寂静的房子。走廊某处有一个旧时钟
在滴答作响，但除此之外一切都死寂无声。一种模糊的感觉  ->  {'source': 'https://www.gutenberg.org/files/48320/48320-0.txt'}
====
```

```python

```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/vectorstores/zep.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接](/use-these-docs) 到 Claude、VSCode 等，以获取实时答案。
</Callout>
</div>
