---
title: "ChatXinference 集成"
description: "使用 LangChain Python 与 ChatXinference 聊天模型集成。"
---

[Xinference](https://github.com/xorbitsai/inference) 是一个功能强大且用途广泛的库，旨在提供大语言模型、语音识别模型和多模态模型的服务，甚至可以在你的笔记本电脑上运行。它支持多种与 GGML 兼容的模型，例如 chatglm、baichuan、whisper、vicuna、orca 等。

## 概述

### 集成详情

| 类 | 包 | 可序列化 | [JS 支持] | 下载量 | 版本 |
| :--- | :--- | :---: |  :---: | :---: | :---: |
| `ChatXinference`| langchain-xinference | ❌ | ✅ | ✅ | ✅ |

### 模型特性

| [工具调用](/oss/python/langchain/tools/) | [结构化输出](/oss/python/langchain/structured-output) | [图像输入](/oss/python/langchain/messages#multimodal) | 音频输入 | 视频输入 | [Token 级流式传输](/oss/python/langchain/streaming/) | 原生异步 | [Token 使用量](/oss/python/langchain/models#token-usage) | [对数概率](/oss/python/langchain/models#log-probabilities) |
| :---: |:----------------------------------------------------:| :---: |  :---: | :---: | :---: | :---: | :---: | :---: |
| ✅ |                          ✅                           | ❌ | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |

## 设置

通过 PyPI 安装 `Xinference`：

```python
pip install -qU  "xinference[all]"
```

### 在本地或分布式集群中部署 xinference

对于本地部署，运行 `xinference`。

要在集群中部署 Xinference，首先使用 `xinference-supervisor` 启动一个 Xinference 主节点。你也可以使用选项 -p 指定端口，-H 指定主机。默认端口是 8080，默认主机是 0.0.0.0。

然后，在你希望运行它们的每个服务器上使用 `xinference-worker` 启动 Xinference 工作节点。

你可以查阅 [Xinference](https://github.com/xorbitsai/inference) 的 README 文件以获取更多信息。

### 封装器

要将 Xinference 与 LangChain 一起使用，你需要先启动一个模型。你可以使用命令行界面 (CLI) 来完成：

```python
%xinference launch -n vicuna-v1.3 -f ggmlv3 -q q4_0
```

```text
模型 UID：7167b2b0-2a04-11ee-83f0-d29396a3f064
```

会返回一个模型 UID 供你使用。现在你可以将 Xinference 与 LangChain 一起使用：

## 安装

LangChain Xinference 集成位于 `langchain-xinference` 包中：

```python
pip install -qU langchain-xinference
```

确保你使用的是最新版本的 Xinference 以支持结构化输出。

## 实例化

现在我们可以实例化我们的模型对象并生成聊天补全：

```python
from langchain_xinference.chat_models import ChatXinference

llm = ChatXinference(
    server_url="your_server_url", model_uid="7167b2b0-2a04-11ee-83f0-d29396a3f064"
)

llm.invoke(
    "问：在法国的首都我们可以参观哪里？",
    config={"max_tokens": 1024},
)
```

## 调用

```python
from langchain.messages import HumanMessage, SystemMessage
from langchain_xinference.chat_models import ChatXinference

llm = ChatXinference(
    server_url="your_server_url", model_uid="7167b2b0-2a04-11ee-83f0-d29396a3f064"
)

system_message = "你是一个将英语翻译成法语的有用助手。翻译用户句子。"
human_message = "我热爱编程。"

llm.invoke([HumanMessage(content=human_message), SystemMessage(content=system_message)])
```

---

## API 参考

有关所有 `ChatXinference` 功能和配置的详细文档，请前往 API 参考：[github.com/TheSongg/langchain-xinference](https://github.com/TheSongg/langchain-xinference)

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/chat/xinference.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>