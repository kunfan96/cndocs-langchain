---
title: "文本嵌入推理集成"
description: "使用 LangChain Python 集成文本嵌入推理嵌入模型。"
---

>[Hugging Face 文本嵌入推理 (TEI)](https://huggingface.co/docs/text-embeddings-inference/index) 是一个用于部署和提供开源文本嵌入与序列分类模型的 toolkits。`TEI` 能够为最受欢迎的模型（包括 `FlagEmbedding`、`Ember`、`GTE` 和 `E5`）实现高性能提取。

要在 LangChain 中使用它，首先安装 `huggingface-hub`。

```python
pip install -U huggingface-hub
```

然后使用 TEI 暴露一个嵌入模型。例如，使用 Docker，你可以按如下方式提供 `BAAI/bge-large-en-v1.5` 服务：

```bash
model=BAAI/bge-large-en-v1.5
revision=refs/pr/5
volume=$PWD/data # 与 Docker 容器共享卷，以避免每次运行都下载权重

docker run --gpus all -p 8080:80 -v $volume:/data --pull always ghcr.io/huggingface/text-embeddings-inference:0.6 --model-id $model --revision $revision
```

Docker 使用的具体细节可能因底层硬件而异。例如，要在 Intel Gaudi/Gaudi2 硬件上提供该模型服务，请参阅 [tei-gaudi 仓库](https://github.com/huggingface/tei-gaudi) 以获取相关的 `docker run` 命令。

最后，实例化客户端并嵌入你的文本。

```python
from langchain_huggingface.embeddings import HuggingFaceEndpointEmbeddings
```

```python
embeddings = HuggingFaceEndpointEmbeddings(model="http://localhost:8080")
```

```python
text = "What is deep learning?"
```

```python
query_result = embeddings.embed_query(text)
query_result[:3]
```

```text
[0.018113142, 0.00302585, -0.049911194]
```

```python
doc_result = embeddings.embed_documents([text])
```

```python
doc_result[0][:3]
```

```text
[0.018113142, 0.00302585, -0.049911194]
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/text_embeddings_inference.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>