---
title: "Decodo 集成"
description: "使用 LangChain JavaScript 与 Decodo 工具集成。"
---

[@decodo/langchain-ts](https://www.npmjs.com/package/@decodo/langchain-ts) 包使开发者能够在 LangChain 应用程序中使用 Decodo 的网页抓取 API。

网页抓取 API 的特点：

- 轻松访问网页数据。简化从网站和在线来源检索信息的过程。
- 地理灵活性。无论区域限制如何，均可访问内容。
- 可靠的抓取。采用先进技术以避免检测和封锁。

## 功能特性

@decodo/langchain-ts 插件的功能特性：

- **网页抓取**：抓取任何 URL 并检索 Markdown 内容
- **Google 搜索**：搜索 Google 并检索结构化结果
- **Amazon 搜索**：搜索 Amazon 并检索结构化产品数据
- **地理位置选择**，用于位置敏感的内容
- **JavaScript 渲染**，用于需要真实浏览器的目标
- **Markdown 输出**，实现与 LLM 的高效令牌连接

## 安装

```bash
npm install @decodo/langchain-ts
```

## 快速开始

要使用此项目中的工具，您需要订阅 [Decodo 高级网页抓取 API](https://help.decodo.com/docs/web-scraping-api-core-and-advanced-plans)。免费试用可在 [控制面板](https://dashboard.decodo.com/) 获取。

## 示例

每个工具的一组代理示例。

### 通用工具

```typescript
import dotenv from "dotenv";
import { ChatOpenAI } from "@langchain/openai";
import { createAgent } from "@langchain/agents";
import { DecodoUniversalTool } from "@decodo/langchain-ts";

dotenv.config();

const main = async () => {
  const username = process.env.SCRAPER_API_USERNAME!;
  const password = process.env.SCRAPER_API_PASSWORD!;

  const decodoUniversalTool = new DecodoUniversalTool({ username, password });

  const model = new ChatOpenAI({
    model: "gpt-5.4-mini",
  });

  const agent = createAgent({
    llm: model,
    tools: [decodoUniversalTool],
  });

  const result = await agent.invoke({
    messages: [
      {
        role: "user",
        content:
          "抓取维基百科的 NBA 2025 赛季页面，并告诉我 2025 年谁赢了？",
      },
    ],
  });

  console.log(result.messages[result.messages.length - 1].content);
};

if (require.main === module) {
  main();
}
```

更多示例，请参阅 GitHub 上的 [@decodo/langchain-ts](https://github.com/Decodo/decodo-langchain-ts)。

### Google 搜索

```typescript
const main = async () => {
  const username = process.env.SCRAPER_API_USERNAME!;
  const password = process.env.SCRAPER_API_PASSWORD!;

  const decodoGoogleSearchTool = new DecodoGoogleSearchTool({
    username,
    password,
  });

  const model = new ChatOpenAI({
    model: "gpt-5-mini",
  });

  const agent = createAgent({
    llm: model,
    tools: [decodoGoogleSearchTool],
  });

  const prompt =
    "在德国，哪家移动服务提供商在 Google 上排名第一？";

  const result = await agent.invoke({
    messages: [
      {
        role: "user",
        content: prompt,
      },
    ],
  });

  console.log(result.messages[result.messages.length - 1].content);
};
```

### Amazon 搜索

```typescript
const main = async () => {
  const username = process.env.SCRAPER_API_USERNAME!;
  const password = process.env.SCRAPER_API_PASSWORD!;

  const decodoAmazonSearchTool = new DecodoAmazonSearchTool({
    username,
    password,
  });

  const model = new ChatOpenAI({
    model: "gpt-5-mini",
  });

  const agent = createAgent({
    llm: model,
    tools: [decodoAmazonSearchTool],
  });

  const prompt =
    "在法国 Amazon 上，配备 GeForce RTX 5080 的最便宜笔记本电脑是什么？";

  const result = await agent.invoke({
    messages: [
      {
        role: "user",
        content: prompt,
      },
    ],
  });

  console.log(result.messages[result.messages.length - 1].content);
};
```

## 配置

所有工具都接受一个 `DecodoConfig` 对象：

```typescript
type DecodoConfig = {
  username: string; // 您的 Web Advanced 产品用户名
  password: string; // 您的 Web Advanced 产品密码
};
```

## API 参数

有关可用参数列表，请参阅 [抓取 API 文档](https://help.decodo.com/docs/web-scraping-api-parameters)。

## 许可证

MIT

## 支持

如需支持，请访问 [Decodo 的文档](https://help.decodo.com/) 或在 GitHub 上提交 issue。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs) 通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/tools/decodo.mdx) 或 [提交 issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>