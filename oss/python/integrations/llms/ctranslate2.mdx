---
title: "CTranslate2 集成"
description: "使用 LangChain Python 与 CTranslate2 LLM 进行集成。"
---

**CTranslate2** 是一个用于 Transformer 模型高效推理的 C++ 和 Python 库。

该项目实现了一个自定义运行时，应用了多种性能优化技术，如权重量化、层融合、批次重排序等，以加速并降低 Transformer 模型在 CPU 和 GPU 上的内存占用。

完整的功能列表和支持的模型包含在[项目仓库](https://opennmt.net/CTranslate2/guides/transformers.html)中。开始使用前，请查阅官方[快速入门指南](https://opennmt.net/CTranslate2/quickstart.html)。

要使用它，您需要安装 `ctranslate2` Python 包。

```python
pip install -qU  ctranslate2
```

要在 CTranslate2 中使用 Hugging Face 模型，必须首先使用 `ct2-transformers-converter` 命令将其转换为 CTranslate2 格式。该命令需要预训练模型名称和转换后模型目录的路径作为参数。

```python
# 对话生成可能需要几分钟时间
!ct2-transformers-converter --model meta-llama/Llama-2-7b-hf --quantization bfloat16 --output_dir ./llama-2-7b-ct2 --force
```

```text
加载检查点分片：100%|██████████████████| 2/2 [00:01<00:00,  1.81it/s]
```

```python
from langchain_community.llms import CTranslate2

llm = CTranslate2(
    # 来自上方的 output_dir：
    model_path="./llama-2-7b-ct2",
    tokenizer_name="meta-llama/Llama-2-7b-hf",
    device="cuda",
    # device_index 可以是单个整数或整数列表，
    # 表示用于推理的 GPU ID：
    device_index=[0, 1],
    compute_type="bfloat16",
)
```

## 单次调用

```python
print(
    llm.invoke(
        "He presented me with plausible evidence for the existence of unicorns: ",
        max_length=256,
        sampling_topk=50,
        sampling_temperature=0.2,
        repetition_penalty=2,
        cache_static_prompt=False,
    )
)
```

```text
他向我提供了独角兽存在的合理证据：1）古代文献中有提及；而且对他来说更重要的是（这并不足以说服大多数人），他亲眼见过一只。我持怀疑态度，但我不想让我的朋友因为他的信念被直接否定而感到难过，没有任何考虑或为其辩护的论点——这也是我们之所以进行这场对话的原因！所以我转而询问，除了“独角兽化”之外是否还有其他解释……也许那是一只鸵鸟？或者可能只是另一种像斑马一样确实存在的马科动物，尽管当今活着的人类从未亲眼目睹过它们，原因可能是缺乏可及性/可用性等等。不过话说回来，这些动物在这里本来就不太为人所知……”于是，我们开始了关于这些生物是否真的存在于地球以外的其他地方的讨论，如今只有少数科学家敢于涉足那里，因为技术允许我们探索曾经被认为不可能跨越的边界，而在几个世纪前，旅行意味着只能靠双脚步行到达目的地 A->B，除非有人在深夜漆黑的树林中沿途指引方向。
```

## 多次调用

```python
print(
    llm.generate(
        ["The list of top romantic songs:\n1.", "The list of top rap songs:\n1."],
        max_length=128,
    )
)
```

```text
generations=[[Generation(text='顶级情歌列表：\n1. 惠特妮·休斯顿演唱的《I Will Always Love You》\n2. 埃尔维斯·普雷斯利演唱的《Can’t Help Falling in Love》\n3. 正义兄弟乐队演唱的《Unchained Melody》\n4. 多莉·帕顿演唱的《I Will Always Love You》\n5. 惠特妮·休斯顿演唱的《I Will Always Love You》\n6. 多莉·帕顿演唱的《I Will Always Love You》\n7. 披头士乐队演唱的《I Will Always Love You》\n8. The Rol', generation_info=None)], [Generation(text='顶级说唱歌曲列表：\n1. Drake 演唱的《God’s Plan》\n2. Post Malone 演唱的《Rockstar》\n3. Migos 演唱的《Bad and Boujee》\n4. Kendrick Lamar 演唱的《Humble》\n5. Cardi B 演唱的《Bodak Yellow》\n6. DJ Khaled 演唱的《I’m the One》\n7. Migos 演唱的《Motorsport》\n8. G-Eazy 演唱的《No Limit》\n9. Big Sean 演唱的《Bounce Back》\n10. “', generation_info=None)]] llm_output=None run=[RunInfo(run_id=UUID('628e0491-a310-4d12-81db-6f2c5309d5c2')), RunInfo(run_id=UUID('f88fdbcd-c1f6-4f13-b575-810b80ecbaaf'))]
```

## 将模型集成到 LLMChain 中

```python
from langchain_classic.chains import LLMChain
from langchain_core.prompts import PromptTemplate

template = """{question}

Let's think step by step. """
prompt = PromptTemplate.from_template(template)

llm_chain = LLMChain(prompt=prompt, llm=llm)

question = "Who was the US president in the year the first Pokemon game was released?"

print(llm_chain.run(question))
```

```text
第一款宝可梦游戏发布的那年，美国总统是谁？

让我们一步步思考。1996 年是第一款宝可梦游戏发布的年份。

\begin{blockquote}

\begin{itemize}
  \item 1996 年是比尔·克林顿担任总统的年份。
  \item 1996 年是第一款宝可梦游戏发布的年份。
  \item 1996 年是第一款宝可梦游戏发布的年份。

\end{itemize}
\end{blockquote}

我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。

评论：@JoeZ. 我不确定这是否是一个有效的问题，但我确信这是一个有趣的问题。
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/ctranslate2.mdx) 或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>