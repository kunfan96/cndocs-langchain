---
title: "ChatLlamaAPI 集成"
description: "使用 LangChain Python 与 ChatLlamaAPI 聊天模型集成。"
---

本笔记本展示了如何将 LangChain 与 [LlamaAPI](https://llama-api.com/) 结合使用——这是 Llama2 的托管版本，增加了对函数调用的支持。

pip install -qU  llamaapi

```python
from llamaapi import LlamaAPI

# 将 'Your_API_Token' 替换为您的实际 API 令牌
llama = LlamaAPI("Your_API_Token")
```

```python
from langchain_experimental.llms import ChatLlamaAPI
```

```text
/Users/harrisonchase/.pyenv/versions/3.9.1/envs/langchain/lib/python3.9/site-packages/deeplake/util/check_latest_version.py:32: UserWarning: A newer version of deeplake (3.6.12) is available. It's recommended that you update to the latest version using `pip install -U deeplake`.
  warnings.warn(
```

```python
model = ChatLlamaAPI(client=llama)
```

```python
from langchain_classic.chains import create_tagging_chain

schema = {
    "properties": {
        "sentiment": {
            "type": "string",
            "description": "段落中遇到的情感",
        },
        "aggressiveness": {
            "type": "integer",
            "description": "段落攻击性的 0-10 分评分",
        },
        "language": {"type": "string", "description": "段落的语言"},
    }
}

chain = create_tagging_chain(schema, model)
```

```python
chain.run("give me your money")
```

```python
{'sentiment': 'aggressive', 'aggressiveness': 8, 'language': 'english'}
```

```python

```

---

<div className="source-links">
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/chat/llama_api.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>