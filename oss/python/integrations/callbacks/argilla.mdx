---
title: "Argilla 集成"
description: "使用 LangChain Python 与 Argilla 回调集成。"
---

>[Argilla](https://argilla.io/) 是一个面向 LLM 的开源数据管理平台。
> 使用 Argilla，每个人都可以通过结合人工和机器反馈的更快数据管理来构建强大的语言模型。我们支持 MLOps 周期的每个步骤，从数据标注到模型监控。

<a target="_blank" href="https://colab.research.google.com/github/langchain-ai/langchain/blob/v0.3/docs/docs/integrations/callbacks/argilla.ipynb">
    <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="在 Colab 中打开"/>
</a>

在本指南中，我们将演示如何使用 `ArgillaCallbackHandler` 跟踪 LLM 的输入和响应，以在 Argilla 中生成数据集。

跟踪 LLM 的输入和输出以生成用于未来微调的数据集非常有用。当您使用 LLM 为特定任务（如问答、摘要或翻译）生成数据时，这尤其有用。

## 安装与设置

```python
pip install -qU  langchain langchain-openai argilla
```

### 获取 API 凭证

要获取 Argilla API 凭证，请按照以下步骤操作：

1. 转到您的 Argilla UI。
2. 点击您的个人资料图片并转到“我的设置”。
3. 然后复制 API 密钥。

在 Argilla 中，API URL 将与您的 Argilla UI 的 URL 相同。

要获取 OpenAI API 凭证，请访问 [platform.openai.com/account/api-keys](https://platform.openai.com/account/api-keys)

```python
import os

os.environ["ARGILLA_API_URL"] = "..."
os.environ["ARGILLA_API_KEY"] = "..."

os.environ["OPENAI_API_KEY"] = "..."
```

### 设置 argilla

要使用 `ArgillaCallbackHandler`，我们需要在 Argilla 中创建一个新的 `FeedbackDataset` 来跟踪您的 LLM 实验。为此，请使用以下代码：

```python
import argilla as rg
```

```python
from packaging.version import parse as parse_version

if parse_version(rg.__version__) < parse_version("1.8.0"):
    raise RuntimeError(
        "`FeedbackDataset` 仅在 Argilla v1.8.0 或更高版本中可用，请通过 `pip install argilla --upgrade` 升级 `argilla`。"
    )
```

```python
dataset = rg.FeedbackDataset(
    fields=[
        rg.TextField(name="prompt"),
        rg.TextField(name="response"),
    ],
    questions=[
        rg.RatingQuestion(
            name="response-rating",
            description="您如何评价响应的质量？",
            values=[1, 2, 3, 4, 5],
            required=True,
        ),
        rg.TextQuestion(
            name="response-feedback",
            description="您对响应有什么反馈？",
            required=False,
        ),
    ],
    guidelines="您被要求评价响应的质量并提供反馈。",
)

rg.init(
    api_url=os.environ["ARGILLA_API_URL"],
    api_key=os.environ["ARGILLA_API_KEY"],
)

dataset.push_to_argilla("langchain-dataset")
```

> 📌 注意：目前，`FeedbackDataset.fields` 仅支持提示-响应对，因此 `ArgillaCallbackHandler` 将只跟踪提示（即 LLM 输入）和响应（即 LLM 输出）。

## 跟踪

要使用 `ArgillaCallbackHandler`，您可以使用以下代码，或者只需重现以下部分中介绍的示例之一。

```python
from langchain_community.callbacks.argilla_callback import ArgillaCallbackHandler

argilla_callback = ArgillaCallbackHandler(
    dataset_name="langchain-dataset",
    api_url=os.environ["ARGILLA_API_URL"],
    api_key=os.environ["ARGILLA_API_KEY"],
)
```

### 场景 1：跟踪单个 LLM

首先，让我们只运行一个 LLM 几次，并在 Argilla 中捕获生成的提示-响应对。

```python
from langchain_core.callbacks.stdout import StdOutCallbackHandler
from langchain_openai import OpenAI

argilla_callback = ArgillaCallbackHandler(
    dataset_name="langchain-dataset",
    api_url=os.environ["ARGILLA_API_URL"],
    api_key=os.environ["ARGILLA_API_KEY"],
)
callbacks = [StdOutCallbackHandler(), argilla_callback]

llm = OpenAI(temperature=0.9, callbacks=callbacks)
llm.generate(["给我讲个笑话", "给我写首诗"] * 3)
```

```text
LLMResult(generations=[[Generation(text='\n\n问：鱼撞到墙时说了什么？ \n答：Dam（该死/水坝）。', generation_info={'finish_reason': 'stop', 'logprobs': None})], [Generation(text='\n\n月亮 \n\n月亮高悬在午夜的天空，\n像星星一样在上方闪耀。\n夜晚如此平和，如此宁静，\n充满了爱意。\n\n不断变化和更新，\n永恒优雅的光芒。\n月亮始终如一，\n提醒着生命的温柔节奏。\n\n穿越时空指引我们，\n永不褪色的希望灯塔。\n月亮照耀着我们所有人，\n当它继续升起和私奔。', generation_info={'finish_reason': 'stop', 'logprobs': None})], [Generation(text='\n\n问：一个磁铁对另一个磁铁说了什么？\n答：“我觉得你很有吸引力！”', generation_info={'finish_reason': 'stop', 'logprobs': None})], [Generation(text="\n\n世界充满了上帝的庄严。\n它会燃烧起来，像从摇晃的箔片中闪耀；\n它汇聚成伟大，像被压榨的油渗出。\n那么，为什么人们现在不敬畏他的杖呢？\n\n一代又一代走过，走过，走过；\n一切都被贸易灼伤；模糊，沾满辛劳；\n带着人类的污迹，分享人类的气味：土壤\n现在裸露，脚无法感受，因为穿着鞋。\n\n尽管如此，大自然从未耗尽；\n最深处的事物中存在着最珍贵的清新；\n尽管最后的光芒从黑暗的西方消失\n哦，早晨，在东方棕色的边缘，涌现——\n\n因为圣灵在弯曲的\n世界上空，用温暖的胸膛和啊！明亮的翅膀孵育。\n\n~杰拉尔德·曼利·霍普金斯", generation_info={'finish_reason': 'stop', 'logprobs': None})], [Generation(text='\n\n问：一个海洋对另一个海洋说了什么？\n答：什么也没说，它们只是挥了挥手。', generation_info={'finish_reason': 'stop', 'logprobs': None})], [Generation(text="\n\n给你的一首诗\n\n在绿色的田野上\n\n天空如此蔚蓝\n\n微风轻拂，太阳高悬\n\n美丽的世界，供我们去爱\n\n生活是一段旅程，充满惊喜\n\n充满欢乐和惊喜\n\n勇敢一点，迈出小步\n\n未来将随着深度而显现\n\n在早晨，当黎明到来\n\n新的开始，无需隐藏\n\n在路的某处，有一颗跳动的心\n\n相信自己，你总会成功。", generation_info={'finish_reason': 'stop', 'logprobs': None})]], llm_output={'token_usage': {'completion_tokens': 504, 'total_tokens': 528, 'prompt_tokens': 24}, 'model_name': 'text-davinci-003'})
```

![Argilla UI 显示 LangChain LLM 输入-响应](https://docs.argilla.io/en/latest/_images/llm.png)

### 场景 2：跟踪链中的 LLM

然后我们可以使用提示模板创建一个链，然后在 Argilla 中跟踪初始提示和最终响应。

```python
from langchain_classic.chains import LLMChain
from langchain_core.callbacks.stdout import StdOutCallbackHandler
from langchain_core.prompts import PromptTemplate
from langchain_openai import OpenAI

argilla_callback = ArgillaCallbackHandler(
    dataset_name="langchain-dataset",
    api_url=os.environ["ARGILLA_API_URL"],
    api_key=os.environ["ARGILLA_API_KEY"],
)
callbacks = [StdOutCallbackHandler(), argilla_callback]
llm = OpenAI(temperature=0.9, callbacks=callbacks)

template = """你是一位剧作家。给定一个戏剧的标题，你的工作是为该标题写一个剧情简介。
标题：{title}
剧作家：这是上述戏剧的剧情简介："""
prompt_template = PromptTemplate(input_variables=["title"], template=template)
synopsis_chain = LLMChain(llm=llm, prompt=prompt_template, callbacks=callbacks)

test_prompts = [{"title": "关于巴黎大脚怪的纪录片"}]
synopsis_chain.apply(test_prompts)
```

```text
> 进入新的 LLMChain 链...
格式化后的提示：
你是一位剧作家。给定一个戏剧的标题，你的工作是为该标题写一个剧情简介。
标题：关于巴黎大脚怪的纪录片
剧作家：这是上述戏剧的剧情简介：

> 链完成。
```

```text
[{'text': "\n\n《关于巴黎大脚怪的纪录片》聚焦于一位纪录片电影制作人的故事，以及他们在巴黎市寻找传说中的大脚怪生物证据的过程。该剧跟随电影制作人探索这座城市，遇见来自各行各业、曾与神秘生物有过接触的人们。通过他们的对话，电影制作人揭开了大脚怪的故事，并发现了该生物在巴黎存在的真相。随着故事的进展，电影制作人对神秘生物以及城市居民的不同观点有了越来越多的了解，以及他们对这个生物的看法。最终，电影制作人的发现引导他们得出了一些关于该生物存在及其在巴黎人民生活中重要性的令人惊讶且温暖的结论。"}]
```

![Argilla UI 显示 LangChain 链输入-响应](https://docs.argilla.io/en/latest/_images/chain.png)

### 场景 3：使用带有工具的代理

最后，作为一个更高级的工作流，您可以创建一个使用一些工具的代理。`ArgillaCallbackHandler` 将跟踪输入和输出，但不会跟踪中间步骤/思考。给定一个提示，我们记录原始提示和最终响应。

> 请注意，对于此场景，我们将使用 Google Search API (Serp API)，因此您需要同时安装 `google-search-results`（通过 `pip install google-search-results`），并将 Serp API 密钥设置为 `os.environ["SERPAPI_API_KEY"] = "..."`（您可以在 [serpapi.com/dashboard](https://serpapi.com/dashboard) 找到它），否则下面的示例将无法工作。

```python
from langchain.agents import create_agent, load_tools
from langchain_core.callbacks.stdout import StdOutCallbackHandler
from langchain_openai import OpenAI

argilla_callback = ArgillaCallbackHandler(
    dataset_name="langchain-dataset",
    api_url=os.environ["ARGILLA_API_URL"],
    api_key=os.environ["ARGILLA_API_KEY"],
)

callbacks = [StdOutCallbackHandler(), argilla_callback]
llm = OpenAI(temperature=0.9, callbacks=callbacks)

tools = load_tools(["serpapi"], llm=llm, callbacks=callbacks)

agent = create_agent(
    model=llm,
    tools=tools,
    callbacks=callbacks,
)

agent.invoke("谁是美利坚合众国的第一任总统？")
```

```text
> 进入新的 AgentExecutor 链...
 我需要回答一个历史问题
操作：搜索
操作输入："谁是美利坚合众国的第一任总统"
观察：乔治·华盛顿
思考：乔治·华盛顿是第一任总统
最终答案：乔治·华盛顿是美利坚合众国的第一任总统。

> 链完成。
```

```text
'乔治·华盛顿是美利坚合众国的第一任总统。'
```

![Argilla UI 显示 LangChain 代理输入-响应](https://docs.argilla.io/en/latest/_images/agent.png)

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/callbacks/argilla.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>