---
title: "OCI 生成式 AI 与 LangChain 集成"
description: "使用 LangChain Python 与 OCI 生成式 AI 集成。"
sidebarTitle: "OCI"
---

本页涵盖所有与 [Oracle Cloud Infrastructure (OCI)](https://www.oracle.com/cloud/) 的 LangChain 集成。

## 安装与设置

<CodeGroup>
```bash pip
pip install langchain-oci oci
```

```bash uv
uv add langchain-oci oci
```
</CodeGroup>

## 身份验证

<Note>
    初始化 [ChatOCIGenAI](#chatocigenai) 是资源密集型操作。为获得最佳性能，建议将此客户端视为单例，并在您的应用程序中重用该实例。
</Note>

OCI 服务支持四种身份验证方法。所有方法均遵循 [标准 OCI SDK 身份验证](https://docs.oracle.com/en-us/iaas/Content/API/Concepts/sdk_authentication_methods.htm)。

### API 密钥（默认）

使用 `~/.oci/config` 中的凭据：

```python
from langchain_oci import ChatOCIGenAI

llm = ChatOCIGenAI(
    model_id="meta.llama-3.3-70b-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..xxx",
    auth_type="API_KEY",        # 默认
    auth_profile="DEFAULT",      # ~/.oci/config 中的配置文件名称
)
```

### 安全令牌

用于基于会话的身份验证：

```bash
oci session authenticate --profile-name MY_PROFILE
```

```python
llm = ChatOCIGenAI(
    model_id="meta.llama-3.3-70b-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..your-compartment-id",
    auth_type="SECURITY_TOKEN",
    auth_profile="MY_PROFILE",
)
```

### 实例主体

用于在 OCI 计算实例上运行的应用程序：

```python
llm = ChatOCIGenAI(
    model_id="meta.llama-3.3-70b-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..your-compartment-id",
    auth_type="INSTANCE_PRINCIPAL",
)
```

### 资源主体

用于 OCI Functions 和其他资源：

```python
llm = ChatOCIGenAI(
    model_id="meta.llama-3.3-70b-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..your-compartment-id",
    auth_type="RESOURCE_PRINCIPAL",
)
```

---

## OCI 生成式 AI

> Oracle Cloud Infrastructure (OCI) [生成式 AI](https://docs.oracle.com/en-us/iaas/Content/generative-ai/home.htm) 是一项完全托管的服务，提供一组最先进的、可定制的大型语言模型 (LLM)，涵盖广泛的用例，并可通过单一 API 获取。
> 使用 OCI 生成式 AI 服务，您可以访问即用型预训练模型，或基于您自己的数据在专用 AI 集群上创建和托管您自己的微调自定义模型。

### 聊天模型

#### ChatOCIGenAI

OCI 生成式 AI 服务的主要聊天模型，支持完整的 LangChain 功能。

参见 [使用示例](/oss/python/integrations/chat/oci_generative_ai)。

```python
from langchain_oci import ChatOCIGenAI

llm = ChatOCIGenAI(
    model_id="meta.llama-3.3-70b-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..xxx",
)
```

**支持的功能：**
- ✅ 工具调用（包括 Llama 4+ 的并行工具）
- ✅ 结构化输出（Pydantic、JSON 模式）
- ✅ 视觉与多模态（13+ 模型支持图像）
- ✅ 流式传输（同步和异步）
- ✅ 异步操作（ainvoke、astream、abatch）
- ✅ PDF、视频、音频处理（Gemini 模型）

#### ChatOCIOpenAI

用于 OCI 商业 OpenAI 模型的 OpenAI Responses API 兼容性。

```python
from langchain_oci import ChatOCIOpenAI

llm = ChatOCIOpenAI(
    model="openai.gpt-4.1",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..your-compartment-id",
)
```

**功能：**
- OpenAI 兼容接口
- 会话存储支持持久内存
- 访问 GPT-4、GPT-5、o1、o3 模型（在可用的情况下）

### 嵌入模型

#### OCIGenAIEmbeddings

文本和图像嵌入模型。

参见 [使用示例](/oss/python/integrations/embeddings/oci_generative_ai)。

```python
from langchain_oci import OCIGenAIEmbeddings

# 文本嵌入
embeddings = OCIGenAIEmbeddings(
    model_id="cohere.embed-english-v3.0",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..xxx",
)

# 图像嵌入（使用多模态模型）
vector = embeddings.embed_image("./architecture_diagram.png")
```

**可用模型：**
- `cohere.embed-english-v3.0` (1024 维度)
- `cohere.embed-multilingual-v3.0` (1024 维度)
- `cohere.embed-v4.0` (文本 + 图像，256-1536 维度)

### 视觉与多模态

跨 Meta Llama、Google Gemini、xAI Grok 和 Cohere 的 13+ 个视觉能力模型：

```python
from langchain.messages import HumanMessage
from langchain_oci import ChatOCIGenAI, load_image

llm = ChatOCIGenAI(
    model_id="meta.llama-3.2-90b-vision-instruct",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..xxx",
)

message = HumanMessage(content=[
    {"type": "text", "text": "识别所有微服务、数据流和外部依赖项。"},
    load_image("./architecture_diagram.png"),
])
response = llm.invoke([message])
```

**Gemini 多模态**（PDF、视频、音频）：

```python
import base64
from langchain.messages import HumanMessage
from langchain_oci import ChatOCIGenAI

llm = ChatOCIGenAI(
    model_id="google.gemini-2.5-flash",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    compartment_id="ocid1.compartment.oc1..your-compartment-id",
)

# PDF 处理 - 从合同中提取结构化数据
with open("vendor_contract.pdf", "rb") as f:
    pdf_data = base64.b64encode(f.read()).decode()

message = HumanMessage(content=[
    {"type": "text", "text": "提取：各方、生效日期、付款条款。以 JSON 返回。"},
    {"type": "document_url", "document_url": {"url": f"data:application/pdf;base64,{pdf_data}"}}
])
response = llm.invoke([message])
```

### AI 代理

使用 OCI 模型创建 LangGraph 驱动的 ReAct 代理：

```python
from langchain.tools import tool
from langchain_oci import create_oci_agent

@tool
def query_infrastructure(resource_type: str, region: str) -> dict:
    """查询 OCI 基础设施状态和健康指标。

    Args:
        resource_type: 资源类型（计算、数据库、网络）
        region: 要查询的 OCI 区域
    """
    # 示例：调用 OCI 监控 API
    return {
        "status": "healthy",
        "active_instances": 12,
        "cpu_utilization": "45%",
        "alerts": []
    }

agent = create_oci_agent(
    model_id="meta.llama-4-scout-17b-16e-instruct",
    tools=[query_infrastructure],
    compartment_id="ocid1.compartment.oc1..xxx",
    service_endpoint="https://inference.generativeai.us-chicago-1.oci.oraclecloud.com",
    system_prompt="您是基础设施监控助手。",
)

from langchain.messages import HumanMessage
result = agent.invoke({
    "messages": [HumanMessage(content="检查 us-ashburn-1 的计算资源健康状况")]
})
```

### 提供商覆盖范围

| 提供商 | 示例模型 | 关键功能 |
|----------|----------------|--------------|
| **Meta** | Llama 3.2, 3.3, 4 (Scout, Maverick) | 视觉、并行工具调用 |
| **Google** | Gemini 2.0/2.5 Flash, Flash Lite, Pro | PDF、视频、音频处理 |
| **xAI** | Grok 3, 4 (Fast, Mini) | 视觉、推理模式 |
| **Cohere** | Command R+, Command A | RAG 优化、V2 视觉 |
| **OpenAI** | GPT-4.1, GPT-5, o1, o3 | 推理（通过 ChatOCIOpenAI） |

> **注意：** 模型可用性因地区而异。请参阅 [OCI 生成式 AI 文档](https://docs.oracle.com/en-us/iaas/Content/generative-ai/pretrained-models.htm) 获取当前模型目录。

---

## OCI 数据科学模型部署

> [OCI 数据科学](https://docs.oracle.com/en-us/iaas/data-science/using/home.htm) 是一个完全托管的无服务器平台，面向数据科学团队。使用 [OCI 数据科学模型部署服务](https://docs.oracle.com/en-us/iaas/data-science/using/model-dep-about.htm) 将自定义模型部署为端点。

### ChatOCIModelDeployment

用于 OCI 数据科学模型部署的聊天模型。

参见 [使用示例](/oss/python/integrations/chat/oci_data_science)。

```python
from langchain_oci import ChatOCIModelDeployment

llm = ChatOCIModelDeployment(
    endpoint="https://modeldeployment.us-ashburn-1.oci.customer-oci.com/ocid1.datasciencemodeldeployment.oc1.iad.xxx/predict",
    model="odsc-llm",
)
```

### ChatOCIModelDeploymentVLLM

针对基于 vLLM 的部署进行了优化，支持流式传输：

```python
from langchain_oci import ChatOCIModelDeploymentVLLM

llm = ChatOCIModelDeploymentVLLM(
    endpoint="https://modeldeployment.us-ashburn-1.oci.customer-oci.com/ocid1.datasciencemodeldeployment.oc1.iad.xxx/predict",
    model="meta-llama/Llama-2-7b-chat-hf",
    streaming=True,
)
```

### ChatOCIModelDeploymentTGI

用于文本生成推理 (TGI) 部署：

```python
from langchain_oci import ChatOCIModelDeploymentTGI

llm = ChatOCIModelDeploymentTGI(
    endpoint="https://modeldeployment.us-ashburn-1.oci.customer-oci.com/ocid1.datasciencemodeldeployment.oc1.iad.xxx/predict",
)
```

---

## 示例

有关涵盖所有功能的综合指南，请参阅 [langchain-oci 示例](https://github.com/oracle/langchain-oracle/tree/main/samples)：

| 示例 | 级别 | 主题 |
|--------|-------|--------|
| [01: 入门](https://github.com/oracle/langchain-oracle/tree/main/samples/01-getting-started) | 初级 | 身份验证、基本聊天、提供商 |
| [02: 视觉与多模态](https://github.com/oracle/langchain-oracle/tree/main/samples/02-vision-and-multimodal) | 初级 | 图像分析、PDF、视频、音频 |
| [03: 构建 AI 代理](https://github.com/oracle/langchain-oracle/tree/main/samples/03-building-ai-agents) | 中级 | ReAct 代理、工具、内存 |
| [04: 工具调用精通](https://github.com/oracle/langchain-oracle/tree/main/samples/04-tool-calling-mastery) | 中级 | 并行工具、工作流 |
| [05: 结构化输出](https://github.com/oracle/langchain-oracle/tree/main/samples/05-structured-output) | 中级 | Pydantic 模式、JSON 模式 |
| [07: 生产环境异步](https://github.com/oracle/langchain-oracle/tree/main/samples/07-async-for-production) | 高级 | ainvoke、astream、FastAPI |
| [09: 提供商深入探究](https://github.com/oracle/langchain-oracle/tree/main/samples/09-provider-deep-dive) | 专业 | Meta、Gemini、Cohere、xAI 详解 |
| [10: 嵌入](https://github.com/oracle/langchain-oracle/tree/main/samples/10-embeddings) | 专业 | 文本与图像嵌入、RAG |

---

## 其他资源

- [API 参考](https://github.com/oracle/langchain-oracle/tree/main/libs/oci)
- [GitHub 仓库](https://github.com/oracle/langchain-oracle)
- [OCI 生成式 AI 文档](https://docs.oracle.com/en-us/iaas/Content/generative-ai/home.htm)
- [OCI 数据科学文档](https://docs.oracle.com/en-us/iaas/data-science/using/home.htm)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/oci.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>