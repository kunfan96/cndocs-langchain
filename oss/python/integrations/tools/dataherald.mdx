---
title: "Dataherald 集成"
description: "使用 LangChain Python 集成 Dataherald 工具。"
---

本 notebook 介绍了如何使用 Dataherald 组件。

首先，您需要创建 Dataherald 账户并获取 API KEY：

1. 访问 Dataherald 并在[此处](https://www.dataherald.com/)注册
2. 登录管理控制台后，创建一个 API KEY
3. pip install dataherald

然后，我们需要设置一些环境变量：

1. 将您的 API KEY 保存到 DATAHERALD_API_KEY 环境变量中

```python
pip install dataherald
pip install -qU langchain-community
```

```python
import os

os.environ["DATAHERALD_API_KEY"] = ""
```

```python
from langchain_community.utilities.dataherald import DataheraldAPIWrapper
```

```python
dataherald = DataheraldAPIWrapper(db_connection_id="65fb766367dd22c99ce1a12d")
```

```python
dataherald.run("How many employees are in the company?")
```

```text
'select COUNT(*) from employees'
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/tools/dataherald.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs)到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
