---
title: LangChain 概述
sidebarTitle: 概述
description: LangChain 是一个开源框架，提供预构建的 Agent 架构和任意模型或工具的集成——让你能够构建随生态系统演进而快速适应的 Agent
---

LangChain 是构建由 LLM 驱动的 Agent 和应用程序的最简单方式。只需不到 10 行代码，你就可以连接到 OpenAI、Anthropic、Google 以及[更多](/oss/python/integrations/providers/overview)服务商。LangChain 提供预构建的 Agent 架构和模型集成，帮助你快速入门并将 LLM 无缝集成到你的 Agent 和应用程序中。

如果你想快速构建 Agent 和自主应用程序，我们建议你使用 LangChain。当你有更高级的需求，需要结合确定性工作流和 Agent 工作流、大量自定义以及精确控制延迟时，请使用 [LangGraph](/oss/python/langgraph/overview)——我们的低级 Agent 编排框架和运行时。

LangChain [Agent](/oss/python/langchain/agents) 构建在 LangGraph 之上，以提供持久执行、流式传输、人机协作、持久化等功能。使用基本的 LangChain Agent 功能不需要了解 LangGraph。

## <Icon icon="wand-magic-sparkles" /> 创建一个 Agent

```python
# pip install -qU langchain "langchain[anthropic]"
from langchain.agents import create_agent

def get_weather(city: str) -> str:
    """Get weather for a given city."""
    return f"It's always sunny in {city}!"

agent = create_agent(
    model="claude-sonnet-4-5-20250929",
    tools=[get_weather],
    system_prompt="You are a helpful assistant",
)

# Run the agent
agent.invoke(
    {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
)
```




查看[安装说明](/oss/python/langchain/install)和[快速入门指南](/oss/python/langchain/quickstart)，开始使用 LangChain 构建你自己的 Agent 和应用程序。

## <Icon icon="star" size={20} /> 核心优势

<Columns cols={2}>
    <Card title="标准化模型接口" icon="arrows-rotate" href="/oss/python/langchain/models" arrow cta="了解更多">
        不同的服务商有独特的 API 用于与模型交互，包括响应格式。LangChain 标准化了你与模型的交互方式，让你可以无缝切换服务商，避免供应商锁定。
    </Card>

    <Card title="易用且高度灵活的 Agent" icon="wand-magic-sparkles" href="/oss/python/langchain/agents" arrow cta="了解更多">
        LangChain 的 Agent 抽象设计为易于上手，让你可以用不到 10 行代码构建一个简单的 Agent。但它也提供足够的灵活性，让你可以进行任何你想要的上下文工程。
    </Card>

    <Card title="构建在 LangGraph 之上" icon="circle-nodes" href="/oss/python/langgraph/overview" arrow cta="了解更多">
        LangChain 的 Agent 构建在 LangGraph 之上。这使我们能够利用 LangGraph 的持久执行、人机协作支持、持久化等功能。
    </Card>

    <Card title="使用 LangSmith 调试" icon="eye" href="/langsmith/home" arrow cta="了解更多">
        通过可视化工具深入了解复杂的 Agent 行为，追踪执行路径，捕获状态转换，并提供详细的运行时指标。
    </Card>
</Columns>

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/langchain/overview.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
<div class='fixed right-2 bg-white bottom-2'><img src='https://kunfanyang.site/site-file/wx_code.jpg' class='border' alt='微信扫码赞赏' width='200' /></div>