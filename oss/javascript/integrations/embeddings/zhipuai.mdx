---
title: "智谱AI集成"
description: "使用 LangChain JavaScript 与智谱AI嵌入模型集成。"
---

`ZhipuAIEmbeddings` 类使用智谱AI API 为给定文本生成嵌入。

## 设置

您需要注册智谱AI API 密钥，并将其设置为名为 `ZHIPUAI_API_KEY` 的环境变量。

https://open.bigmodel.cn

然后，您需要安装 [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) 包：

<Tip>
请参阅[此部分获取安装 LangChain 包的一般说明](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core jsonwebtoken
```

## 用法

```typescript
import { ZhipuAIEmbeddings } from "@langchain/community/embeddings/zhipuai";

const model = new ZhipuAIEmbeddings({});
const res = await model.embedQuery(
  "What would be a good company name a company that makes colorful socks?"
);
console.log({ res });
```

## 相关内容

- 嵌入模型[概念指南](/oss/javascript/integrations/embeddings)
- 嵌入模型[操作指南](/oss/javascript/integrations/embeddings)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/embeddings/zhipuai.mdx) 或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等工具](/use-these-docs) 以获取实时答案。
</Callout>
</div>