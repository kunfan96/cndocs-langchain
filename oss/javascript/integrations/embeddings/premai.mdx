---
title: "Prem AI 集成"
description: "使用 LangChain JavaScript 与 Prem AI 嵌入模型集成。"
---

`PremEmbeddings` 类使用 Prem AI API 为给定文本生成嵌入。

## 设置

要使用 Prem API，您需要[注册 Prem 账户并创建 API 密钥](https://studio.premai.io/sign-up)。

然后安装 [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) 包：

<Tip>
请参阅[此部分获取安装 LangChain 包的一般说明](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

## 用法

```typescript
import { PremEmbeddings } from "@langchain/community/embeddings/premai";

const embeddings = new PremEmbeddings({
  // 在 Node.js 中默认为 process.env.PREM_API_KEY
  apiKey: "YOUR-API-KEY",
  // 在 Node.js 中默认为 process.env.PREM_PROJECT_ID
  project_id: "YOUR-PROJECT_ID",
  model: "@cf/baai/bge-small-en-v1.5", // 用于生成嵌入的模型
});

const res = await embeddings.embedQuery(
  "为一家生产彩色袜子的公司起个好名字会是什么？"
);
console.log({ res });
```

## 相关内容

- 嵌入模型[概念指南](/oss/javascript/integrations/embeddings)
- 嵌入模型[操作指南](/oss/javascript/integrations/embeddings)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/embeddings/premai.mdx) 或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等工具](/use-these-docs) 以获取实时答案。
</Callout>
</div>