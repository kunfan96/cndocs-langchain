---
title: "IPEX-LLM 集成"
description: "使用 LangChain Python 与 IPEX-LLM 进行集成。"
---

> [IPEX-LLM](https://github.com/intel-analytics/ipex-llm) 是一个 PyTorch 库，用于在 Intel CPU 和 GPU（例如带有集成显卡的本地 PC，或 Arc、Flex 和 Max 等独立显卡）上以极低的延迟运行大语言模型。

- [Intel GPU 上的 IPEX-LLM](#ipex-llm-on-intel-gpu)
- [Intel CPU 上的 IPEX-LLM](#ipex-llm-on-intel-cpu)

## Intel GPU 上的 IPEX-LLM

本示例介绍如何使用 LangChain 与 `ipex-llm` 交互，以便在 Intel GPU 上进行文本生成。

> **注意**
>
> 仅建议拥有 Intel Arc A 系列 GPU（Intel Arc A300 系列或 Pro A60 除外）的 Windows 用户直接运行“Intel GPU 上的 IPEX-LLM”部分的 Jupyter Notebook。对于其他情况（例如 Linux 用户、Intel 集成显卡等），建议在终端中使用 Python 脚本运行代码以获得最佳体验。

### 安装前置条件

为了在 Intel GPU 上充分利用 IPEX-LLM，需要进行若干工具安装和环境准备的前置步骤。

如果您是 Windows 用户，请访问 [在 Windows 上使用 Intel GPU 安装 IPEX-LLM 指南](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md)，并按照 [安装前置条件](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md#install-prerequisites) 更新 GPU 驱动程序（可选）并安装 Conda。

如果您是 Linux 用户，请访问 [在 Linux 上使用 Intel GPU 安装 IPEX-LLM](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md)，并按照 [**安装前置条件**](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md#install-prerequisites) 安装 GPU 驱动程序、Intel® oneAPI Base Toolkit 2024.0 以及 Conda。

### 环境配置

完成前置条件安装后，您应该已经创建了一个包含所有前置依赖的 conda 环境。**请在此 conda 环境中启动 jupyter 服务**：

```python
pip install -qU langchain langchain-community
```

安装 IPEX-LLM 以便在 Intel GPU 上本地运行大语言模型。

```python
pip install --pre --upgrade ipex-llm[xpu] --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
```

> **注意**
>
> 您也可以将 `https://pytorch-extension.intel.com/release-whl/stable/xpu/cn/` 用作额外的索引 URL。

### 运行时配置

为了获得最佳性能，建议根据您的设备设置几个环境变量：

#### 针对使用 Intel Core Ultra 集成显卡的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
os.environ["BIGDL_LLM_XMX_DISABLED"] = "1"
```

#### 针对使用 Intel Arc A 系列显卡的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
```

> **注意**
>
> 每个模型首次在 Intel 集成显卡/Intel Arc A300 系列或 Pro A60 上运行时，可能需要几分钟进行编译。
>
> 对于其他 GPU 类型，请参阅 Windows 用户的 [Windows 运行时配置](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration) 以及 Linux 用户的 [Linux 运行时配置](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration-1)。

### 基本用法

```python
import warnings

from langchain_classic.chains import LLMChain
from langchain_community.llms import IpexLLM
from langchain_core.prompts import PromptTemplate

warnings.filterwarnings("ignore", category=UserWarning, message=".*padding_mask.*")
```

为您的模型指定提示词模板。在本示例中，我们使用 [vicuna-1.5](https://huggingface.co/lmsys/vicuna-7b-v1.5) 模型。如果您使用的是其他模型，请相应地选择合适的模板。

```python
template = "USER: {question}\nASSISTANT:"
prompt = PromptTemplate(template=template, input_variables=["question"])
```

使用 `IpexLLM.from_model_id` 通过 IpexLLM 在本地加载模型。它将直接以 Hugging Face 格式加载模型，并自动转换为低比特格式以进行推理。在初始化 IpexLLM 时，请在 `model_kwargs` 中将 `device` 设置为 `"xpu"`，以便将大语言模型加载到 Intel GPU 上。

```python
llm = IpexLLM.from_model_id(
    model_id="lmsys/vicuna-7b-v1.5",
    model_kwargs={
        "temperature": 0,
        "max_length": 64,
        "trust_remote_code": True,
        "device": "xpu",
    },
)
```

在链（Chains）中使用它

```python
llm_chain = prompt | llm

question = "What is AI?"
output = llm_chain.invoke(question)
```

### 保存/加载低比特模型

或者，您可以将低比特模型保存到磁盘一次，然后使用 `from_model_id_low_bit` 代替 `from_model_id` 来重新加载以供后续使用——甚至可以在不同机器之间使用。这种方式节省空间，因为低比特模型所需的磁盘空间远小于原始模型。此外，由于跳过了模型转换步骤，`from_model_id_low_bit` 在速度和内存使用方面也比 `from_model_id` 更高效。同样，您可以在 `model_kwargs` 中将 `device` 设置为 `"xpu"`，以便将大语言模型加载到 Intel GPU 上。

要保存低比特模型，请按如下方式使用 `save_low_bit`。

```python
saved_lowbit_model_path = "./vicuna-7b-1.5-low-bit"  # 保存低比特模型的路径
llm.model.save_low_bit(saved_lowbit_model_path)
del llm
```

按如下方式从保存的低比特模型路径加载模型。
> 请注意，低比特模型的保存路径仅包含模型本身，不包含分词器（tokenizers）。如果您希望将所有内容放在同一位置，则需要手动从原始模型的目录下载或复制分词器文件到保存低比特模型的位置。

```python
llm_lowbit = IpexLLM.from_model_id_low_bit(
    model_id=saved_lowbit_model_path,
    tokenizer_id="lmsys/vicuna-7b-v1.5",
    # tokenizer_name=saved_lowbit_model_path,  # 如果希望以此方式使用，请将分词器复制到保存路径
    model_kwargs={
        "temperature": 0,
        "max_length": 64,
        "trust_remote_code": True,
        "device": "xpu",
    },
)
```

在链（Chains）中使用加载的模型：

```python
llm_chain = prompt | llm_lowbit


question = "What is AI?"
output = llm_chain.invoke(question)
```

## Intel CPU 上的 IPEX-LLM

本示例介绍如何使用 LangChain 与 `ipex-llm` 交互，以便在 Intel CPU 上进行文本生成。

### 环境配置

```python
# 更新 LangChain

pip install -qU langchain langchain-community
```

安装 IPEX-LLM 以便在 Intel CPU 上本地运行大语言模型：

#### 针对 Windows 用户

```python
pip install --pre --upgrade ipex-llm[all]
```

#### 针对 Linux 用户

```python
pip install --pre --upgrade ipex-llm[all] --extra-index-url https://download.pytorch.org/whl/cpu
```

### 基本用法

```python
import warnings

from langchain_classic.chains import LLMChain
from langchain_community.llms import IpexLLM
from langchain_core.prompts import PromptTemplate

warnings.filterwarnings("ignore", category=UserWarning, message=".*padding_mask.*")
```

为您的模型指定提示词模板。在本示例中，我们使用 [vicuna-1.5](https://huggingface.co/lmsys/vicuna-7b-v1.5) 模型。如果您使用的是其他模型，请相应地选择合适的模板。

```python
template = "USER: {question}\nASSISTANT:"
prompt = PromptTemplate(template=template, input_variables=["question"])
```

使用 `IpexLLM.from_model_id` 通过 IpexLLM 在本地加载模型。它将直接以 Hugging Face 格式加载模型，并自动转换为低比特格式以进行推理。

```python
llm = IpexLLM.from_model_id(
    model_id="lmsys/vicuna-7b-v1.5",
    model_kwargs={"temperature": 0, "max_length": 64, "trust_remote_code": True},
)
```

在链（Chains）中使用它：

```python
llm_chain = prompt | llm

question = "What is AI?"
output = llm_chain.invoke(question)
```

### 保存/加载低比特模型

或者，您可以将低比特模型保存到磁盘一次，然后使用 `from_model_id_low_bit` 代替 `from_model_id` 来重新加载以供后续使用——甚至可以在不同机器之间使用。这种方式节省空间，因为低比特模型所需的磁盘空间远小于原始模型。此外，由于跳过了模型转换步骤，`from_model_id_low_bit` 在速度和内存使用方面也比 `from_model_id` 更高效。

要保存低比特模型，请按如下方式使用 `save_low_bit`：

```python
saved_lowbit_model_path = "./vicuna-7b-1.5-low-bit"  # 保存低比特模型的路径
llm.model.save_low_bit(saved_lowbit_model_path)
del llm
```

按如下方式从保存的低比特模型路径加载模型。

> 请注意，低比特模型的保存路径仅包含模型本身，不包含分词器（tokenizers）。如果您希望将所有内容放在同一位置，则需要手动从原始模型的目录下载或复制分词器文件到保存低比特模型的位置。

```python
llm_lowbit = IpexLLM.from_model_id_low_bit(
    model_id=saved_lowbit_model_path,
    tokenizer_id="lmsys/vicuna-7b-v1.5",
    # tokenizer_name=saved_lowbit_model_path,  # 如果希望以此方式使用，请将分词器复制到保存路径
    model_kwargs={"temperature": 0, "max_length": 64, "trust_remote_code": True},
)
```

在链（Chains）中使用加载的模型：

```python
llm_chain = prompt | llm_lowbit


question = "What is AI?"
output = llm_chain.invoke(question)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/ipex_llm.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>