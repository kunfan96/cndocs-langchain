---
title: "在 Intel GPU 上集成 IPEX-LLM 本地 BGE 嵌入"
description: "使用 LangChain Python 集成基于 Intel GPU 的 IPEX-LLM 本地 BGE 嵌入模型。"
---

> [IPEX-LLM](https://github.com/intel-analytics/ipex-llm) 是一个 PyTorch 库，用于在 Intel CPU 和 GPU（例如带有核显的本地 PC、独立 GPU 如 Arc、Flex 和 Max）上以极低延迟运行 LLM。

本示例介绍如何使用 LangChain 结合 `ipex-llm` 优化在 Intel GPU 上执行嵌入任务。这将有助于 RAG、文档问答等应用场景。

> **注意**
>
> 仅建议配备 Intel Arc A 系列 GPU（Intel Arc A300 系列或 Pro A60 除外）的 Windows 用户直接运行此 Jupyter Notebook。对于其他情况（例如 Linux 用户、Intel 核显等），建议在终端中使用 Python 脚本运行代码以获得最佳体验。

## 安装前置依赖

为了在 Intel GPU 上充分利用 IPEX-LLM，需要进行若干工具安装和环境准备的前置步骤。

如果您是 Windows 用户，请访问 [在 Windows 上使用 Intel GPU 安装 IPEX-LLM 指南](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md)，并按照 [安装前置依赖](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md#install-prerequisites) 更新 GPU 驱动程序（可选）并安装 Conda。

如果您是 Linux 用户，请访问 [在 Linux 上使用 Intel GPU 安装 IPEX-LLM](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md)，并按照 [**安装前置依赖**](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md#install-prerequisites) 安装 GPU 驱动程序、Intel® oneAPI Base Toolkit 2024.0 以及 Conda。

## 环境配置

完成前置依赖安装后，您应该已经创建了一个包含所有前置依赖的 conda 环境。**请在此 conda 环境中启动 Jupyter 服务**：

```python
pip install -qU langchain langchain-community
```

安装用于 Intel GPU 优化的 IPEX-LLM 以及 `sentence-transformers`。

```python
pip install --pre --upgrade ipex-llm[xpu] --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
pip install sentence-transformers
```

> **注意**
>
> 您也可以将 `https://pytorch-extension.intel.com/release-whl/stable/xpu/cn/` 用作 extra-index-url。

## 运行时配置

为了获得最佳性能，建议根据您的设备设置以下环境变量：

### 配备 Intel Core Ultra 核显的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
os.environ["BIGDL_LLM_XMX_DISABLED"] = "1"
```

### 配备 Intel Arc A 系列 GPU 的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
```

> **注意**
>
> 每个模型首次在 Intel 核显 / Intel Arc A300 系列或 Pro A60 上运行时，可能需要几分钟进行编译。
>
> 对于其他 GPU 类型，请参阅 [IPEX-LLM Windows 运行时配置](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration) 或 [IPEX-LLM Linux 运行时配置](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration-1)。

## 基本用法

在初始化 `IpexLLMBgeEmbeddings` 时，将 `model_kwargs` 中的 `device` 设置为 `"xpu"`，即可将嵌入模型部署到 Intel GPU 上并享受 IPEX-LLM 的优化：

```python
from langchain_community.embeddings import IpexLLMBgeEmbeddings

embedding_model = IpexLLMBgeEmbeddings(
    model_name="BAAI/bge-large-en-v1.5",
    model_kwargs={"device": "xpu"},
    encode_kwargs={"normalize_embeddings": True},
)
```

---

## API 参考

- [IpexLLMBgeEmbeddings](https://reference.langchain.com/python/langchain-community/embeddings/ipex_llm/IpexLLMBgeEmbeddings)

```python
sentence = "IPEX-LLM is a PyTorch library for running LLM on Intel CPU and GPU (e.g., local PC with iGPU, discrete GPU such as Arc, Flex and Max) with very low latency."
query = "What is IPEX-LLM?"

text_embeddings = embedding_model.embed_documents([sentence, query])
print(f"text_embeddings[0][:10]: {text_embeddings[0][:10]}")
print(f"text_embeddings[1][:10]: {text_embeddings[1][:10]}")

query_embedding = embedding_model.embed_query(query)
print(f"query_embedding[:10]: {query_embedding[:10]}")
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/ipex_llm_gpu.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>