---
title: "ApertureDB 集成"
description: "使用 LangChain Python 与 ApertureDB 向量存储进行集成。"
---

[ApertureDB](https://docs.aperturedata.io) 是一个数据库，用于存储、索引和管理多模态数据，如文本、图像、视频、边界框和嵌入，以及它们相关的元数据。

本笔记本解释了如何使用 ApertureDB 的嵌入功能。

## 安装 ApertureDB Python SDK

这将安装用于编写 ApertureDB 客户端代码的 [Python SDK](https://docs.aperturedata.io/category/aperturedb-python-sdk)。

```python
pip install -qU aperturedb
```

## 运行 ApertureDB 实例

要继续，您应该有一个 [ApertureDB 实例正在运行](https://docs.aperturedata.io/HowToGuides/start/Setup)，并配置您的环境以使用它。
有多种方法可以做到这一点，例如：

```bash
docker run --publish 55555:55555 aperturedata/aperturedb-standalone
adb config create local --active --no-interactive
```

## 下载一些网页文档

我们将在这里对一个网页进行小型爬取。

```python
# 用于从网页加载文档
from langchain_community.document_loaders import WebBaseLoader

loader = WebBaseLoader("https://docs.aperturedata.io")
docs = loader.load()
```

```text
USER_AGENT 环境变量未设置，考虑设置它以标识您的请求。
```

## 选择嵌入模型

我们想使用 OllamaEmbeddings，因此必须导入必要的模块。

Ollama 可以设置为 Docker 容器，如 [文档](https://hub.docker.com/r/ollama/ollama) 中所述，例如：

```bash
# 运行服务器
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
# 告诉服务器加载特定模型
docker exec ollama ollama run llama2
```

```python
from langchain_community.embeddings import OllamaEmbeddings

embeddings = OllamaEmbeddings()
```

## 将文档拆分为段落

我们希望将单个文档转换为多个段落。

```python
from langchain_text_splitters import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter()
documents = text_splitter.split_documents(docs)
```

## 从文档和嵌入创建向量存储

此代码在 ApertureDB 实例中创建一个向量存储。
在实例内部，此向量存储表示为一个 "[描述符集](https://docs.aperturedata.io/category/descriptorset-commands)"。
默认情况下，描述符集命名为 `langchain`。以下代码将为每个文档生成嵌入，并将它们作为描述符存储在 ApertureDB 中。由于嵌入正在生成，这将需要几秒钟。

```python
from langchain_community.vectorstores import ApertureDB

vector_db = ApertureDB.from_documents(documents, embeddings)
```

## 选择大型语言模型

同样，我们使用为本地处理设置的 Ollama 服务器。

```python
from langchain_community.llms import Ollama

llm = Ollama(model="llama2")
```

## 构建 RAG 链

现在我们拥有了创建 RAG（检索增强生成）链所需的所有组件。此链执行以下操作：

1. 为用户查询生成嵌入描述符
2. 使用向量存储查找与用户查询相似的文本段落
3. 使用提示模板将用户查询和上下文文档传递给 LLM
4. 返回 LLM 的答案

```python
# 创建提示
from langchain_core.prompts import ChatPromptTemplate

prompt = ChatPromptTemplate.from_template("""仅基于提供的上下文回答以下问题：

<context>
{context}
</context>

问题：{input}""")


# 创建一个将文档传递给 LLM 的链
from langchain_classic.chains.combine_documents import create_stuff_documents_chain

document_chain = create_stuff_documents_chain(llm, prompt)


# 将向量存储视为文档检索器
retriever = vector_db.as_retriever()


# 创建一个将检索器连接到 LLM 的 RAG 链
from langchain_classic.chains import create_retrieval_chain

retrieval_chain = create_retrieval_chain(retriever, document_chain)
```

```text
根据提供的上下文，ApertureDB 可以存储图像。事实上，它专门设计用于管理多模态数据，如图像、视频、文档、嵌入和相关元数据（包括注释）。因此，ApertureDB 具有存储和管理图像的能力。
```

## 运行 RAG 链

最后，我们将一个问题传递给链并获取答案。由于 LLM 从查询和上下文文档生成答案，这将需要几秒钟才能运行。

```python
user_query = "How can ApertureDB store images?"
response = retrieval_chain.invoke({"input": user_query})
print(response["answer"])
```

```text
根据提供的上下文，ApertureDB 可以通过以下几种方式存储图像：

1. 多模态数据管理：ApertureDB 提供了一个统一的接口来管理多模态数据，如图像、视频、文档、嵌入和相关元数据（包括注释）。这意味着图像可以与其他类型的数据一起存储在单个数据库实例中。
2. 图像存储：ApertureDB 通过其与公共云提供商或本地安装的集成提供图像存储功能。这允许客户托管自己的 ApertureDB 实例，并在其首选的云提供商或本地基础设施上存储图像。
3. 向量数据库：ApertureDB 还提供了一个向量数据库，能够基于语义含义对图像进行高效的相似性搜索和分类。这对于图像搜索和分类很重要的应用（例如计算机视觉或机器学习工作流）非常有用。

总体而言，ApertureDB 为图像提供了灵活且可扩展的存储选项，允许客户选择最适合其需求的部署模型。
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/vectorstores/aperturedb.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>
