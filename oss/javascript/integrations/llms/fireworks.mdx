---
title: "Fireworks 集成"
description: "使用 LangChain JavaScript 与 Fireworks LLM 集成。"
---

<Warning>
**您当前正在查看的是关于将 Fireworks 模型用作文本补全模型的文档。Fireworks 上提供的许多流行模型是[聊天补全模型](/oss/javascript/langchain/models)。**

您可能想查看[此页面](/oss/javascript/integrations/chat/fireworks/)。
</Warning>

[Fireworks AI](https://fireworks.ai/) 是一个用于运行和定制模型的 AI 推理平台。有关 Fireworks 提供的所有模型列表，请参阅 [Fireworks 文档](https://fireworks.ai/models)。

本文将帮助您开始使用 LangChain 的 Fireworks 补全模型（大语言模型）。有关 `Fireworks` 功能和配置选项的详细文档，请参阅 [API 参考](https://reference.langchain.com/javascript/langchain-community/llms/fireworks/Fireworks)。

## 概述

### 集成详情

| 类 | 包 | 本地 | 可序列化 | [Python 支持](https://python.langchain.com/docs/integrations/llms/fireworks) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [`Fireworks`](https://reference.langchain.com/javascript/langchain-community/llms/fireworks/Fireworks) | [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) | ❌ | ✅ | ✅ | ![NPM - Downloads](https://img.shields.io/npm/dm/@langchain/community?style=flat-square&label=%20&) | ![NPM - Version](https://img.shields.io/npm/v/@langchain/community?style=flat-square&label=%20&) |

## 设置

要访问 Fireworks 模型，您需要创建一个 Fireworks 账户，获取 API 密钥，并安装 `@langchain/community` 集成包。

### 凭证

前往 [fireworks.ai](https://fireworks.ai/) 注册 Fireworks 并生成 API 密钥。完成此操作后，设置 `FIREWORKS_API_KEY` 环境变量：

```bash
export FIREWORKS_API_KEY="your-api-key"
```

如果您想获得模型调用的自动跟踪，也可以取消注释以下内容来设置您的 [LangSmith](/langsmith/home) API 密钥：

```bash
# export LANGSMITH_TRACING="true"
# export LANGSMITH_API_KEY="your-api-key"
```

### 安装

LangChain Fireworks 集成位于 `@langchain/community` 包中：

<CodeGroup>
```bash npm
npm install @langchain/community @langchain/core
```
```bash yarn
yarn add @langchain/community @langchain/core
```
```bash pnpm
pnpm add @langchain/community @langchain/core
```
</CodeGroup>

## 实例化

现在我们可以实例化模型对象并生成聊天补全：

```typescript
import { Fireworks } from "@langchain/community/llms/fireworks"

const llm = new Fireworks({
  model: "accounts/fireworks/models/llama-v3-70b-instruct",
  temperature: 0,
  maxTokens: undefined,
  timeout: undefined,
  maxRetries: 2,
  // 其他参数...
})
```

## 调用

```typescript
const inputText = "Fireworks is an AI company that "

const completion = await llm.invoke(inputText)
completion
```

```text
 帮助企业使用 AI 驱动的聊天机器人实现客户支持自动化。我们相信 AI 可以帮助企业以更低的成本提供更好的客户支持。我们的聊天机器人设计为高度可定制，并且可以与 Facebook Messenger、Slack 等各种平台集成。

我们正在寻找一位有才华且积极主动的**机器学习工程师**加入我们的团队。作为 Fireworks 的机器学习工程师，您将负责开发和改进驱动我们聊天机器人的 AI 模型。您将与我们的数据科学家、软件工程师和产品经理紧密合作，设计、开发和部署能够理解和回应客户咨询的 AI 模型。

**职责：**

* 开发和改进能够理解和回应客户咨询的 AI 模型
* 与数据科学家合作设计和开发新的 AI 模型
* 与软件工程师合作将 AI 模型集成到我们的聊天机器人平台
* 与产品经理合作了解客户需求并开发满足这些需求的 AI 模型
* 开发和维护数据管道以支持 AI 模型的开发和部署
* 开发和维护用于监控和评估 AI 模型性能的工具
* 了解 AI 和机器学习的最新发展，并将这些知识应用于改进我们的 AI 模型

**要求：**

* 学士学位
```

---

## API 参考

有关所有 Fireworks 功能和配置的详细文档，请访问 [API 参考](https://reference.langchain.com/javascript/langchain-community/llms/fireworks/Fireworks)。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/llms/fireworks.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>