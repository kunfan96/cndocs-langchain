---
title: "Nomic 集成"
description: "使用 LangChain Python 与 Nomic 进行集成。"
---

>[Nomic](https://www.nomic.ai/) 构建工具，使每个人都能在消费级计算机上与大规模 AI 数据集交互并运行 AI 模型。
>
>`Nomic` 目前提供两款产品：
>
>- `Atlas`: 可视化数据引擎
>- `GPT4All`: 开源边缘语言模型生态系统

Nomic 集成存在于两个合作包中：[langchain-nomic](https://pypi.org/project/langchain-nomic/)
以及 [langchain-community](https://pypi.org/project/langchain-community/)。

## 安装

您可以使用以下命令安装它们：

<CodeGroup>
```bash pip
pip install -U langchain-nomic
pip install -U langchain-community
```

```bash uv
uv add langchain-nomic
uv add langchain-community
```
</CodeGroup>

## LLMs

### GPT4All

请参阅[使用示例](/oss/python/integrations/llms/gpt4all)。

```python
from langchain_community.llms import GPT4All
```

## 嵌入模型

### NomicEmbeddings

请参阅[使用示例](/oss/python/integrations/embeddings/nomic)。

```python
from langchain_nomic import NomicEmbeddings
```

### GPT4All

请参阅[使用示例](/oss/python/integrations/embeddings/gpt4all)。

```python
from langchain_community.embeddings import GPT4AllEmbeddings
```

## 向量存储

### Atlas

请参阅[使用示例和安装说明](/oss/python/integrations/vectorstores/atlas)。

```python
from langchain_community.vectorstores import AtlasDB
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/nomic.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>