---
title: "DeepInfra 集成"
description: "使用 LangChain Python 与 DeepInfra 的聊天模型进行集成。"
sidebarTitle: "DeepInfra 集成"
---

[DeepInfra](https://deepinfra.com/?utm_source=langchain) 是一个无服务器的推理即服务，提供多种 [LLM](https://deepinfra.com/models?utm_source=langchain) 和 [嵌入式模型](https://deepinfra.com/models?type=embeddings&utm_source=langchain) 的访问权限。本笔记本将介绍如何使用 LangChain 与 DeepInfra 集成以用于聊天模型。

## 设置环境 API 密钥

请从 DeepInfra 获取您的 API 密钥。您需要 [登录](https://deepinfra.com/login?from=%2Fdash)，并获取一个新的令牌。

您会获得一个小时的无服务器 GPU 计算免费试用，以便测试不同的模型。（参见 [deepctl 文档](https://github.com/deepinfra/deepctl#deepctl)）
您可以使用 `deepctl auth token` 命令打印您的令牌。

```python
# 获取新令牌: https://deepinfra.com/login?from=%2Fdash

import os
from getpass import getpass

from langchain_community.chat_models import ChatDeepInfra
from langchain.messages import HumanMessage

DEEPINFRA_API_TOKEN = getpass()

# 或者将 deepinfra_api_token 参数传递给 ChatDeepInfra 构造函数
os.environ["DEEPINFRA_API_TOKEN"] = DEEPINFRA_API_TOKEN

chat = ChatDeepInfra(model="meta-llama/Llama-2-7b-chat-hf")

messages = [
    HumanMessage(
        content="将这句话从英语翻译成法语。我热爱编程。"
    )
]
chat.invoke(messages)
```

## `ChatDeepInfra` 还支持异步和流式功能

```python
from langchain_core.callbacks import StreamingStdOutCallbackHandler
```

```python
await chat.agenerate([messages])
```

```python
chat = ChatDeepInfra(
    streaming=True,
    verbose=True,
    callbacks=[StreamingStdOutCallbackHandler()],
)
chat.invoke(messages)
```

# 工具调用

目前，DeepInfra 仅支持 `invoke` 和 `async invoke` 工具调用。

要查看支持工具调用的完整模型列表，请参阅我们的 [工具调用文档](https://deepinfra.com/docs/advanced/function_calling)。

```python
import asyncio

from dotenv import find_dotenv, load_dotenv
from langchain_community.chat_models import ChatDeepInfra
from langchain.messages import HumanMessage
from langchain.tools import tool
from pydantic import BaseModel

model_name = "meta-llama/Meta-Llama-3-70B-Instruct"

_ = load_dotenv(find_dotenv())


# LangChain 工具
@tool
def foo(something):
    """
    当调用 foo 时
    """
    pass


# Pydantic 类
class Bar(BaseModel):
    """
    当调用 Bar 时
    """

    pass


llm = ChatDeepInfra(model=model_name)
tools = [foo, Bar]
llm_with_tools = llm.bind_tools(tools)
messages = [
    HumanMessage("请使用 foo 和 bar。"),
]

response = llm_with_tools.invoke(messages)
print(response.tool_calls)
# [{'name': 'foo', 'args': {'something': None}, 'id': 'call_Mi4N4wAtW89OlbizFE1aDxDj'}, {'name': 'Bar', 'args': {}, 'id': 'call_daiE0mW454j2O1KVbmET4s2r'}]


async def call_ainvoke():
    result = await llm_with_tools.ainvoke(messages)
    print(result.tool_calls)


# 异步调用
asyncio.run(call_ainvoke())
# [{'name': 'foo', 'args': {'something': None}, 'id': 'call_ZH7FetmgSot4LHcMU6CEb8tI'}, {'name': 'Bar', 'args': {}, 'id': 'call_2MQhDifAJVoijZEvH8PeFSVB'}]
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/chat/deepinfra.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    通过 MCP 连接这些文档，与 Claude、VSCode 等进行实时问答。
</Callout>
</div>
