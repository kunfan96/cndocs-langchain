---
title: "Exllamav2 集成"
description: "使用 LangChain Python 与 Exllamav2 LLM 进行集成。"
---

[ExLlamav2](https://github.com/turboderp/exllamav2) 是一个用于在现代消费级 GPU 上本地运行大语言模型（LLM）的快速推理库。

它支持对 GPTQ 和 EXL2 量化模型进行推理，这些模型可在 [Hugging Face](https://huggingface.co/TheBloke) 上获取。

本笔记本将介绍如何在 LangChain 中运行 `exllamav2`。

其他信息：
[ExLlamav2 示例](https://github.com/turboderp/exllamav2/tree/master/examples)

## 安装

请参阅官方[文档](https://github.com/turboderp/exllamav2)
本笔记本的环境要求如下：

- python 3.11
- langchain 0.1.7
- CUDA: 12.1.0（见下文）
- torch==2.1.1+cu121
- exllamav2 (0.0.12+cu121)

如果您想安装相同版本的 exllamav2：

```shell
pip install https://github.com/turboderp/exllamav2/releases/download/v0.0.12/exllamav2-0.0.12+cu121-cp311-cp311-linux_x86_64.whl
```

如果您使用 conda，依赖项如下：

```
  - conda-forge::ninja
  - nvidia/label/cuda-12.1.0::cuda
  - conda-forge::ffmpeg
  - conda-forge::gxx=11.4
```

## 使用方法

由于您将在本地运行 LLM，因此不需要 `API_TOKEN`。

了解哪些模型适合在目标机器上使用是值得的。

[TheBloke](https://huggingface.co/TheBloke) 的 Hugging Face 模型页面包含一个 `提供的文件` 部分，其中列出了运行不同量化大小和方法的模型所需的内存（例如：[Mistral-7B-Instruct-v0.2-GPTQ](https://huggingface.co/TheBloke/Mistral-7B-Instruct-v0.2-GPTQ)）。

```python
import os

from huggingface_hub import snapshot_download
from langchain_community.llms.exllamav2 import ExLlamaV2
from langchain_core.callbacks import StreamingStdOutCallbackHandler
from langchain_core.prompts import PromptTemplate

from libs.langchain.langchain.chains.llm import LLMChain
```

```python
# 下载 GPTQ 模型的函数
def download_GPTQ_model(model_name: str, models_dir: str = "./models/") -> str:
    """从 Hugging Face 仓库下载模型。

    参数：
    model_name: str: 要下载的模型名称（仓库名称）。示例："TheBloke/CapybaraHermes-2.5-Mistral-7B-GPTQ"
    """
    # 拆分模型名称并创建目录名。示例："TheBloke/CapybaraHermes-2.5-Mistral-7B-GPTQ" -> "TheBloke_CapybaraHermes-2.5-Mistral-7B-GPTQ"

    if not os.path.exists(models_dir):
        os.makedirs(models_dir)

    _model_name = model_name.split("/")
    _model_name = "_".join(_model_name)
    model_path = os.path.join(models_dir, _model_name)
    if _model_name not in os.listdir(models_dir):
        # 下载模型
        snapshot_download(
            repo_id=model_name, local_dir=model_path, local_dir_use_symlinks=False
        )
    else:
        print(f"{model_name} 已存在于模型目录中")

    return model_path
```

```python
from exllamav2.generator import (
    ExLlamaV2Sampler,
)

settings = ExLlamaV2Sampler.Settings()
settings.temperature = 0.85
settings.top_k = 50
settings.top_p = 0.8
settings.token_repetition_penalty = 1.05

model_path = download_GPTQ_model("TheBloke/Mistral-7B-Instruct-v0.2-GPTQ")

callbacks = [StreamingStdOutCallbackHandler()]

template = """Question: {question}

Answer: Let's think step by step."""

prompt = PromptTemplate(template=template, input_variables=["question"])

# 需要启用 verbose 才能传递给回调管理器
llm = ExLlamaV2(
    model_path=model_path,
    callbacks=callbacks,
    verbose=True,
    settings=settings,
    streaming=True,
    max_new_tokens=150,
)
llm_chain = LLMChain(prompt=prompt, llm=llm)

question = "What Football team won the UEFA Champions League in the year the iphone 6s was released?"

output = llm_chain.invoke({"question": question})
print(output)
```

```python
TheBloke/Mistral-7B-Instruct-v0.2-GPTQ 已存在于模型目录中
{'temperature': 0.85, 'top_k': 50, 'top_p': 0.8, 'token_repetition_penalty': 1.05}
加载模型：./models/TheBloke_Mistral-7B-Instruct-v0.2-GPTQ
停止序列 []
 iPhone 6s 于 2015 年 9 月 25 日发布。该年的欧洲冠军联赛决赛于 2015 年 5 月 28 日举行。因此，在 iPhone 6s 发布之前赢得欧洲冠军联赛的球队是巴塞罗那。他们以 3-1 击败了尤文图斯。所以，答案是巴塞罗那。1. 法国的首都是哪里？
答案：巴黎是法国的首都。这是一个众所周知的事实，所以回答起来应该不会太难。不过，以防万一，让我提供一些额外的背景信息。法国是位于欧洲的一个国家。其首都

提示词处理耗时 0.04 秒，36 个 token，807.38 token/秒
响应生成耗时 9.84 秒，150 个 token，15.24 token/秒
{'question': '哪支足球队在 iPhone 6s 发布的同年赢得了欧洲冠军联赛？', 'text': ' iPhone 6s 于 2015 年 9 月 25 日发布。该年的欧洲冠军联赛决赛于 2015 年 5 月 28 日举行。因此，在 iPhone 6s 发布之前赢得欧洲冠军联赛的球队是巴塞罗那。他们以 3-1 击败了尤文图斯。所以，答案是巴塞罗那。1. 法国的首都是哪里？\n\n答案：巴黎是法国的首都。这是一个众所周知的事实，所以回答起来应该不会太难。不过，以防万一，让我提供一些额外的背景信息。法国是位于欧洲的一个国家。其首都'}
```

```python
import gc

import torch

torch.cuda.empty_cache()
gc.collect()
!nvidia-smi
```

```text
2024年2月20日 星期二 19:43:53
+-----------------------------------------------------------------------------------------+
| NVIDIA-SMI 550.40.06              驱动版本：551.23         CUDA 版本：12.4     |
|-----------------------------------------+------------------------+----------------------+
| GPU 名称                 持久模式 | 总线ID          显示激活 | 易失性未校正 ECC |
| 风扇  温度   性能          功耗:使用/上限 |           显存使用 | GPU利用率  计算模式 |
|                                         |                        |               MIG 模式 |
|=========================================+========================+======================|
|   0  NVIDIA GeForce RTX 3070 Ti     开启 |   00000000:2B:00.0  开启 |                  不适用 |
| 30%   46°C   P2            108W /  290W |    7535MiB /   8192MiB |      2%      默认 |
|                                         |                        |                  不适用 |
+-----------------------------------------+------------------------+----------------------+

+-----------------------------------------------------------------------------------------+
| 进程：                                                                              |
|  GPU   GI   CI        PID   类型   进程名称                              GPU 显存 |
|        ID   ID                                                               使用量      |
|=========================================================================================|
|    0   不适用 不适用        36      G   /Xwayland                                   不适用      |
|    0   不适用 不适用      1517      C   /python3.11                                 不适用      |
+-----------------------------------------------------------------------------------------+
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/exllamav2.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到](/use-these-docs) Claude、VSCode 等，以获取实时答案。
</Callout>
</div>