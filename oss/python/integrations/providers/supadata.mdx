# Supadata

本页介绍如何在 LangChain 中使用 [Supadata](https://supadata.ai) 生态系统。它允许您从 YouTube、TikTok 和其他平台加载数据。

## 安装与设置

要使用 Supadata，您需要安装独立的集成包。

```bash
pip install supadata-langchain
```

## 文档加载器

Supadata 集成允许您从各种视频平台加载内容作为 LangChain 文档。它支持获取字幕和元数据。

```python
import os
from supadata_langchain import SupadataLoader

os.environ["SUPADATA_API_KEY"] = "YOUR_API_KEY"

loader = SupadataLoader()

# 获取字幕
docs = loader.load(
    {
        "url": "[https://www.youtube.com/watch?v=dQw4w9WgXcQ](https://www.youtube.com/watch?v=dQw4w9WgXcQ)",
        "operation": "transcript",
        "lang": "en",
        "text": True,
        "mode": "auto",
    }
)

print(docs[0].page_content)
print(docs[0].metadata)
```

### 元数据

您也可以获取视频的结构化元数据。

```python
# 获取元数据
docs = loader.load(
    {
        "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
        "operation": "metadata",
    }
)

print(docs[0].page_content)
```

## 资源

* **PyPI 包：** [`supadata-langchain`](https://pypi.org/project/supadata-langchain/)
* **GitHub 仓库：** [`supadata-ai/langchain-py`](https://github.com/supadata-ai/langchain-py)

有关详细的使用示例和 API 文档，请参阅 [官方 GitHub 仓库](https://github.com/supadata-ai/langchain-py)。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/supadata.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等工具](/use-these-docs)，以获取实时答案。
</Callout>
</div>