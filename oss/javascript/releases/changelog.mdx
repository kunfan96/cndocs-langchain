---
title: 更新日志（Changelog）
sidebarTitle: 更新日志
description: JavaScript/TypeScript 包的更新与改进记录
rss: true
---

<Callout icon="rss" color="#504B5F" iconType="regular">
**订阅**：我们的更新日志包含 [RSS 订阅](https://docs.langchain.com/oss/javascript/releases/changelog/rss.xml)，可与 Slack、邮件、Discord 机器人等集成。
</Callout>

<Update label="Jan 14, 2026" tags={["langgraph"]} rss={{ title: "Jan 14, 2026 - langgraph" }}>
    ## v1.1.0

    ### `@langchain/langgraph`

    引入 **StateSchema** —— 一种更清晰、与库无关的方式来定义图状态，适用于任何兼容 Standard Schema 规范的验证库。

    ### 支持标准 JSON Schema

    LangGraph 现在支持 [Standard JSON Schema](https://standardschema.dev/json-schema)，该规范被 Zod 4、Valibot、ArkType 等实现。你可以使用你偏好的验证库而不被绑定：

    ```typescript
    import { z } from "zod"; // 或 valibot、arktype 等
    import { StateSchema, ReducedValue, MessagesValue } from "@langchain/langgraph";

    const AgentState = new StateSchema({
      messages: MessagesValue,
      currentStep: z.string(),
      count: z.number().default(0),
      history: new ReducedValue(
        z.array(z.string()).default(() => []),
        {
          inputSchema: z.string(),
          reducer: (current, next) => [...current, next],
        }
      ),
    });
    ```

    ### 新的状态值原语

    * **ReducedValue**：定义具有自定义 reducer 的字段以进行累积。
    * **UntrackedValue**：定义在执行期间存在但不会被检查点持久化的瞬态状态（用于数据库连接、缓存或仅运行时配置）。
    * **MessagesValue**：用于聊天消息的预构建 ReducedValue，含标准消息 reducer。

    ### 类型辅助导出

    新增导出类型工具用于在图构建器之外为函数添加类型：例如 `GraphNode`、`ConditionalEdgeRouter` 等（示例见原文）。

</Update>

<Update label="Dec 12, 2025" tags={["langchain","@langchain/openai","@langchain/anthropic","@langchain/ollama","@langchain/community","@langchain/xai","@langchain/tavily","@langchain/mongodb","@langchain/mcp-adapters","@langchain/google-common","@langchain/core"]} rss={{ title: "Dec 12, 2025 - langchain" }}>
    ## v1.2.0
    ### `langchain`
    * [结构化输出](/oss/javascript/langchain/structured-output)：为 `providerStrategy` 添加手动设置 `strict` 模式的能力。

    ### `@langchain/openai`
    * **新的内置提供者工具**：支持文件搜索、网页搜索、代码解释器、图像生成、计算机操作、Shell 和 MCP 连接器等工具。
    * **内容审核**：为 `ChatOpenAI` 添加 `moderateContent` 选项以检测并处理不安全内容。

    ## v1.3.0
    ### `@langchain/anthropic`
    * **新内置工具**：支持文本编辑器、网页抓取、计算机操作、工具搜索及 MCP 工具集。
    * 暴露 `ChatAnthropicInput` 类型以提高类型安全。

    ## 其他包更新（节选）
    * `@langchain/ollama`：原生结构化输出支持、可自定义 `baseUrl`。
    * `@langchain/community`、`@langchain/xai`、`@langchain/mongodb` 等包的若干改进和修复。

</Update>

<Update label="Nov 25, 2025" tags={["langchain"]} rss={{ title: "Nov 25, 2025 - langchain" }}>
    ## v1.1.0

    * [模型配置文件]( /oss/javascript/langchain/models#model-profiles )：聊天模型现在通过 `.profile` 暴露支持的特性和能力。
    * [模型重试中间件](/oss/javascript/langchain/middleware/built-in#model-retry)：用于自动重试失败的模型调用。
    * [内容审核中间件](/oss/javascript/langchain/middleware/built-in#content-moderation)：用于检测并处理不安全内容。
    * 更多改进与修复（详见原文）。
</Update>

<Update label="Oct 20, 2025" tags={["langchain","langgraph"]} rss={{ title: "Oct 20, 2025 - langchain" }}>
    ## v1.0.0

    ### `langchain`
    * [发行说明](/oss/javascript/releases/langchain-v1)
    * [迁移指南](/oss/javascript/migrate/langchain-v1)

    ### `langgraph`
    * [发行说明](/oss/javascript/releases/langgraph-v1)
    * [迁移指南](/oss/javascript/migrate/langgraph-v1)

    <Callout icon="speakerphone" color="#504B5F" iconType="regular">
    如果遇到问题或有反馈，请在 GitHub 提交 issue。要查看 v0.x 文档，请访问存档内容。
    </Callout>
</Update>

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/releases/changelog.mdx) 或 [提交 issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接到 MCP 以实现实时答案](/use-these-docs)。
</Callout>
</div>