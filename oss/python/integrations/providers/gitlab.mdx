---
title: "GitLab 集成"
description: "使用 LangChain Python 与 GitLab 集成。"
---

>[GitLab Inc.](https://about.gitlab.com/) 是一家开放核心公司，
> 运营着 `GitLab`，一个可以开发、
> 保护和运营软件的 DevOps 软件包。`GitLab` 包含基于 Git 的分布式版本
> 控制，具有访问控制、错误跟踪、
> 软件功能请求、任务管理以及每个项目的 wiki 和代码片段等功能。

## 工具/工具包

### GitLabToolkit

`GitLab` 工具包包含让 LLM 代理与 GitLab 仓库交互的工具。

该工具包是 `python-gitlab` 库的封装器。

查看[使用示例](/oss/python/integrations/tools/gitlab)。

```python
from langchain_community.agent_toolkits.gitlab.toolkit import GitLabToolkit
```

### GitLabAction

用于与 GitLab API 交互的工具。

```python
from langchain_community.tools.gitlab.tool import GitLabAction
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/gitlab.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
