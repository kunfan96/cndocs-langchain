---
title: "Arthur 集成"
description: "使用 LangChain Python 与 Arthur 进行集成。"
---

>[Arthur](https://arthur.ai) 是一个模型监控与可观测性平台。

以下指南展示了如何使用 Arthur 回调处理器运行已注册的聊天大语言模型（LLM），以自动将模型推理日志记录到 Arthur。

如果您尚未将模型接入 Arthur，请访问我们的[生成式文本模型接入指南](https://docs.arthur.ai/user-guide/walkthroughs/model-onboarding/generative_text_onboarding.html)。有关如何使用 `Arthur SDK` 的更多信息，请访问我们的[文档](https://docs.arthur.ai/)。

## 安装与配置

在此处放置 Arthur 凭据

```python
arthur_url = "https://app.arthur.ai"
arthur_login = "your-arthur-login-username-here"
arthur_model_id = "your-arthur-model-id-here"
```

## 回调处理器

```python
from langchain_community.callbacks import ArthurCallbackHandler
from langchain_core.callbacks import StreamingStdOutCallbackHandler
from langchain.messages import HumanMessage
from langchain_openai import ChatOpenAI
```

创建带有 Arthur 回调处理器的 LangChain LLM

```python
def make_langchain_chat_llm():
    return ChatOpenAI(
        streaming=True,
        temperature=0.1,
        callbacks=[
            StreamingStdOutCallbackHandler(),
            ArthurCallbackHandler.from_credentials(
                arthur_model_id, arthur_url=arthur_url, arthur_login=arthur_login
            ),
        ],
    )
```

```python
chatgpt = make_langchain_chat_llm()
```

```text
请输入管理员密码：········
```

使用此 `run` 函数运行聊天 LLM 会将聊天记录保存在一个持续更新的列表中，以便对话能够引用之前的消息，并将每条响应记录到 Arthur 平台。您可以在您的[模型仪表板页面](https://app.arthur.ai/)查看该模型的推理历史记录。

输入 `q` 退出运行循环

```python
def run(llm):
    history = []
    while True:
        user_input = input("\n>>> input >>>\n>>>: ")
        if user_input == "q":
            break
        history.append(HumanMessage(content=user_input))
        history.append(llm(history))
```

```python
run(chatgpt)
```

```text
>>> 输入 >>>
>>>: 什么是回调处理器？
回调处理器（也称为回调函数或回调方法）是一段代码，用于在特定事件或条件发生时执行。它通常用于支持事件驱动或异步编程范式的编程语言中。

回调处理器的目的是为开发人员提供一种定义自定义行为的方式，以便在特定事件发生时执行该行为。程序无需等待结果或阻塞执行，而是注册一个回调函数并继续执行其他任务。当事件触发时，回调函数将被调用，使程序能够做出相应响应。

回调处理器常用于各种场景，例如处理用户输入、响应网络请求、处理异步操作以及实现事件驱动架构。它们提供了一种灵活且模块化的方式来处理事件，并解耦系统的不同组件。
>>> 输入 >>>
>>>: 我需要做什么才能充分发挥其全部优势？
为了充分利用回调处理器的优势，您应考虑以下几点：

1. 理解事件或条件：确定您希望使用回调处理器响应的具体事件或条件。这可能是用户输入、网络请求或其他任何异步操作。

2. 定义回调函数：创建一个将在事件或条件发生时执行的函数。该函数应包含您希望在事件发生时采取的预期行为或操作。

3. 注册回调函数：根据您使用的编程语言或框架，您可能需要将回调函数注册或附加到相应的事件或条件上。这确保了当事件发生时回调函数会被调用。

4. 处理回调：在回调函数内部实现必要的逻辑来处理事件或条件。这可能涉及更新用户界面、处理数据、发起进一步请求或触发其他操作。

5. 考虑错误处理：处理回调函数内可能出现的任何潜在错误或异常非常重要。这确保您的程序能够优雅地处理意外情况，并防止崩溃或不期望的行为。

6. 保持代码可读性与模块化：随着代码库的增长，保持回调处理器的组织性和可维护性至关重要。考虑使用设计模式或架构原则以模块化和可扩展的方式构建代码。

遵循这些步骤，您可以利用回调处理器的优势，例如异步和事件驱动编程、提高响应速度以及模块化代码设计。
>>> 输入 >>>
>>>: q
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/arthur_tracking.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>