---
title: "Zep 开源集成"
description: "使用 LangChain JavaScript 与 Zep 开源检索器集成。"
---

> [Zep](https://www.getzep.com) 是一个用于 AI 助手应用的长期记忆服务。
> 借助 Zep，您可以为 AI 助手提供回忆过去对话的能力，无论对话多么久远，
> 同时还能减少幻觉、延迟和成本。

> 对 Zep Cloud 感兴趣？请参阅 [Zep Cloud 安装指南](https://help.getzep.com/sdks)

此示例展示了如何在检索链中使用 Zep 检索器，从 Zep 开源记忆存储中检索文档。

## 安装

请按照 [Zep 开源快速入门指南](https://help.getzep.com/quickstart) 安装并开始使用 Zep。

## 设置

<Tip>
请参阅 [此部分了解安装 LangChain 包的一般说明](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm i @getzep/zep-js @langchain/community @langchain/core
```

## 用法

```typescript
import { ZepRetriever } from "@langchain/community/retrievers/zep";
import { ZepMemory } from "@langchain/community/memory/zep";
import { Memory as MemoryModel, Message } from "@getzep/zep-js";
import { randomUUID } from "crypto";

function sleep(ms: number) {
  // eslint-disable-next-line no-promise-executor-return
  return new Promise((resolve) => setTimeout(resolve, ms));
}

export const run = async () => {
  const zepConfig = {
    url: process.env.ZEP_URL || "http://localhost:8000",
    sessionId: `session_${randomUUID()}`,
  };

  console.log(`Zep Config: ${JSON.stringify(zepConfig)}`);

  const memory = new ZepMemory({
    baseURL: zepConfig.url,
    sessionId: zepConfig.sessionId,
  });

  // 生成关于法国旅行的聊天消息
  const chatMessages = [
    {
      role: "AI",
      message: "Bonjour! 今天我能如何协助您的旅行计划？",
    },
    { role: "User", message: "我正在计划去法国旅行。" },
    {
      role: "AI",
      message: "听起来很令人兴奋！您计划访问哪些城市？",
    },
    { role: "User", message: "我正在考虑访问巴黎和尼斯。" },
    {
      role: "AI",
      message: "很好的选择！您对任何特定活动感兴趣吗？",
    },
    { role: "User", message: "我想参观一些葡萄园。" },
    {
      role: "AI",
      message:
        "法国拥有一些世界上最好的葡萄园。我可以帮您找到一些。",
    },
    { role: "User", message: "那太好了！" },
    { role: "AI", message: "您更喜欢红葡萄酒还是白葡萄酒？" },
    { role: "User", message: "我更喜欢红葡萄酒。" },
    {
      role: "AI",
      message:
        "太好了！我会找一些以红葡萄酒闻名的葡萄园。",
    },
    { role: "User", message: "谢谢，这将非常有帮助。" },
    {
      role: "AI",
      message:
        "不客气！我还会为您查找一些法国葡萄酒礼仪。",
    },
    {
      role: "User",
      message: "听起来很棒。我迫不及待地想开始我的旅行！",
    },
    {
      role: "AI",
      message:
        "我相信您会度过一段美妙的时光。您对您的旅行还有其他问题吗？",
    },
    { role: "User", message: "目前没有，谢谢您的帮助！" },
  ];

  const zepClient = await memory.zepClientPromise;
  if (!zepClient) {
    throw new Error("ZepClient is not initialized");
  }

  // 将聊天消息添加到内存
  for (const chatMessage of chatMessages) {
    let m: MemoryModel;
    if (chatMessage.role === "AI") {
      m = new MemoryModel({
        messages: [new Message({ role: "ai", content: chatMessage.message })],
      });
    } else {
      m = new MemoryModel({
        messages: [
          new Message({ role: "human", content: chatMessage.message }),
        ],
      });
    }

    await zepClient.memory.addMemory(zepConfig.sessionId, m);
  }

  // 等待消息被总结、丰富、嵌入和索引。
  await sleep(10000);

  // 简单相似性搜索
  const query = "Can I drive red cars in France?";
  const retriever = new ZepRetriever({ ...zepConfig, topK: 3 });
  const docs = await retriever.invoke(query);
  console.log("Simple similarity search");
  console.log(JSON.stringify(docs, null, 2));

  // mmr 重排序搜索
  const mmrRetriever = new ZepRetriever({
    ...zepConfig,
    topK: 3,
    searchType: "mmr",
    mmrLambda: 0.5,
  });
  const mmrDocs = await mmrRetriever.invoke(query);
  console.log("MMR reranking search");
  console.log(JSON.stringify(mmrDocs, null, 2));

  // 带有 mmr 重排序的摘要搜索
  const mmrSummaryRetriever = new ZepRetriever({
    ...zepConfig,
    topK: 3,
    searchScope: "summary",
    searchType: "mmr",
    mmrLambda: 0.5,
  });
  const mmrSummaryDocs = await mmrSummaryRetriever.invoke(query);
  console.log("Summary search with MMR reranking");
  console.log(JSON.stringify(mmrSummaryDocs, null, 2));

  // 筛选搜索
  const filteredRetriever = new ZepRetriever({
    ...zepConfig,
    topK: 3,
    filter: {
      where: { jsonpath: '$.system.entities[*] ? (@.Label == "GPE")' },
    },
  });
  const filteredDocs = await filteredRetriever.invoke(query);
  console.log("Filtered search");
  console.log(JSON.stringify(filteredDocs, null, 2));
};
```

## 相关

- [检索指南](/oss/javascript/langchain/retrieval)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/retrievers/zep-retriever.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>