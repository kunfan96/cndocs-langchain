---
title: "MyScale 集成"
description: "使用 LangChain JavaScript 集成 MyScale 向量存储。"
---

<Tip>
**兼容性**

仅在 Node.js 上可用。
</Tip>

[MyScale](https://myscale.com/) 是一个新兴的 AI 数据库，它协调了向量搜索和 SQL 分析的强大功能，提供了托管、高效且响应迅速的体验。

## 设置

1. 通过 [MyScale Web 控制台](https://console.myscale.com/) 启动集群。有关更多信息，请参阅 [MyScale 的官方文档](https://docs.myscale.com/en/quickstart/)。
2. 启动集群后，从集群的 `Actions` 菜单中查看您的 `Connection Details`。您将需要主机、端口、用户名和密码。
3. 在您的工作区中安装所需的 Node.js 对等依赖项。

<Tip>
请参阅 [此部分](/oss/javascript/langchain/install) 以获取有关安装 LangChain 包的一般说明。
</Tip>

```bash npm
npm install -S @langchain/openai @clickhouse/client @langchain/community @langchain/core
```

## 索引和查询文档

```typescript
import { MyScaleStore } from "@langchain/community/vectorstores/myscale";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await MyScaleStore.fromTexts(
  ["Hello world", "Bye bye", "hello nice world"],
  [
    { id: 2, name: "2" },
    { id: 1, name: "1" },
    { id: 3, name: "3" },
  ],
  new OpenAIEmbeddings(),
  {
    host: process.env.MYSCALE_HOST || "localhost",
    port: process.env.MYSCALE_PORT || "8443",
    username: process.env.MYSCALE_USERNAME || "username",
    password: process.env.MYSCALE_PASSWORD || "password",
    database: "default", // defaults to "default"
    table: "your_table", // defaults to "vector_table"
  }
);

const results = await vectorStore.similaritySearch("hello world", 1);
console.log(results);

const filteredResults = await vectorStore.similaritySearch("hello world", 1, {
  whereStr: "metadata.name = '1'",
});
console.log(filteredResults);
```

## 从现有集合中查询文档

```typescript
import { MyScaleStore } from "@langchain/community/vectorstores/myscale";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await MyScaleStore.fromExistingIndex(
  new OpenAIEmbeddings(),
  {
    host: process.env.MYSCALE_HOST || "localhost",
    port: process.env.MYSCALE_PORT || "8443",
    username: process.env.MYSCALE_USERNAME || "username",
    password: process.env.MYSCALE_PASSWORD || "password",
    database: "default", // defaults to "default"
    table: "your_table", // defaults to "vector_table"
  }
);

const results = await vectorStore.similaritySearch("hello world", 1);
console.log(results);

const filteredResults = await vectorStore.similaritySearch("hello world", 1, {
  whereStr: "metadata.name = '1'",
});
console.log(filteredResults);
```

## 相关

- 向量存储 [概念指南](/oss/javascript/integrations/vectorstores)
- 向量存储 [操作指南](/oss/javascript/integrations/vectorstores)

---

<div className="source-links">
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/vectorstores/myscale.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
</div>
