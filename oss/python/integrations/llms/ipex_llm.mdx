---
title: "IPEX-LLM 集成"
description: "使用 LangChain Python 与 IPEX-LLM 集成。"
---

> [IPEX-LLM](https://github.com/intel-analytics/ipex-llm) 是一个 PyTorch 库，可在 Intel CPU 和 GPU（例如搭载集成显卡的本地 PC、Arc、Flex 和 Max 等独立显卡）上以极低延迟运行 LLM。

- [IPEX-LLM 在 Intel GPU 上运行](#ipex-llm-on-intel-gpu)
- [IPEX-LLM 在 Intel CPU 上运行](#ipex-llm-on-intel-cpu)

## IPEX-LLM 在 Intel GPU 上运行

本示例介绍如何使用 LangChain 与 `ipex-llm` 交互，在 Intel GPU 上进行文本生成。

> **注意**
>
> 建议只有搭载 Intel Arc A 系列 GPU（Intel Arc A300 系列或 Pro A60 除外）的 Windows 用户在"IPEX-LLM 在 Intel GPU 上运行"章节中直接运行 Jupyter notebook。其他情况（如 Linux 用户、Intel iGPU 等），建议在终端中使用 Python 脚本运行代码以获得最佳体验。

### 安装前置条件

要在 Intel GPU 上使用 IPEX-LLM，需要完成几个工具安装和环境准备的前置步骤。

如果您是 Windows 用户，请访问 [在 Windows 上安装 IPEX-LLM（Intel GPU）指南](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md)，并按照[安装前置条件](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_windows_gpu.md#install-prerequisites)更新 GPU 驱动（可选）并安装 Conda。

如果您是 Linux 用户，请访问 [在 Linux 上安装 IPEX-LLM（Intel GPU）](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md)，并按照[**安装前置条件**](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Quickstart/install_linux_gpu.md#install-prerequisites)安装 GPU 驱动、Intel® oneAPI Base Toolkit 2024.0 和 Conda。

### 设置

完成前置条件安装后，您应该已创建了一个安装了所有前置条件的 conda 环境。**在此 conda 环境中启动 jupyter 服务**：

```python
pip install -qU langchain langchain-community
```

安装 IEPX-LLM 以在 Intel GPU 上本地运行 LLM。

```python
pip install --pre --upgrade ipex-llm[xpu] --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
```

> **注意**
>
> 您也可以使用 `https://pytorch-extension.intel.com/release-whl/stable/xpu/cn/` 作为 extra-index-url。

### 运行时配置

为获得最佳性能，建议根据您的设备设置以下环境变量：

#### 适用于搭载 Intel Core Ultra 集成显卡的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
os.environ["BIGDL_LLM_XMX_DISABLED"] = "1"
```

#### 适用于搭载 Intel Arc A 系列 GPU 的 Windows 用户

```python
import os

os.environ["SYCL_CACHE_PERSISTENT"] = "1"
```

> **注意**
>
> 每个模型首次在 Intel iGPU/Intel Arc A300 系列或 Pro A60 上运行时，可能需要几分钟进行编译。
>
> 对于其他 GPU 类型，Windows 用户请参考[此处](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration)，Linux 用户请参考[此处](https://github.com/intel-analytics/ipex-llm/blob/main/docs/mddocs/Overview/install_gpu.md#runtime-configuration-1)。

### 基本用法

```python
import warnings

from langchain_classic.chains import LLMChain
from langchain_community.llms import IpexLLM
from langchain_core.prompts import PromptTemplate

warnings.filterwarnings("ignore", category=UserWarning, message=".*padding_mask.*")
```

为您的模型指定 prompt 模板。本示例使用 [vicuna-1.5](https://huggingface.co/lmsys/vicuna-7b-v1.5) 模型。如果您使用不同的模型，请相应选择合适的模板。

```python
template = "USER: {question}\nASSISTANT:"
prompt = PromptTemplate(template=template, input_variables=["question"])
```

使用 IpexLLM 通过 `IpexLLM.from_model_id` 在本地加载模型。它将直接以 Hugging Face 格式加载模型，并自动将其转换为低比特格式进行推理。在初始化 IpexLLM 时，将 `model_kwargs` 中的 `device` 设置为 `"xpu"` 以将 LLM 模型加载到 Intel GPU。

```python
llm = IpexLLM.from_model_id(
    model_id="lmsys/vicuna-7b-v1.5",
    model_kwargs={
        "temperature": 0,
        "max_length": 64,
        "trust_remote_code": True,
        "device": "xpu",
    },
)
```

在链中使用

```python
llm_chain = prompt | llm

question = "What is AI?"
output = llm_chain.invoke(question)
```

### 保存/加载低比特模型

您也可以将低比特模型一次性保存到磁盘，之后使用 `from_model_id_low_bit` 代替 `from_model_id` 重新加载，甚至可以跨机器使用。这样更节省空间，因为低比特模型占用的磁盘空间远少于原始模型。而且 `from_model_id_low_bit` 在速度和内存使用方面也比 `from_model_id` 更高效，因为它跳过了模型转换步骤。您同样可以在 `model_kwargs` 中将 `device` 设置为 `"xpu"` 以将 LLM 模型加载到 Intel GPU。

使用 `save_low_bit` 保存低比特模型，如下所示。

```python
saved_lowbit_model_path = "./vicuna-7b-1.5-low-bit"  # path to save low-bit model
llm.model.save_low_bit(saved_lowbit_model_path)
del llm
```

从已保存的低比特模型路径加载模型，如下所示。
> 注意，低比特模型的保存路径只包含模型本身，不包含 tokenizer。如果您希望将所有内容放在同一位置，需要手动将原始模型目录中的 tokenizer 文件下载或复制到低比特模型的保存位置。

```python
llm_lowbit = IpexLLM.from_model_id_low_bit(
    model_id=saved_lowbit_model_path,
    tokenizer_id="lmsys/vicuna-7b-v1.5",
    # tokenizer_name=saved_lowbit_model_path,  # copy the tokenizers to saved path if you want to use it this way
    model_kwargs={
        "temperature": 0,
        "max_length": 64,
        "trust_remote_code": True,
        "device": "xpu",
    },
)
```

在链中使用已加载的模型：

```python
llm_chain = prompt | llm_lowbit


question = "What is AI?"
output = llm_chain.invoke(question)
```

## IPEX-LLM 在 Intel CPU 上运行

本示例介绍如何使用 LangChain 与 `ipex-llm` 交互，在 Intel CPU 上进行文本生成。

### 设置

```python
# Update LangChain

pip install -qU langchain langchain-community
```

安装 IEPX-LLM 以在 Intel CPU 上本地运行 LLM：

#### 适用于 Windows 用户

```python
pip install --pre --upgrade ipex-llm[all]
```

#### 适用于 Linux 用户

```python
pip install --pre --upgrade ipex-llm[all] --extra-index-url https://download.pytorch.org/whl/cpu
```

### 基本用法

```python
import warnings

from langchain_classic.chains import LLMChain
from langchain_community.llms import IpexLLM
from langchain_core.prompts import PromptTemplate

warnings.filterwarnings("ignore", category=UserWarning, message=".*padding_mask.*")
```

为您的模型指定 prompt 模板。本示例使用 [vicuna-1.5](https://huggingface.co/lmsys/vicuna-7b-v1.5) 模型。如果您使用不同的模型，请相应选择合适的模板。

```python
template = "USER: {question}\nASSISTANT:"
prompt = PromptTemplate(template=template, input_variables=["question"])
```

使用 IpexLLM 通过 `IpexLLM.from_model_id` 在本地加载模型。它将直接以 Hugging Face 格式加载模型，并自动将其转换为低比特格式进行推理。

```python
llm = IpexLLM.from_model_id(
    model_id="lmsys/vicuna-7b-v1.5",
    model_kwargs={"temperature": 0, "max_length": 64, "trust_remote_code": True},
)
```

在链中使用：

```python
llm_chain = prompt | llm

question = "What is AI?"
output = llm_chain.invoke(question)
```

### 保存/加载低比特模型

您也可以将低比特模型一次性保存到磁盘，之后使用 `from_model_id_low_bit` 代替 `from_model_id` 重新加载，甚至可以跨机器使用。这样更节省空间，因为低比特模型占用的磁盘空间远少于原始模型。而且 `from_model_id_low_bit` 在速度和内存使用方面也比 `from_model_id` 更高效，因为它跳过了模型转换步骤。

使用 `save_low_bit` 保存低比特模型，如下所示：

```python
saved_lowbit_model_path = "./vicuna-7b-1.5-low-bit"  # path to save low-bit model
llm.model.save_low_bit(saved_lowbit_model_path)
del llm
```

从已保存的低比特模型路径加载模型，如下所示。

> 注意，低比特模型的保存路径只包含模型本身，不包含 tokenizer。如果您希望将所有内容放在同一位置，需要手动将原始模型目录中的 tokenizer 文件下载或复制到低比特模型的保存位置。

```python
llm_lowbit = IpexLLM.from_model_id_low_bit(
    model_id=saved_lowbit_model_path,
    tokenizer_id="lmsys/vicuna-7b-v1.5",
    # tokenizer_name=saved_lowbit_model_path,  # copy the tokenizers to saved path if you want to use it this way
    model_kwargs={"temperature": 0, "max_length": 64, "trust_remote_code": True},
)
```

在链中使用已加载的模型：

```python
llm_chain = prompt | llm_lowbit


question = "What is AI?"
output = llm_chain.invoke(question)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/ipex_llm.mdx) 或 [提交 Issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs)到 Claude、VSCode 等，通过 MCP 获取实时解答。
</Callout>
</div>
