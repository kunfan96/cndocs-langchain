---
title: "C Transformers 集成"
description: "使用 LangChain Python 与 C Transformers LLM 进行集成。"
---

[C Transformers](https://github.com/marella/ctransformers) 库为 GGML 模型提供了 Python 绑定。

本示例介绍如何使用 LangChain 与 `C Transformers` [模型](https://github.com/marella/ctransformers#supported-models) 进行交互。

**安装**

```python
pip install -qU  ctransformers
```

**加载模型**

```python
from langchain_community.llms import CTransformers

llm = CTransformers(model="marella/gpt-2-ggml")
```

**生成文本**

```python
print(llm.invoke("AI is going to"))
```

**流式输出**

```python
from langchain_core.callbacks import StreamingStdOutCallbackHandler

llm = CTransformers(
    model="marella/gpt-2-ggml", callbacks=[StreamingStdOutCallbackHandler()]
)

response = llm.invoke("AI is going to")
```

**LLMChain**

```python
from langchain_classic.chains import LLMChain
from langchain_core.prompts import PromptTemplate

template = """Question: {question}

Answer:"""

prompt = PromptTemplate.from_template(template)

llm_chain = LLMChain(prompt=prompt, llm=llm)

response = llm_chain.run("What is AI?")
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/ctransformers.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将本文档连接到](/use-these-docs) Claude、VSCode 等工具，以获取实时答案。
</Callout>
</div>