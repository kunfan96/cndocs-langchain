---
title: "Discord 集成"
description: "使用 LangChain Python 与 Discord 文档加载器集成。"
---

>[Discord](https://discord.com/) 是一个 VoIP 和即时消息社交平台。用户可以通过语音通话、视频通话、文本消息、媒体和文件在私人聊天或作为称为“服务器”的社区的一部分进行交流。服务器是持久聊天室和语音频道的集合，可以通过邀请链接访问。

按照以下步骤下载您的 `Discord` 数据：

1. 转到您的 **用户设置**
2. 然后转到 **隐私和安全**
3. 前往 **请求我的所有数据** 并点击 **请求数据** 按钮

您可能需要 30 天才能收到您的数据。您将收到一封发送到在 Discord 注册的地址的电子邮件。该电子邮件将包含一个下载按钮，使用该按钮您可以下载您的个人 Discord 数据。

```python
import os

import pandas as pd
```

```python
path = input('Please enter the path to the contents of the Discord "messages" folder: ')
li = []
for f in os.listdir(path):
    expected_csv_path = os.path.join(path, f, "messages.csv")
    csv_exists = os.path.isfile(expected_csv_path)
    if csv_exists:
        df = pd.read_csv(expected_csv_path, index_col=None, header=0)
        li.append(df)

df = pd.concat(li, axis=0, ignore_index=True, sort=False)
```

```python
from langchain_community.document_loaders.discord import DiscordChatLoader
```

```python
loader = DiscordChatLoader(df, user_id_col="ID")
print(loader.load())
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/document_loaders/discord.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>