---
title: "Wolfram Alpha 集成"
description: "使用 LangChain Python 与 Wolfram Alpha 集成。"
---

>[WolframAlpha](https://en.wikipedia.org/wiki/WolframAlpha) 是由 `Wolfram Research` 开发的答案引擎。
> 它通过计算来自外部数据的答案来回答事实性查询。

本页介绍如何在 LangChain 中使用 `Wolfram Alpha API`。

## 安装与设置
- 使用以下命令安装依赖项
<CodeGroup>
```bash pip
pip install wolframalpha
```

```bash uv
uv add wolframalpha
```
</CodeGroup>

- 注册一个 [Wolfram Alpha 开发者账户](https://developer.wolframalpha.com/)
- 创建一个应用并获取你的 `APP ID`
- 将你的 APP ID 设置为环境变量 `WOLFRAM_ALPHA_APPID`

## 封装器

### 实用工具

存在一个 WolframAlphaAPIWrapper 实用工具，它封装了此 API。要导入此工具：

```python
from langchain_community.utilities.wolfram_alpha import WolframAlphaAPIWrapper
```

有关此封装器的更详细演练，请参阅[此笔记本](/oss/python/integrations/tools/wolfram_alpha)。

### 工具

你也可以轻松地将此封装器作为工具加载（用于与代理配合使用）。
你可以这样做：
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["wolfram-alpha"])
```

有关工具的更多信息，请参阅[工具](/oss/python/integrations/tools)。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/wolfram_alpha.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>