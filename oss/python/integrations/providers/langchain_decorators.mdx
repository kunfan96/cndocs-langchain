---
title: "LangChain 装饰器集成"
description: "使用 LangChain Python 与 LangChain 装饰器进行集成。"
---

~~~
免责声明：`LangChain decorators` 并非由 LangChain 团队创建，也不受其支持。
~~~

>`LangChain decorators` 是建立在 LangChain 之上的一层，为编写自定义的 langchain 提示词和链提供了语法糖 🍭
>
>如需反馈、提交问题或贡献代码，请在此处提出问题：
>[ju-bezdek/langchain-decorators](https://github.com/ju-bezdek/langchain-decorators)


主要原则与优势：

- 采用更符合 `Pythonic` 规范的编码方式
- 编写多行提示词，无需担心缩进破坏代码流程
- 利用 IDE 内置的**智能提示**、**类型检查**和**文档弹窗**功能，快速查看函数以了解提示词、所需参数等
- 充分利用 🦜🔗 LangChain 生态系统的全部能力
- 支持**可选参数**
- 通过将参数绑定到同一个类，轻松在提示词之间共享参数


以下是使用 **LangChain Decorators ✨** 编写的简单代码示例：

``` python
@llm_prompt
def write_me_short_post(topic:str, platform:str="twitter", audience:str = "developers")->str:
    """
    Write me a short header for my post about {topic} for {platform} platform.
    It should be for {audience} audience.
    (Max 15 words)
    """
    return

# 自然运行它
write_me_short_post(topic="starwars")
# 或者
write_me_short_post(topic="starwars", platform="redit")
```

# 快速开始

## 安装

<CodeGroup>
```bash
pip install langchain_decorators
```

```bash uv
uv add langchain_decorators
```
</CodeGroup>

## 示例

建议从这里开始查看以下示例：
    - [Jupyter Notebook](https://github.com/ju-bezdek/langchain-decorators/blob/main/example_notebook.ipynb)
    - [Colab Notebook](https://colab.research.google.com/drive/1no-8WfeP6JaLD9yUtkPgym6x0G9ZYZOG#scrollTo=N4cf__D0E2Yk)

# 定义其他参数
此处我们仅使用 `llm_prompt` 装饰器将函数标记为提示词，从而使其实质上成为一个 LLMChain。与其直接运行它，


标准的 LLMChain 接受的初始化参数远不止 input_variables 和 prompt……这些实现细节已隐藏在装饰器中。
具体工作原理如下：

1. 使用**全局设置**：

``` python
# 为所有提示词定义全局设置（若未设置，则默认使用 ChatGPT）
from langchain_decorators import GlobalSettings

GlobalSettings.define_settings(
    default_llm=ChatOpenAI(temperature=0.0), # 此为默认设置……可在此处全局修改
    default_streaming_llm=ChatOpenAI(temperature=0.0, streaming=True), # 此为默认设置……可在此处全局修改……将用于流式输出
)
```
2. 使用预定义的**提示词类型**

``` python
# 您可以更改默认的提示词类型
from langchain_decorators import PromptTypes, PromptTypeSettings

PromptTypes.AGENT_REASONING.llm = ChatOpenAI()

# 或者您也可以自定义自己的类型：
class MyCustomPromptTypes(PromptTypes):
    GPT4 = PromptTypeSettings(llm=ChatOpenAI(model="gpt-4"))

@llm_prompt(prompt_type=MyCustomPromptTypes.GPT4)
def write_a_complicated_code(app_idea:str)->str:
    ...

```
3. 在装饰器中**直接定义设置**

``` python
from langchain_openai import OpenAI

@llm_prompt(
    llm=OpenAI(temperature=0.7),
    stop_tokens=["\nObservation"],
    ...
    )
def creative_writer(book_title:str)->str:
    ...
```
## 传递内存和/或回调：

要传递上述任意参数，只需在函数中声明它们即可（或使用 kwargs 传递任何内容）。

```python

@llm_prompt()
async def write_me_short_post(topic:str, platform:str="twitter", memory:SimpleMemory = None):
    """
    {history_key}
    Write me a short header for my post about {topic} for {platform} platform.
    It should be for {audience} audience.
    (Max 15 words)
    """
    pass

await write_me_short_post(topic="old movies")

```
# 简化流式输出

如果我们希望利用流式输出功能：
 - 需要将提示词定义为异步函数
 - 在装饰器上开启流式输出，或者我们可以定义带有流式输出的 PromptType
 - 使用 StreamingContext 捕获流

这样我们只需标记哪些提示词需要流式输出，而无需纠结应使用哪个 LLM，也无需在创建后将流处理程序分发给链的特定部分……只需在提示词/提示词类型上开关流式输出即可。

仅当我们在流式上下文中调用时才会发生流式输出……在此处我们可以定义一个简单的函数来处理流。

``` python
# 此代码示例完整，可直接运行

from langchain_decorators import StreamingContext, llm_prompt

# 这将标记提示词以进行流式输出（如果您只想在应用中流式输出某些提示词……但不想分发回调处理程序，这将非常有用）
# 注意：只有异步函数才能进行流式输出（如果不是则会报错）
@llm_prompt(capture_stream=True)
async def write_me_short_post(topic:str, platform:str="twitter", audience:str = "developers"):
    """
    Write me a short header for my post about {topic} for {platform} platform.
    It should be for {audience} audience.
    (Max 15 words)
    """
    pass



# 仅为演示流式输出而编写的任意函数……在实际应用中将是 WebSocket 相关代码
tokens=[]
def capture_stream_func(new_token:str):
    tokens.append(new_token)

# 如果我们要捕获流，则需要将执行过程包装在 StreamingContext 中……
# 即使提示词调用隐藏在更高级别的方法内部，这也允许我们捕获流
# 只有标记了 capture_stream 的提示词才会在此处被捕获
with StreamingContext(stream_to_stdout=True, callback=capture_stream_func):
    result = await run_prompt()
    print("Stream finished ... we can distinguish tokens thanks to alternating colors")


print("\nWe've captured",len(tokens),"tokens🎉\n")
print("Here is the result:")
print(result)
```
# 提示词声明
默认情况下，提示词为整个函数的文档字符串，除非您明确标记您的提示词。

## 记录您的提示词

我们可以通过指定带有 `<prompt>` 语言标签的代码块，来指明文档中哪一部分是提示词定义。

``` python
@llm_prompt
def write_me_short_post(topic:str, platform:str="twitter", audience:str = "developers"):
    """
    这是将提示词作为函数文档字符串一部分编写的良好方式，并附带面向开发者的额外文档说明。

    它必须是一个代码块，并标记为 `<prompt>` 语言
    ```<prompt>
    Write me a short header for my post about {topic} for {platform} platform.
    It should be for {audience} audience.
    (Max 15 words)
    ```
    现在只有上面的代码块会被用作提示词，而文档字符串的其余部分将用作面向开发者的描述。
    （这还有一个好处：IDE（如 VS Code）会正确显示提示词（不会尝试将其解析为 Markdown，从而避免换行符显示不正确的问题））
    """
    return
```

## 聊天消息提示词

对于聊天模型而言，将提示词定义为一组消息模板非常有用……操作方法如下：

``` python
@llm_prompt
def simulate_conversation(human_input:str, agent_role:str="a pirate"):
    """
    ## 系统消息
     - 注意 `<prompt:_role_>` 标签内的 `:system` 后缀


    ```<prompt:system>
    You are a {agent_role} hacker. You mus act like one.
    You reply always in code, using python or javascript code block...
    for example:

    ... do not reply with anything else.. just with code - respecting your role.
    ```
    # 人类消息
    （我们使用的是 LLM 强制执行的真实角色——GPT 支持 system、assistant、user）
    ``` <prompt:user>
    Helo, who are you
    ```
    回复：


    ``` <prompt:assistant>
    \``` python <<- 使用 \ 转义内部代码块，该转义符应作为提示词的一部分
    def hello():
        print("Argh... hello you pesky pirate")
    \```
    ```

    我们还可以使用占位符添加一些历史记录
    ```<prompt:placeholder>
    {history}
    ```
    ```<prompt:user>
    {human_input}
    ```

    现在只有上面的代码块会被用作提示词，而文档字符串的其余部分将用作面向开发者的描述。
    （这还有一个好处：IDE（如 VS Code）会正确显示提示词（不会尝试将其解析为 Markdown，从而避免换行符显示不正确的问题））
    """
    pass

```

此处的角色为模型原生角色（针对 ChatGPT 分别为 assistant、user、system）



# 可选部分
- 您可以定义提示词中的整个可选部分
- 如果该部分缺少任何输入，则整个部分都不会被渲染

其语法如下：

``` python
@llm_prompt
def prompt_with_optional_partials():
    """
    this text will be rendered always, but

    {? anything inside this block will be rendered only if all the {value}s parameters are not empty (None | "")   ?}

    you can also place it in between the words
    this too will be rendered{? , but
        this  block will be rendered only if {this_value} and {this_value}
        is not empty?} !
    """
```
# 输出解析器

- `llm_prompt` 装饰器会基于输出类型自动尝试检测最佳输出解析器。（若未设置，则返回原始字符串）
- 原生（自动）支持 list、dict 和 pydantic 类型的输出

``` python
# 此代码示例完整，可直接运行

from langchain_decorators import llm_prompt

@llm_prompt
def write_name_suggestions(company_business:str, count:int)->list:
    """ Write me {count} good name suggestions for company that {company_business}
    """
    pass

write_name_suggestions(company_business="sells cookies", count=5)
```
## 更复杂的结构

对于 dict / pydantic 类型，您需要指定格式化指令……
这可能比较繁琐，因此您可以让输出解析器根据模型（pydantic）为您生成指令。

``` python
from langchain_decorators import llm_prompt
from pydantic import BaseModel, Field


class TheOutputStructureWeExpect(BaseModel):
    name:str = Field (description="The name of the company")
    headline:str = Field( description="The description of the company (for landing page)")
    employees:list[str] = Field(description="5-8 fake employee names with their positions")

@llm_prompt()
def fake_company_generator(company_business:str)->TheOutputStructureWeExpect:
    """ Generate a fake company that {company_business}
    {FORMAT_INSTRUCTIONS}
    """
    return

company = fake_company_generator(company_business="sells cookies")

# 以格式良好的方式打印结果
print("Company name: ",company.name)
print("company headline: ",company.headline)
print("company employees: ",company.employees)

```
# 将提示词绑定到对象

``` python
from pydantic import BaseModel
from langchain_decorators import llm_prompt

class AssistantPersonality(BaseModel):
    assistant_name:str
    assistant_role:str
    field:str

    @property
    def a_property(self):
        return "whatever"

    def hello_world(self, function_kwarg:str=None):
        """
        We can reference any {field} or {a_property} inside our prompt... and combine it with {function_kwarg} in the method
        """


    @llm_prompt
    def introduce_your_self(self)->str:
        """
        ``` <prompt:system>
        You are an assistant named {assistant_name}.
        Your role is to act as {assistant_role}
        ```
        ```<prompt:user>
        Introduce your self (in less than 20 words)
        ```
        """



personality = AssistantPersonality(assistant_name="John", assistant_role="a pirate")

print(personality.introduce_your_self(personality))
```


# 更多示例：

- 这些以及更多示例也可在 [此处 Colab Notebook](https://colab.research.google.com/drive/1no-8WfeP6JaLD9yUtkPgym6x0G9ZYZOG#scrollTo=N4cf__D0E2Yk) 中找到
- 包括完全使用 langchain 装饰器实现的 [ReAct Agent 重新实现](https://colab.research.google.com/drive/1no-8WfeP6JaLD9yUtkPgym6x0G9ZYZOG#scrollTo=3bID5fryE2Yp)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/langchain_decorators.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接到](/use-these-docs) Claude、VSCode 等，并通过 MCP 获取实时答案。
</Callout>
</div>