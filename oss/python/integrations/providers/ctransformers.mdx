---
title: "C Transformers 集成"
description: "使用 LangChain Python 与 C Transformers 进行集成。"
---

本页介绍如何在 LangChain 中使用 [C Transformers](https://github.com/marella/ctransformers) 库。
内容分为两部分：安装与设置，以及针对特定 C Transformers 包装器的参考文档。

## 安装与设置

- 使用 `pip install ctransformers` 安装 Python 包
- 下载受支持的 [GGML 模型](https://huggingface.co/TheBloke)（参见 [受支持的模型](https://github.com/marella/ctransformers#supported-models)）

## 包装器

### LLM

CTransformers 提供了 LLM 包装器，你可以通过以下方式访问它：

```python
from langchain_community.llms import CTransformers
```

它为所有模型提供了一个统一的接口：

```python
llm = CTransformers(model='/path/to/ggml-gpt-2.bin', model_type='gpt2')

print(llm.invoke('AI is going to'))
```

如果遇到 `illegal instruction` 错误，请尝试使用 `lib='avx'` 或 `lib='basic'`：

```py
llm = CTransformers(model='/path/to/ggml-gpt-2.bin', model_type='gpt2', lib='avx')
```

它可以与托管在 Hugging Face Hub 上的模型配合使用：

```py
llm = CTransformers(model='marella/gpt-2-ggml')
```

如果模型仓库包含多个模型文件（`.bin` 文件），请使用以下方式指定模型文件：

```py
llm = CTransformers(model='marella/gpt-2-ggml', model_file='ggml-model.bin')
```

可以使用 `config` 参数传递其他参数：

```py
config = {'max_new_tokens': 256, 'repetition_penalty': 1.1}

llm = CTransformers(model='marella/gpt-2-ggml', config=config)
```

有关可用参数列表，请参阅 [文档](https://github.com/marella/ctransformers#config)。

如需更详细的操作指南，请参阅 [此笔记本](/oss/python/integrations/llms/ctransformers)。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/ctransformers.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到](/use-these-docs) Claude、VSCode 等工具，以获取实时答案。
</Callout>
</div>