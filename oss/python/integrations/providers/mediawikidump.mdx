---
title: "Mediawikidump 集成"
description: "使用 LangChain Python 集成 Mediawikidump。"
---

>[MediaWiki XML 转储](https://www.mediawiki.org/wiki/Manual:Importing_XML_dumps) 包含 wiki 的内容
>（wiki 页面及其所有修订版本），但不包含站点相关数据。XML 转储不会创建 wiki 数据库的完整备份，
> 不包含用户账户、图像、编辑日志等。


## 安装与设置

我们需要安装几个 Python 包。

`mediawiki-utilities` 在未合并的分支中支持 XML 模式 0.11。

<CodeGroup>
```bash pip
pip install -qU git+https://github.com/mediawiki-utilities/python-mwtypes@updates_schema_0.11
```

```bash uv
uv add U git+https://github.com/mediawiki-utilities/python-mwtypes@updates_schema_0.11
```
</CodeGroup>

`mediawiki-utilities mwxml` 存在一个 bug，修复 PR 待审核中。

<CodeGroup>
```bash pip
pip install -qU git+https://github.com/gdedrouas/python-mwxml@xml_format_0.11
pip install -qU mwparserfromhell
```

```bash uv
uv add git+https://github.com/gdedrouas/python-mwxml@xml_format_0.11
uv add mwparserfromhell
```
</CodeGroup>

## 文档加载器

查看[使用示例](/oss/python/integrations/document_loaders/mediawikidump)。


```python
from langchain_community.document_loaders import MWDumpLoader
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/mediawikidump.mdx) 或 [提交 issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
