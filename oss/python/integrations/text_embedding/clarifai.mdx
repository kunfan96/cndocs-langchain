---
title: "Clarifai 集成"
description: "使用 LangChain Python 集成 Clarifai 嵌入模型。"
---

>[Clarifai](https://www.clarifai.com/) 是一个 AI 平台，提供从数据探索、数据标注、模型训练、评估到推理的完整 AI 生命周期服务。

本示例介绍如何使用 LangChain 与 `Clarifai` [模型](https://clarifai.com/explore/models)交互。文本嵌入模型可在[此处](https://clarifai.com/explore/models?page=1&perPage=24&filterData=%5B%7B%22field%22%3A%22model_type_id%22%2C%22value%22%3A%5B%22text-embedder%22%5D%7D%5D)找到。

使用 Clarifai 前，您必须拥有账户和个人访问令牌（PAT）密钥。
[点击此处](https://clarifai.com/settings/security)获取或创建 PAT。

# 依赖项

```python
# Install required dependencies
pip install -qU  clarifai
```

# 导入

在此设置个人访问令牌。您可以在 Clarifai 账户的[设置/安全](https://clarifai.com/settings/security)下找到您的 PAT。

```python
# Please login and get your API key from  https://clarifai.com/settings/security
from getpass import getpass

CLARIFAI_PAT = getpass()
```

```python
# Import the required modules
from langchain_classic.chains import LLMChain
from langchain_community.embeddings import ClarifaiEmbeddings
from langchain_core.prompts import PromptTemplate
```

# 输入

创建与 LLM Chain 配合使用的提示词模板：

```python
template = """Question: {question}

Answer: Let's think step by step."""

prompt = PromptTemplate.from_template(template)
```

# 设置

将 user id 和 app id 设置为模型所在的应用程序。您可以在 [clarifai.com/explore/models](https://clarifai.com/explore/models) 找到公开模型列表。

您还需要初始化模型 ID，如有需要，还需设置模型版本 ID。某些模型有多个版本，您可以选择适合您任务的版本。

```python
USER_ID = "clarifai"
APP_ID = "main"
MODEL_ID = "BAAI-bge-base-en-v15"
MODEL_URL = "https://clarifai.com/clarifai/main/models/BAAI-bge-base-en-v15"

# Further you can also provide a specific model version as the model_version_id arg.
# MODEL_VERSION_ID = "MODEL_VERSION_ID"
```

```python
# Initialize a Clarifai embedding model
embeddings = ClarifaiEmbeddings(user_id=USER_ID, app_id=APP_ID, model_id=MODEL_ID)

# Initialize a clarifai embedding model using model URL
embeddings = ClarifaiEmbeddings(model_url=MODEL_URL)

# Alternatively you can initialize clarifai class with pat argument.
```

```python
text = "roses are red violets are blue."
text2 = "Make hay while the sun shines."
```

您可以使用 embed_query 函数嵌入单条文本！

```python
query_result = embeddings.embed_query(text)
```

还可以使用 embed_documents 函数嵌入文本/文档列表。

```python
doc_result = embeddings.embed_documents([text, text2])
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/text_embedding/clarifai.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    通过 MCP [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等，获取实时解答。
</Callout>
</div>
