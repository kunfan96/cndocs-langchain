```python
from langchain.tools import tool


@tool
def get_weather_data(city: str) -> dict:
    """获取城市的结构化天气数据。"""
    return {
        "city": city,
        "temperature_c": 22,
        "conditions": "sunny",
    }
```
