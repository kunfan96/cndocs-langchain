---
title: "OctoAI 集成"
description: "使用 LangChain Python 与 OctoAI LLM 进行集成。"
---

[OctoAI](https://docs.octoai.cloud/docs) 提供对高效计算资源的便捷访问，并允许用户将其选择的 AI 模型集成到应用程序中。`OctoAI` 计算服务可帮助您轻松运行、调优和扩展 AI 应用。

本示例介绍如何使用 LangChain 与 `OctoAI` [LLM 端点](https://octoai.cloud/templates) 进行交互。

## 环境配置

要运行我们的示例应用，只需执行以下两个简单步骤：

1. 从[您的 OctoAI 账户页面](https://octoai.cloud/settings)获取 API Token。

2. 将您的 API Key 粘贴到下方的代码单元格中。

注意：如果您想使用不同的 LLM 模型，可以按照[使用 Python 构建容器](https://octo.ai/docs/bring-your-own-model/advanced-build-a-container-from-scratch-in-python)和[从容器创建自定义端点](https://octo.ai/docs/bring-your-own-model/create-custom-endpoints-from-a-container/create-custom-endpoints-from-a-container)的指南，自行将模型容器化并创建自定义 OctoAI 端点，然后更新您的 `OCTOAI_API_BASE` 环境变量。

```python
import os

os.environ["OCTOAI_API_TOKEN"] = "OCTOAI_API_TOKEN"
```

```python
from langchain_classic.chains import LLMChain
from langchain_community.llms.octoai_endpoint import OctoAIEndpoint
from langchain_core.prompts import PromptTemplate
```

## 示例

```python
template = """Below is an instruction that describes a task. Write a response that appropriately completes the request.\n Instruction:\n{question}\n Response: """
prompt = PromptTemplate.from_template(template)
```

```python
llm = OctoAIEndpoint(
    model_name="llama-2-13b-chat-fp16",
    max_tokens=200,
    presence_penalty=0,
    temperature=0.1,
    top_p=0.9,
)
```

```python
question = "Who was Leonardo da Vinci?"

chain = prompt | llm

print(chain.invoke(question))
```

列奥纳多·达·芬奇是一位真正的文艺复兴时期通才。他于 1452 年出生在意大利的芬奇镇，以其在艺术、科学、工程和数学等多个领域的成就而闻名。他被公认为有史以来最伟大的画家之一，其最著名的作品包括《蒙娜丽莎》和《最后的晚餐》。除了艺术创作，达·芬奇在工程和人体解剖学方面也做出了重大贡献，他对机器和发明的设计领先时代数个世纪。他还以大量的笔记和手稿著称，这些资料为了解他的思想和创意提供了宝贵见解。达·芬奇的遗产至今仍在激励和影响世界各地的艺术家、科学家和思想家。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/octoai.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs)至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>