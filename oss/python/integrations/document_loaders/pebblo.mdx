---
title: "Pebblo 安全 DocumentLoader 集成"
description: "使用 LangChain Python 与 Pebblo 安全 DocumentLoader 文档加载器集成。"
---

> [Pebblo](https://daxa-ai.github.io/pebblo/) 使开发者能够安全地加载数据，并将其 Gen AI 应用推广至部署，而无需担心组织的合规性和安全要求。该项目会识别加载数据中发现的语义主题和实体，并在 UI 或 PDF 报告中进行汇总。

Pebblo 包含两个组件。

1. 用于 LangChain 的 Pebblo Safe DocumentLoader
1. Pebblo Server

本文档描述了如何使用 Pebblo Safe DocumentLoader 来增强您现有的 LangChain DocumentLoader，以获取关于 Gen-AI LangChain 应用程序中摄入的主题和实体类型的深度数据可见性。有关 `Pebblo Server` 的详细信息，请参阅此 [pebblo server](https://daxa-ai.github.io/pebblo/daemon) 文档。

Pebblo Safeloader 为 LangChain `DocumentLoader` 提供安全的数据摄入功能。这是通过使用 `Pebblo Safe DocumentLoader` 包装文档加载器调用来实现的。

注意：要在 Pebblo 默认 URL（localhost:8000）以外的 URL 上配置 Pebblo 服务器，请将正确的 URL 放入 `PEBBLO_CLASSIFIER_URL` 环境变量中。这也可以使用 `classifier_url` 关键字参数进行配置。参考：[server-configurations](https://daxa-ai.github.io/pebblo/config)

#### 如何启用 Pebblo 文档加载？

假设一个 LangChain RAG 应用程序片段使用 `CSVLoader` 读取 CSV 文档进行推理。

以下是使用 `CSVLoader` 进行文档加载的代码片段。

```python
from langchain_community.document_loaders import CSVLoader

loader = CSVLoader("data/corp_sens_data.csv")
documents = loader.load()
print(documents)
```

只需对上述代码片段进行几行代码更改，即可启用 Pebblo SafeLoader。

```python
from langchain_community.document_loaders import CSVLoader, PebbloSafeLoader

loader = PebbloSafeLoader(
    CSVLoader("data/corp_sens_data.csv"),
    name="acme-corp-rag-1",  # 应用名称（必填）
    owner="Joe Smith",  # 所有者（可选）
    description="支持生产力 RAG 应用程序",  # 描述（可选）
)
documents = loader.load()
print(documents)
```

### 将语义主题和身份发送到 Pebblo 云服务器

要将语义数据发送到 pebblo-cloud，请将 api-key 作为参数传递给 PebbloSafeLoader，或者将 api-key 放入 `PEBBLO_API_KEY` 环境变量中。

```python
from langchain_community.document_loaders import CSVLoader, PebbloSafeLoader

loader = PebbloSafeLoader(
    CSVLoader("data/corp_sens_data.csv"),
    name="acme-corp-rag-1",  # 应用名称（必填）
    owner="Joe Smith",  # 所有者（可选）
    description="支持生产力 RAG 应用程序",  # 描述（可选）
    api_key="my-api-key",  # API 密钥（可选，可在环境变量 PEBBLO_API_KEY 中设置）
)
documents = loader.load()
print(documents)
```

### 将语义主题和身份添加到已加载的元数据中

要将语义主题和语义实体添加到已加载文档的元数据中，请将 load_semantic 参数设置为 True，或者定义一个新的环境变量 `PEBBLO_LOAD_SEMANTIC`，并将其设置为 True。

```python
from langchain_community.document_loaders import CSVLoader, PebbloSafeLoader

loader = PebbloSafeLoader(
    CSVLoader("data/corp_sens_data.csv"),
    name="acme-corp-rag-1",  # 应用名称（必填）
    owner="Joe Smith",  # 所有者（可选）
    description="支持生产力 RAG 应用程序",  # 描述（可选）
    api_key="my-api-key",  # API 密钥（可选，可在环境变量 PEBBLO_API_KEY 中设置）
    load_semantic=True,  # 加载语义数据（可选，默认为 False，可在环境变量 PEBBLO_LOAD_SEMANTIC 中设置）
)
documents = loader.load()
print(documents[0].metadata)
```

### 匿名化代码片段以屏蔽所有 PII 详细信息

将 `anonymize_snippets` 设置为 `True`，以匿名化进入 VectorDB 和生成报告的所有代码片段中的个人可识别信息 (PII)。

> 注意：_Pebblo 实体分类器_ 能有效识别个人可识别信息 (PII)，并且在不断进化。虽然其召回率尚未达到 100%，但正在稳步提高。
> 有关更多详细信息，请参阅 [_Pebblo 实体分类器文档_](https://daxa-ai.github.io/pebblo/entityclassifier/)

```python
from langchain_community.document_loaders import CSVLoader, PebbloSafeLoader

loader = PebbloSafeLoader(
    CSVLoader("data/corp_sens_data.csv"),
    name="acme-corp-rag-1",  # 应用名称（必填）
    owner="Joe Smith",  # 所有者（可选）
    description="支持生产力 RAG 应用程序",  # 描述（可选）
    anonymize_snippets=True,  # 是否在 PDF 报告中匿名化实体（可选，默认=False）
)
documents = loader.load()
print(documents[0].metadata)
```

```python

```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/document_loaders/pebblo.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
