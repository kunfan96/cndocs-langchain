---
title: "UpstageEmbeddings 集成"
description: "使用 LangChain Python 与 UpstageEmbeddings 嵌入模型进行集成。"
---

本笔记本介绍如何开始使用 Upstage 嵌入模型。

## 安装

安装 `langchain-upstage` 包。

```bash
pip install -U langchain-upstage
```

## 环境配置

请确保设置以下环境变量：

- `UPSTAGE_API_KEY`：来自 [Upstage 控制台](https://console.upstage.ai/) 的 Upstage API 密钥。

```python
import os

os.environ["UPSTAGE_API_KEY"] = "YOUR_API_KEY"
```

## 用法

初始化 `UpstageEmbeddings` 类。

```python
from langchain_upstage import UpstageEmbeddings

embeddings = UpstageEmbeddings(model="solar-embedding-1-large")
```

使用 `embed_documents` 对文本或文档列表进行嵌入。

```python
doc_result = embeddings.embed_documents(
    ["Sung is a professor.", "This is another document"]
)
print(doc_result)
```

使用 `embed_query` 对查询字符串进行嵌入。

```python
query_result = embeddings.embed_query("What does Sung do?")
print(query_result)
```

使用 `aembed_documents` 和 `aembed_query` 执行异步操作。

```python
# 异步嵌入查询
await embeddings.aembed_query("My query to look up")
```

```python
# 异步嵌入文档
await embeddings.aembed_documents(
    ["This is a content of the document", "This is another document"]
)
```

## 与向量存储配合使用

您可以将 `UpstageEmbeddings` 与向量存储组件配合使用。以下展示了一个简单示例。

```python
from langchain_community.vectorstores import DocArrayInMemorySearch

vectorstore = DocArrayInMemorySearch.from_texts(
    ["harrison worked at kensho", "bears like to eat honey"],
    embedding=UpstageEmbeddings(model="solar-embedding-1-large"),
)
retriever = vectorstore.as_retriever()
docs = retriever.invoke("Where did Harrison work?")
print(docs)
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/upstage.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>