---
title: "Vectorize 集成"
description: "使用 LangChain Python 与 Vectorize 集成。"
---

> [Vectorize](https://vectorize.io/) 帮助你更快、更轻松地构建 AI 应用。
> 它自动提取数据，通过 RAG 评估找到最佳向量化策略，
> 并让你快速为非结构化数据部署实时 RAG 流水线。
> 你的向量搜索索引始终保持最新状态，并与现有向量数据库集成，
> 从而让你完全掌控数据。
> Vectorize 处理繁重的工作，让你专注于构建强大的 AI 解决方案，而无需陷入数据管理的困扰。

# 安装与设置

安装以下 Python 包：

<CodeGroup>
```bash pip
pip install langchain-vectorize
```

```bash uv
uv add langchain-vectorize
```
</CodeGroup>

在[此处](https://platform.vectorize.io/)免费注册 Vectorize 账户
在 [Access Token](https://docs.vectorize.io/rag-pipelines/retrieval-endpoint#access-tokens) 部分生成访问令牌
获取你的组织 ID：从浏览器 URL 中提取 /organization/ 后面的 UUID

设置以下变量：
```python
VECTORIZE_ORG_ID="your-organization-id"
VECTORIZE_API_TOKEN="your-api-token"
```

## 检索器

```python
from langchain_vectorize import VectorizeRetriever

retriever = VectorizeRetriever(
    api_token=VECTORIZE_API_TOKEN,
    organization=VECTORIZE_ORG_ID,
    pipeline_id="...",
)
retriever.invoke("query")
```

在[示例笔记本](/oss/python/integrations/retrievers/vectorize)中了解更多。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/vectorize.mdx)或[提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    通过 MCP [将这些文档连接](/use-these-docs)到 Claude、VSCode 等以获取实时答案。
</Callout>
</div>
