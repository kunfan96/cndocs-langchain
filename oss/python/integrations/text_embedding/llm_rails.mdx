---
title: "LLMRails 集成"
description: "使用 LangChain Python 集成 LLMRails 嵌入模型。"
---

加载 LLMRails 嵌入类。

要使用 LLMRails 嵌入，您需要通过参数传入 API 密钥，或在环境变量中设置 `LLM_RAILS_API_KEY`。
要获取 API 密钥，请先在 [console.llmrails.com/signup](https://console.llmrails.com/signup) 注册账号，然后前往 [console.llmrails.com/api-keys](https://console.llmrails.com/api-keys)，在平台上创建一个密钥后复制使用。

```python
from langchain_community.embeddings import LLMRailsEmbeddings
```

```python
embeddings = LLMRailsEmbeddings(model="embedding-english-v1")  # or embedding-multi-v1
```

```python
text = "This is a test document."
```

要生成嵌入向量，您可以查询单个文本，也可以查询一组文本。

```python
query_result = embeddings.embed_query(text)
query_result[:5]
```

```text
[-0.09996652603149414,
 0.015568195842206478,
 0.17670190334320068,
 0.16521021723747253,
 0.21193109452724457]
```

```python
doc_result = embeddings.embed_documents([text])
doc_result[0][:5]
```

```text
[-0.04242777079343796,
 0.016536075621843338,
 0.10052520781755447,
 0.18272875249385834,
 0.2079043835401535]
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/text_embedding/llm_rails.mdx) 或 [提交 Issue](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    通过 MCP [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等，获取实时解答。
</Callout>
</div>
