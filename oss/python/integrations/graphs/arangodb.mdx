---
title: "ArangoDB 集成"
description: "使用 LangChain Python 与 ArangoDB 图数据库进行集成。"
---

[![在 Colab 中打开](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/arangodb/interactive_tutorials/blob/master/notebooks/LangChain.ipynb)

>[ArangoDB](https://github.com/arangodb/arangodb) 是一个可扩展的图数据库系统，旨在更快地从关联数据中挖掘价值。通过单一查询语言提供原生图、集成搜索引擎和 JSON 支持。`ArangoDB` 可在本地部署或运行于云端。

本笔记本演示了如何使用大语言模型（LLM）为 [ArangoDB](https://github.com/arangodb/arangodb#readme) 数据库提供自然语言接口。

## 环境配置

你可以通过 [ArangoDB Docker 镜像](https://hub.docker.com/_/arangodb) 启动本地的 `ArangoDB` 实例：

```
docker run -p 8529:8529 -e ARANGO_ROOT_PASSWORD= arangodb/arangodb
```

另一种方法是使用 [ArangoDB Cloud Connector 包](https://github.com/arangodb/adb-cloud-connector#readme) 来启动一个临时的云实例：

```shell
pip install -qU  python-arango # ArangoDB Python 驱动
pip install -qU  adb-cloud-connector # ArangoDB 云实例预配器
pip install -qU  langchain-openai
pip install -qU  langchain
```

```python
# 实例化 ArangoDB 数据库
import json

from adb_cloud_connector import get_temp_credentials
from arango import ArangoClient

con = get_temp_credentials()

db = ArangoClient(hosts=con["url"]).db(
    con["dbName"], con["username"], con["password"], verify=True
)

print(json.dumps(con, indent=2))
```

```text
日志：正在请求新凭据...
成功：已获取新凭据
{
  "dbName": "TUT3sp29s3pjf1io0h4cfdsq",
  "username": "TUTo6nkwgzkizej3kysgdyeo8",
  "password": "x",
  "hostname": "tutorials.arangodb.cloud",
  "port": 8529,
  "url": "https://tutorials.arangodb.cloud:8529"
}
```

```python
# 实例化 ArangoDB-LangChain 图
from langchain_community.graphs import ArangoGraph

graph = ArangoGraph(db)
```

## 填充数据库

我们将依赖 `Python Driver` 将我们的 [GameOfThrones](https://github.com/arangodb/example-datasets/tree/master/GameOfThrones) 数据导入到数据库中。

```python
if db.has_graph("GameOfThrones"):
    db.delete_graph("GameOfThrones", drop_collections=True)

db.create_graph(
    "GameOfThrones",
    edge_definitions=[
        {
            "edge_collection": "ChildOf",
            "from_vertex_collections": ["Characters"],
            "to_vertex_collections": ["Characters"],
        },
    ],
)

documents = [
    {
        "_key": "NedStark",
        "name": "Ned",
        "surname": "Stark",
        "alive": True,
        "age": 41,
        "gender": "male",
    },
    {
        "_key": "CatelynStark",
        "name": "Catelyn",
        "surname": "Stark",
        "alive": False,
        "age": 40,
        "gender": "female",
    },
    {
        "_key": "AryaStark",
        "name": "Arya",
        "surname": "Stark",
        "alive": True,
        "age": 11,
        "gender": "female",
    },
    {
        "_key": "BranStark",
        "name": "Bran",
        "surname": "Stark",
        "alive": True,
        "age": 10,
        "gender": "male",
    },
]

edges = [
    {"_to": "Characters/NedStark", "_from": "Characters/AryaStark"},
    {"_to": "Characters/NedStark", "_from": "Characters/BranStark"},
    {"_to": "Characters/CatelynStark", "_from": "Characters/AryaStark"},
    {"_to": "Characters/CatelynStark", "_from": "Characters/BranStark"},
]

db.collection("Characters").import_bulk(documents)
db.collection("ChildOf").import_bulk(edges)
```

```text
{'error': False,
 'created': 4,
 'errors': 0,
 'empty': 0,
 'updated': 0,
 'ignored': 0,
 'details': []}
```

## 获取和设置 ArangoDB 模式

在实例化 `ArangoDBGraph` 对象时会自动生成初始的 `ArangoDB Schema`。如果你有兴趣查看或修改该模式，以下是模式的 getter 和 setter 方法：

```python
# 此处模式应为空，
# 因为 `graph` 是在 ArangoDB 数据摄入之前初始化的（见上文）。

import json

print(json.dumps(graph.schema, indent=4))
```

```json
{
    "Graph Schema": [],
    "Collection Schema": []
}
```

```python
graph.set_schema()
```

```python
# 我们现在可以查看生成的模式

import json

print(json.dumps(graph.schema, indent=4))
```

```json
{
    "Graph Schema": [
        {
            "graph_name": "GameOfThrones",
            "edge_definitions": [
                {
                    "edge_collection": "ChildOf",
                    "from_vertex_collections": [
                        "Characters"
                    ],
                    "to_vertex_collections": [
                        "Characters"
                    ]
                }
            ]
        }
    ],
    "Collection Schema": [
        {
            "collection_name": "ChildOf",
            "collection_type": "edge",
            "edge_properties": [
                {
                    "name": "_key",
                    "type": "str"
                },
                {
                    "name": "_id",
                    "type": "str"
                },
                {
                    "name": "_from",
                    "type": "str"
                },
                {
                    "name": "_to",
                    "type": "str"
                },
                {
                    "name": "_rev",
                    "type": "str"
                }
            ],
            "example_edge": {
                "_key": "266218884025",
                "_id": "ChildOf/266218884025",
                "_from": "Characters/AryaStark",
                "_to": "Characters/NedStark",
                "_rev": "_gVPKGSq---"
            }
        },
        {
            "collection_name": "Characters",
            "collection_type": "document",
            "document_properties": [
                {
                    "name": "_key",
                    "type": "str"
                },
                {
                    "name": "_id",
                    "type": "str"
                },
                {
                    "name": "_rev",
                    "type": "str"
                },
                {
                    "name": "name",
                    "type": "str"
                },
                {
                    "name": "surname",
                    "type": "str"
                },
                {
                    "name": "alive",
                    "type": "bool"
                },
                {
                    "name": "age",
                    "type": "int"
                },
                {
                    "name": "gender",
                    "type": "str"
                }
            ],
            "example_document": {
                "_key": "NedStark",
                "_id": "Characters/NedStark",
                "_rev": "_gVPKGPi---",
                "name": "Ned",
                "surname": "Stark",
                "alive": true,
                "age": 41,
                "gender": "male"
            }
        }
    ]
}
```

## 查询 ArangoDB 数据库

我们现在可以使用 `ArangoDB Graph` QA Chain 来查询我们的数据。

```python
import os

os.environ["OPENAI_API_KEY"] = "your-key-here"
```

```python
from langchain_classic.chains import ArangoGraphQAChain
from langchain_openai import ChatOpenAI

chain = ArangoGraphQAChain.from_llm(
    ChatOpenAI(temperature=0), graph=graph, verbose=True
)
```

```python
chain.run("Is Ned Stark alive?")
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
WITH Characters
FOR character IN Characters
FILTER character.name == "Ned" AND character.surname == "Stark"
RETURN character.alive

AQL 结果:
[True]

> 链执行完毕。
```

```text
'是的，Ned Stark 还活着。'
```

```python
chain.run("How old is Arya Stark?")
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
WITH Characters
FOR character IN Characters
FILTER character.name == "Arya" && character.surname == "Stark"
RETURN character.age

AQL 结果:
[11]

> 链执行完毕。
```

```text
'Arya Stark 今年 11 岁。'
```

```python
chain.run("Are Arya Stark and Ned Stark related?")
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
WITH Characters, ChildOf
FOR v, e, p IN 1..1 OUTBOUND 'Characters/AryaStark' ChildOf
    FILTER p.vertices[-1]._key == 'NedStark'
    RETURN p

AQL 结果:
[{'vertices': [{'_key': 'AryaStark', '_id': 'Characters/AryaStark', '_rev': '_gVPKGPi--B', 'name': 'Arya', 'surname': 'Stark', 'alive': True, 'age': 11, 'gender': 'female'}, {'_key': 'NedStark', '_id': 'Characters/NedStark', '_rev': '_gVPKGPi---', 'name': 'Ned', 'surname': 'Stark', 'alive': True, 'age': 41, 'gender': 'male'}], 'edges': [{'_key': '266218884025', '_id': 'ChildOf/266218884025', '_from': 'Characters/AryaStark', '_to': 'Characters/NedStark', '_rev': '_gVPKGSq---'}], 'weights': [0, 1]}]

> 链执行完毕。
```

```text
'是的，Arya Stark 和 Ned Stark 有亲属关系。根据从数据库中检索到的信息，他们之间存在关系。Arya Stark 是 Ned Stark 的孩子。'
```

```python
chain.run("Does Arya Stark have a dead parent?")
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
WITH Characters, ChildOf
FOR v, e IN 1..1 OUTBOUND 'Characters/AryaStark' ChildOf
FILTER v.alive == false
RETURN e

AQL 结果:
[{'_key': '266218884027', '_id': 'ChildOf/266218884027', '_from': 'Characters/AryaStark', '_to': 'Characters/CatelynStark', '_rev': '_gVPKGSu---'}]

> 链执行完毕。
```

```text
'是的，Arya Stark 有一位已故的父母。这位父母是 Catelyn Stark。'
```

## 链修饰符

你可以更改以下 `ArangoDBGraphQAChain` 类变量的值，以修改链输出的行为。

```python
# 指定要返回的最大 AQL 查询结果数量
chain.top_k = 10

# 指定是否在输出字典中返回 AQL 查询语句
chain.return_aql_query = True

# 指定是否在输出字典中返回 AQL JSON 结果
chain.return_aql_result = True

# 指定应尝试的最大 AQL 生成次数
chain.max_aql_generation_attempts = 5

# 指定一组 AQL 查询示例，这些示例将被传递到
# AQL 生成提示模板中以促进少样本学习。
# 默认为空字符串。
chain.aql_examples = """
# Ned Stark 还活着吗？
RETURN DOCUMENT('Characters/NedStark').alive

# Arya Stark 是 Ned Stark 的孩子吗？
FOR e IN ChildOf
    FILTER e._from == "Characters/AryaStark" AND e._to == "Characters/NedStark"
    RETURN e
"""
```

```python
chain.run("Is Ned Stark alive?")

# chain("Is Ned Stark alive?") # 返回包含 AQL 查询和 AQL 结果的字典
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
RETURN DOCUMENT('Characters/NedStark').alive

AQL 结果:
[True]

> 链执行完毕。
```

```text
'是的，根据数据库中的信息，Ned Stark 还活着。'
```

```python
chain.run("Is Bran Stark the child of Ned Stark?")
```

```text
> 进入新的 ArangoGraphQAChain 链...
AQL 查询 (1):
FOR e IN ChildOf
    FILTER e._from == "Characters/BranStark" AND e._to == "Characters/NedStark"
    RETURN e

AQL 结果:
[{'_key': '266218884026', '_id': 'ChildOf/266218884026', '_from': 'Characters/BranStark', '_to': 'Characters/NedStark', '_rev': '_gVPKGSq--_'}]

> 链执行完毕。
```

```text
'是的，根据 ArangoDB 数据库中的信息，Bran Stark 确实是 Ned Stark 的孩子。'
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/graphs/arangodb.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
