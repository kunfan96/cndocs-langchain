---
title: "ChatAbso 集成"
description: "使用 LangChain Python 集成 ChatAbso 聊天模型。"
---

这将帮助您开始使用 `ChatAbso` [聊天模型](/oss/python/langchain/models)。

您还可以查阅完整的 [Abso 路由器文档](https://abso.ai)。

## 概述

### 集成详情

| 类 | 包 | 可序列化 | [JS 支持](https://js.langchain.com/docs/integrations/chat/abso) | 下载量 | 版本 |
| :--- | :--- | :---: |  :---: | :---: | :---: |
| `ChatAbso` | `langchain-abso` | ❌ | ❌ | ![PyPI - 下载量](https://img.shields.io/pypi/dm/langchain-abso?style=flat-square&label=%20) | ![PyPI - 版本](https://img.shields.io/pypi/v/langchain-abso?style=flat-square&label=%20) |

## 设置

要访问 ChatAbso 模型，您需要创建一个 OpenAI 账户，获取 API 密钥，并安装 `langchain-abso` 集成包。

### 凭证

- TODO: 更新相关信息。

前往 (TODO: 链接) 注册 ChatAbso 并生成 API 密钥。完成此操作后，设置 ABSO_API_KEY 环境变量：

```python
import getpass
import os

if not os.getenv("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter your OpenAI API key: ")
```

### 安装

LangChain ChatAbso 集成位于 `langchain-abso` 包中：

```python
pip install -qU langchain-abso
```

## 实例化

现在我们可以实例化模型对象并生成聊天补全：

```python
from langchain_abso import ChatAbso

llm = ChatAbso(fast_model="gpt-4.1", slow_model="o3-mini")
```

## 调用

```python
messages = [
    (
        "system",
        "You are a helpful assistant that translates English to French. Translate the user sentence.",
    ),
    ("human", "I love programming."),
]
ai_msg = llm.invoke(messages)
ai_msg
```

```python
print(ai_msg.content)
```

---

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/chat/abso.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>
