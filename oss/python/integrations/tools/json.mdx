---
title: "JSON 工具包集成"
description: "使用 LangChain Python 集成 JSON 工具包。"
---

本笔记本展示了一个代理与大型 `JSON/dict` 对象交互的过程。
当您想要回答关于一个太大而无法放入 LLM 上下文窗口的 JSON 数据块的问题时，这非常有用。该代理能够迭代地探索数据块，以找到回答用户问题所需的信息。

在下面的示例中，我们使用 OpenAI API 的 OpenAPI 规范，您可以在 [OpenAI OpenAPI 规范仓库](https://github.com/openai/openai-openapi/blob/master/openapi.yaml) 中找到它。

我们将使用 JSON 代理来回答一些关于 API 规范的问题。

```python
pip install -qU langchain-community
```

## 初始化

```python
import yaml
from langchain_community.agent_toolkits import JsonToolkit, create_json_agent
from langchain_community.tools.json.tool import JsonSpec
from langchain_openai import OpenAI
```

```python
with open("openai_openapi.yml") as f:
    data = yaml.load(f, Loader=yaml.FullLoader)
json_spec = JsonSpec(dict_=data, max_value_length=4000)
json_toolkit = JsonToolkit(spec=json_spec)

json_agent_executor = create_json_agent(
    llm=OpenAI(temperature=0), toolkit=json_toolkit, verbose=True
)
```

## 单个工具

让我们看看 Jira 工具包中有哪些单个工具。

```python
[(el.name, el.description) for el in json_toolkit.get_tools()]
```

```text
[('json_spec_list_keys',
  '\n    可用于列出给定路径下的所有键。\n    调用此工具前，您应确保该路径存在。\n    输入是字典路径的文本表示，使用 Python 语法（例如 data["key1"][0]["key2"]）。\n    '),
 ('json_spec_get_value',
  '\n    可用于查看给定路径下的字符串格式值。\n    调用此工具前，您应确保该路径存在。\n    输入是字典路径的文本表示，使用 Python 语法（例如 data["key1"][0]["key2"]）。\n    ')]
```

## 示例：获取请求所需的 POST 参数

```python
json_agent_executor.run(
    "What are the required parameters in the request body to the /completions endpoint?"
)
```

```text
> 进入新的 AgentExecutor 链...
操作: json_spec_list_keys
操作输入: data
观察结果: ['openapi', 'info', 'servers', 'tags', 'paths', 'components', 'x-oaiMeta']
思考: 我应该查看 paths 键以了解存在哪些端点
操作: json_spec_list_keys
操作输入: data["paths"]
观察结果: ['/engines', '/engines/{engine_id}', '/completions', '/edits', '/images/generations', '/images/edits', '/images/variations', '/embeddings', '/engines/{engine_id}/search', '/files', '/files/{file_id}', '/files/{file_id}/content', '/answers', '/classifications', '/fine-tunes', '/fine-tunes/{fine_tune_id}', '/fine-tunes/{fine_tune_id}/cancel', '/fine-tunes/{fine_tune_id}/events', '/models', '/models/{model}', '/moderations']
思考: 我应该查看 /completions 端点以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]
观察结果: ['post']
思考: 我应该查看 post 键以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]["post"]
观察结果: ['operationId', 'tags', 'summary', 'requestBody', 'responses', 'x-oaiMeta']
思考: 我应该查看 requestBody 键以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]["post"]["requestBody"]
观察结果: ['required', 'content']
思考: 我应该查看 required 键以了解需要哪些参数
操作: json_spec_get_value
操作输入: data["paths"]["/completions"]["post"]["requestBody"]["required"]
观察结果: True
思考: 我应该查看 content 键以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]["post"]["requestBody"]["content"]
观察结果: ['application/json']
思考: 我应该查看 application/json 键以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]["post"]["requestBody"]["content"]["application/json"]
观察结果: ['schema']
思考: 我应该查看 schema 键以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["paths"]["/completions"]["post"]["requestBody"]["content"]["application/json"]["schema"]
观察结果: ['$ref']
思考: 我应该查看 $ref 键以了解需要哪些参数
操作: json_spec_get_value
操作输入: data["paths"]["/completions"]["post"]["requestBody"]["content"]["application/json"]["schema"]["$ref"]
观察结果: #/components/schemas/CreateCompletionRequest
思考: 我应该查看 CreateCompletionRequest 模式以了解需要哪些参数
操作: json_spec_list_keys
操作输入: data["components"]["schemas"]["CreateCompletionRequest"]
观察结果: ['type', 'properties', 'required']
思考: 我应该查看 required 键以了解需要哪些参数
操作: json_spec_get_value
操作输入: data["components"]["schemas"]["CreateCompletionRequest"]["required"]
观察结果: ['model']
思考: 我现在知道最终答案了
最终答案: /completions 端点请求体中的必需参数是 'model'。

> 链结束。
```

```text
"/completions 端点请求体中的必需参数是 'model'。"
```

```python

```

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/tools/json.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>