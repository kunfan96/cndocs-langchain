---
title: "Grobid 集成"
description: "使用 LangChain Python 与 Grobid 进行集成。"
---

GROBID 是一个用于提取、解析和重构原始文档的机器学习库。

它专为解析学术论文而设计，在此类场景下表现尤为出色。

*注意*：如果提供给 Grobid 的文章是大型文档（例如学位论文）且元素数量超过一定阈值，可能无法进行处理。

本页介绍如何在 LangChain 中使用 Grobid 解析文章。

## 安装
Grobid 的安装详情见 https://grobid.readthedocs.io/en/latest/Install-Grobid/。
不过，通过 Docker 容器运行 Grobid 可能更简单、更少麻烦，具体操作请参阅 [Grobid Docker 指南](https://grobid.readthedocs.io/en/latest/Grobid-docker/)。

## 在 LangChain 中使用 Grobid

安装并启动 Grobid 后（可通过访问 http://localhost:8070 进行检查），即可开始使用。

现在你可以使用 `GrobidParser` 来生成文档：
```python
from langchain_community.document_loaders.parsers import GrobidParser
from langchain_community.document_loaders.generic import GenericLoader

# 从文章段落生成块
loader = GenericLoader.from_filesystem(
    "/Users/31treehaus/Desktop/Papers/",
    glob="*",
    suffixes=[".pdf"],
    parser= GrobidParser(segment_sentences=False)
)
docs = loader.load()

# 从文章句子生成块
loader = GenericLoader.from_filesystem(
    "/Users/31treehaus/Desktop/Papers/",
    glob="*",
    suffixes=[".pdf"],
    parser= GrobidParser(segment_sentences=True)
)
docs = loader.load()
```
块的元数据将包含边界框（Bounding Boxes）。尽管解析这些数据稍显复杂，但详细说明可参考 https://grobid.readthedocs.io/en/latest/Coordinates-in-PDF/

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/grobid.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>