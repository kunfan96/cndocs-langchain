---
title: "Premai 集成"
description: "使用 LangChain Python 与 Premai 嵌入模型进行集成。"
---

[PremAI](https://premai.io/) 是一个一体化平台，简化了由生成式 AI 驱动的健壮、生产就绪型应用的创建过程。通过简化开发流程，PremAI 使您能够专注于提升用户体验并推动应用的整体增长。您可以快速 [开始使用 PremAI](https://docs.premai.io/quick-start)。

### 安装与设置

我们首先安装 `langchain` 和 `premai-sdk`。您可以输入以下命令进行安装：

```bash
pip install premai langchain
```

在继续操作之前，请确保您已在 PremAI 上注册了账户并创建了项目。如果没有，请参阅 [快速入门](https://docs.premai.io/introduction) 指南以开始使用 PremAI 平台。创建您的第一个项目并获取您的 API 密钥。

## PremEmbeddings

在本节中，我们将讨论如何使用 LangChain 中的 `PremEmbeddings` 访问不同的嵌入模型。让我们从导入模块和设置 API 密钥开始。

```python
# 让我们先进行一些导入并定义我们的嵌入对象

from langchain_community.embeddings import PremAIEmbeddings
```

导入所需模块后，让我们设置客户端。目前假设我们的 `project_id` 为 `8`。但请务必使用您自己的 project-id，否则会抛出错误。

> 注意：与 [ChatPremAI.](https://python.langchain.com/v0.1/docs/integrations/chat/premai/) 不同，为 PremAIEmbeddings 设置 `model_name` 参数是必需的。

```python
import getpass
import os

if os.environ.get("PREMAI_API_KEY") is None:
    os.environ["PREMAI_API_KEY"] = getpass.getpass("PremAI API Key:")
```

```python
model = "text-embedding-3-large"
embedder = PremAIEmbeddings(project_id=8, model=model)
```

我们支持大量最先进的嵌入模型。您可以查看我们 [支持的 LLM 和嵌入模型列表](https://docs.premai.io/get-started/supported-models)。在本示例中，我们先使用 `text-embedding-3-large` 模型。

```python
query = "Hello, this is a test query"
query_result = embedder.embed_query(query)

# 让我们打印查询嵌入向量的前五个元素

print(query_result[:5])
```

```text
[-0.02129288576543331, 0.0008162345038726926, -0.004556538071483374, 0.02918623760342598, -0.02547479420900345]
```

最后，让我们对文档进行嵌入处理

```python
documents = ["This is document1", "This is document2", "This is document3"]

doc_result = embedder.embed_documents(documents)

# 与之前的结果类似，让我们打印第一个文档向量的前五个元素

print(doc_result[0][:5])
```

```text
[-0.0030691148713231087, -0.045334383845329285, -0.0161729846149683, 0.04348714277148247, -0.0036920777056366205]
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/premai.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 到 Claude、VSCode 及其他工具，以便通过 MCP 获取实时答案。
</Callout>
</div>