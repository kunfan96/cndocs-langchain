---
title: "GPT4All 集成"
description: "使用 LangChain Python 与 GPT4All 集成。"
---

本页介绍如何在 LangChain 中使用 `GPT4All` 包装器。教程分为两部分：安装与设置，以及使用示例。

## 安装与设置

- 使用 `pip install gpt4all` 安装 Python 包
- 下载一个 [GPT4All 模型](https://gpt4all.io/index.html) 并将其放置在您所需的目录中

在此示例中，我们使用 `mistral-7b-openorca.Q4_0.gguf`：

```bash
mkdir models
wget https://gpt4all.io/models/gguf/mistral-7b-openorca.Q4_0.gguf -O models/mistral-7b-openorca.Q4_0.gguf
```

## 使用方法

### GPT4All

要使用 GPT4All 包装器，您需要提供预训练模型文件的路径以及模型的配置。

```python
from langchain_community.llms import GPT4All

# 实例化模型。回调支持令牌级流式传输
model = GPT4All(model="./models/mistral-7b-openorca.Q4_0.gguf", n_threads=8)

# 生成文本
response = model.invoke("Once upon a time, ")
```

您还可以自定义生成参数，例如 `n_predict`、`temp`、`top_p`、`top_k` 等。

要流式传输模型的预测结果，请添加 CallbackManager。

```python
from langchain_community.llms import GPT4All
from langchain.callbacks.streaming_stdout import StreamingStdOutCallbackHandler

# 支持多种 CallbackHandler，例如
# from langchain.callbacks.streamlit import StreamlitCallbackHandler

callbacks = [StreamingStdOutCallbackHandler()]
model = GPT4All(model="./models/mistral-7b-openorca.Q4_0.gguf", n_threads=8)

# 生成文本。令牌通过回调管理器流式传输。
model.invoke("Once upon a time, ", callbacks=callbacks)
```

## 模型文件

您可以从 GPT4All 客户端下载模型文件。您可以从 [GPT4All](https://gpt4all.io/index.html) 网站下载客户端。

有关更详细的演练，请参阅[此笔记本](/oss/python/integrations/llms/gpt4all)

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/gpt4all.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>