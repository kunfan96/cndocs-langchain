---
title: 排查变量缓存问题
sidebarTitle: 排查变量缓存问题
---

如果您在追踪项目中看不到追踪记录，或发现追踪记录被记录到错误的项目/工作区，问题可能源于 LangSmith 默认的环境变量缓存机制。这种情况在 Jupyter notebook 中运行 LangSmith 时尤为常见。请按照以下步骤诊断并解决问题：

## 1. 验证环境变量

首先，通过运行以下命令检查环境变量是否设置正确：

```python
import os
print(os.getenv("LANGSMITH_PROJECT"))
print(os.getenv("LANGSMITH_TRACING"))
print(os.getenv("LANGSMITH_ENDPOINT"))
print(os.getenv("LANGSMITH_API_KEY"))
```

如果输出结果与您在 .env 文件中定义的值不匹配，则很可能是环境变量缓存导致的问题。

## 2. 清除缓存

使用以下命令清除已缓存的环境变量：

```python
utils.get_env_var.cache_clear()
```

## 3. 重新加载环境变量

通过执行以下命令从 .env 文件重新加载环境变量：

```python
from dotenv import load_dotenv
import os
load_dotenv(<.env 文件路径>, override=True)
```

重新加载后，您的环境变量应已正确设置。

如果问题仍然存在，请通过共享的 Slack 频道或电子邮件支持（适用于 Plus 和 Enterprise 计划）联系我们，或在 [LangChain 论坛](https://forum.langchain.com/) 中寻求帮助。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs) 通过 MCP 连接到 Claude、VSCode 等工具，以获取实时解答。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/langsmith/troubleshooting-variable-caching.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>