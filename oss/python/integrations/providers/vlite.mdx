---
title: "VLite 集成"
description: "使用 LangChain Python 与 VLite 集成。"
---

本页介绍如何在 LangChain 中使用 [vlite](https://github.com/sdan/vlite)。vlite 是一个简单快速的向量数据库，用于存储和检索嵌入向量。

## 安装与设置

要安装 vlite，请运行以下命令：

<CodeGroup>
```bash pip
pip install vlite
```

```bash uv
uv add vlite
```
</CodeGroup>

如需 PDF OCR 支持，请安装 `vlite[ocr]` 扩展：

<CodeGroup>
```bash pip
pip install "vlite[ocr]"
```

```bash uv
uv add "vlite[ocr]"
```
</CodeGroup>

## VectorStore

vlite 提供了对其向量数据库的封装，允许您将其用作向量存储，进行语义搜索和示例选择。

要导入 vlite 向量存储：

```python
from langchain_community.vectorstores import vlite
```

### 用法

有关 vlite 封装器的更详细演练，请参阅[此笔记本](/oss/python/integrations/vectorstores/vlite)。

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/vlite.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>