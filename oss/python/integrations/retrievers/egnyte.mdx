---
title: EgnyteRetriever
---

这将帮助您开始使用 Egnyte [检索器](/oss/python/langchain/retrieval)。有关所有 `EgnyteRetriever` 功能和配置的详细文档，请前往 [API 参考](https://github.com/egnyte/egnyte-langchain-connector)。

# 概述

`EgnyteRetriever` 类帮助您使用结合了语义搜索和关键词搜索的混合搜索功能，从 Egnyte 搜索和检索文档。此检索器完全符合 LangChain 标准，并支持同步和异步操作。

### 集成详情

自带数据（即索引和搜索自定义文档语料库）：

| 检索器 | 自托管 | 云服务 | 包 |
| :--- | :--- | :---: | :---: |
[EgnyteRetriever](https://github.com/egnyte/egnyte-langchain-connector) | ❌ | ✅ | egnyte-langchain-connector |

## 设置

要使用 Egnyte 包，您需要：

* 一个 Egnyte 账户 — 如果您不是当前的 Egnyte 客户或想在生产环境之外进行测试，您可以使用 [免费开发者账户](https://developers.egnyte.com/member/register)。
* 一个 Egnyte 应用 — 这在 [开发者控制台](https://developers.egnyte.com/) 中配置，并且必须启用适当的权限范围。
* 该应用必须由管理员启用。对于免费开发者账户，启用者即为注册账户的用户。

### 凭证

在这些示例中，我们将使用带有 Egnyte 用户令牌的 Bearer 令牌认证。要生成用户令牌：

1. 在 [https://developers.egnyte.com/member/register](https://developers.egnyte.com/member/register) 注册一个开发者账户
2. 按照 [公共 API 认证指南](https://developers.egnyte.com/docs/read/Public_API_Authentication) 生成用户令牌
3. **重要**：生成令牌时使用 `Egnyte.ai` 权限范围

```python
import getpass
import os

egnyte_user_token = getpass.getpass("Enter your Egnyte User Token: ")
domain = input("Enter your Egnyte domain (e.g., company.egnyte.com): ")
```

如果您想从单个查询中获取自动跟踪，还可以通过取消注释下方代码来设置您的 [LangSmith](https://docs.smith.langchain.com/) API 密钥：

```python
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

此检索器位于 `egnyte-langchain-connector` 包中：

```bash
pip install -qU egnyte-langchain-connector
```

```output
Note: you may need to restart the kernel to use updated packages.
```

## 实例化

现在我们可以实例化我们的检索器：

```python
from langchain_egnyte import EgnyteRetriever

retriever = EgnyteRetriever(domain=domain, k=100)
```

## 用法

### 基本搜索

```python
query = "machine learning policies"

documents = retriever.invoke(query, egnyte_user_token=egnyte_user_token)

for doc in documents:
    print(f"Title: {doc.metadata.get('title', 'N/A')}")
    print(f"Content: {doc.page_content[:200]}...")
    print("---")
```

### 带选项的高级搜索

为了进行更精细的搜索，您可以使用 `EgnyteSearchOptions` 按文件夹路径、日期范围等过滤结果：

```python
from langchain_egnyte import EgnyteRetriever, EgnyteSearchOptions

search_options = EgnyteSearchOptions(
    limit=50,
    folderPath="/policies",
    excludeFolderPaths=["/temp", "/archive"],
    createdAfter=1640995200000,  # Unix 时间戳（毫秒）(2022年1月1日)
    createdBefore=1672531200000  # Unix 时间戳（毫秒）(2023年1月1日)
)

retriever = EgnyteRetriever(
    domain=domain,
    k=50,
    search_options=search_options
)

documents = retriever.invoke(
    "compliance requirements",
    egnyte_user_token=egnyte_user_token
)
```

### 异步用法

检索器支持异步操作：

```python
import asyncio

async def search_async():
    documents = await retriever.ainvoke(
        "data privacy guidelines",
        egnyte_user_token=egnyte_user_token
    )
    return documents

# 运行异步搜索
documents = asyncio.run(search_async())
```

### 批量操作

您可以批量处理多个查询：

```python
queries = [
    "security policies",
    "employee handbook",
    "compliance guidelines"
]

# 同步批量
results = retriever.batch(
    queries,
    config={"configurable": {"egnyte_user_token": egnyte_user_token}}
)

# 异步批量
results = await retriever.abatch(
    queries,
    config={"configurable": {"egnyte_user_token": egnyte_user_token}}
)
```

## 用作代理工具

与其他检索器一样，EgnyteRetriever 可以作为工具添加到 LangGraph 代理中。

```python
from langchain import hub
from langchain.agents import AgentExecutor, create_openai_tools_agent
from langchain.tools.retriever import create_retriever_tool
from langchain_openai import ChatOpenAI

retriever = EgnyteRetriever(domain=domain, k=50)

egnyte_search_tool = create_retriever_tool(
    retriever,
    "egnyte_search_tool",
    "此工具搜索 Egnyte 并使用混合搜索检索符合搜索条件的文档"
)

tools = [egnyte_search_tool]

prompt = hub.pull("hwchase17/openai-tools-agent")
llm = ChatOpenAI(temperature=0)

agent = create_openai_tools_agent(llm, tools, prompt)
agent_executor = AgentExecutor(agent=agent, tools=tools)

result = agent_executor.invoke({
    "input": "查找所有与数据隐私政策相关的文档"
})

print(result['output'])
```

---

## API 参考

有关所有 `EgnyteRetriever` 功能和配置的详细文档，请访问 [GitHub 仓库](https://github.com/egnyte/egnyte-langchain-connector)。

## 帮助

如果您有疑问，请查看 [Egnyte 开发者文档](https://developers.egnyte.com/) 或联系 Egnyte 开发者社区。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/retrievers/egnyte.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>