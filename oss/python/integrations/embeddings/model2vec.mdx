---
title: "Model2vec 集成"
description: "使用 LangChain Python 与 Model2vec 嵌入模型进行集成。"
---

Model2Vec 是一种将任意句子转换器转换为极小静态模型的技术。
[model2vec](https://github.com/MinishLab/model2vec) 可用于生成嵌入向量。

## 安装设置

```bash
pip install -U langchain-community
```

## 实例化

确保已安装 `model2vec`

```bash
pip install -U model2vec
```

## 索引与检索

```python
from langchain_community.embeddings import Model2vecEmbeddings
```

```python
embeddings = Model2vecEmbeddings("minishlab/potion-base-8M")
```

```python
query_text = "This is a test query."
query_result = embeddings.embed_query(query_text)
```

```python
document_text = "This is a test document."
document_result = embeddings.embed_documents([document_text])
```

## 直接调用

以下是直接使用 `model2vec` 的方法：

```python
from model2vec import StaticModel

# 从 HuggingFace Hub 加载模型（此处为 potion-base-8M 模型）
model = StaticModel.from_pretrained("minishlab/potion-base-8M")

# 生成嵌入向量
embeddings = model.encode(["It's dangerous to go alone!", "It's a secret to everybody."])

# 生成词元嵌入序列
token_embeddings = model.encode_as_sequence(["It's dangerous to go alone!", "It's a secret to everybody."])
```

---

## API 参考

如需更多信息，请参阅 model2vec 的 GitHub [仓库](https://github.com/MinishLab/model2vec)

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/model2vec.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [连接这些文档](/use-these-docs) 至 Claude、VSCode 等，通过 MCP 获取实时答案。
</Callout>
</div>