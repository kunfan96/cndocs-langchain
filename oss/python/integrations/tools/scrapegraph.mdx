---
title: "ScrapeGraph 集成"
description: "使用 LangChain Python 与 ScrapeGraph 工具集成。"
---

本指南提供了开始使用 ScrapeGraph [工具](/oss/python/integrations/tools/) 的快速概览。有关所有 ScrapeGraph 功能和配置的详细文档，请前往 [API 参考](https://python.langchain.com/docs/integrations/tools/scrapegraph)。

有关 ScrapeGraph AI 的更多信息：

- [ScrapeGraph AI 网站](https://scrapegraphai.com)
- [开源项目](https://github.com/ScrapeGraphAI/ScrapeGraph-ai)

## 概述

### 集成详情

| 类 | 包 | 可序列化 | JS 支持 | 版本 |
| :--- | :--- | :---: | :---: | :---: |
| [`SmartScraperTool`](https://python.langchain.com/docs/integrations/tools/scrapegraph) | langchain-scrapegraph | ✅ | ❌ | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-scrapegraph?style=flat-square&label=%20) |
| [`SmartCrawlerTool`](https://python.langchain.com/docs/integrations/tools/scrapegraph) | langchain-scrapegraph | ✅ | ❌ | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-scrapegraph?style=flat-square&label=%20) |
| [`MarkdownifyTool`](https://python.langchain.com/docs/integrations/tools/scrapegraph) | langchain-scrapegraph | ✅ | ❌ | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-scrapegraph?style=flat-square&label=%20) |
| [`AgenticScraperTool`](https://python.langchain.com/docs/integrations/tools/scrapegraph) | langchain-scrapegraph | ✅ | ❌ | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-scrapegraph?style=flat-square&label=%20) |
| [`GetCreditsTool`](https://python.langchain.com/docs/integrations/tools/scrapegraph) | langchain-scrapegraph | ✅ | ❌ | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-scrapegraph?style=flat-square&label=%20) |

### 工具功能

| 工具 | 用途 | 输入 | 输出 |
| :--- | :--- | :--- | :--- |
| `SmartScraperTool` | 从网站提取结构化数据 | URL + 提示 | JSON |
| `SmartCrawlerTool` | 通过爬网从多个页面提取数据 | URL + 提示 + 爬网选项 | JSON |
| `MarkdownifyTool` | 将网页转换为 Markdown | URL | Markdown 文本 |
| `GetCreditsTool` | 检查 API 额度 | 无 | 额度信息 |

## 设置

集成需要以下包：

```python
pip install --quiet -U langchain-scrapegraph
```

### 凭据

您需要 ScrapeGraph AI API 密钥才能使用这些工具。在 [scrapegraphai.com](https://scrapegraphai.com) 获取一个。

```python
import getpass
import os

if not os.environ.get("SGAI_API_KEY"):
    os.environ["SGAI_API_KEY"] = getpass.getpass("ScrapeGraph AI API key:\n")
```

设置 [LangSmith](https://smith.langchain.com/) 也很有帮助（但不是必需的），以获得最佳的可观察性：

```python
os.environ["LANGSMITH_TRACING"] = "true"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass()
```

## 实例化

这里我们展示如何实例化 ScrapeGraph 工具：

```python
from scrapegraph_py.logger import sgai_logger
import json

from langchain_scrapegraph.tools import (
    GetCreditsTool,
    MarkdownifyTool,
    SmartCrawlerTool,
    SmartScraperTool,
)

sgai_logger.set_logging(level="INFO")

smartscraper = SmartScraperTool()
smartcrawler = SmartCrawlerTool()
markdownify = MarkdownifyTool()
credits = GetCreditsTool()
```

## 调用

### [直接使用参数调用](/oss/python/langchain/tools)

让我们单独尝试每个工具：

### SmartCrawler 工具

SmartCrawlerTool 允许您从网站爬取多个页面，并使用高级爬网选项（如深度控制、页面限制和域限制）提取结构化数据。

```python
# SmartScraper
result = smartscraper.invoke(
    {
        "user_prompt": "提取公司名称和描述",
        "website_url": "https://scrapegraphai.com",
    }
)
print("SmartScraper Result:", result)

# Markdownify
markdown = markdownify.invoke({"website_url": "https://scrapegraphai.com"})
print("\nMarkdownify Result (first 200 chars):", markdown[:200])

# SmartCrawler
url = "https://scrapegraphai.com/"
prompt = (
    "公司做什么？我需要从他们的隐私和条款中获取文本内容"
)

# 使用带爬网参数的工具
result_crawler = smartcrawler.invoke(
    {
        "url": url,
        "prompt": prompt,
        "cache_website": True,
        "depth": 2,
        "max_pages": 2,
        "same_domain_only": True,
    }
)

print("\nSmartCrawler Result:")
print(json.dumps(result_crawler, indent=2))

# 检查额度
credits_info = credits.invoke({})
print("\nCredits Info:", credits_info)
```

```text
SmartScraper Result: {'company_name': 'ScrapeGraphAI', 'description': "ScrapeGraphAI 是一个强大的 AI 网络抓取工具，可通过简单的 API 将整个网站转换为干净、结构化的数据。它旨在帮助开发人员和 AI 公司高效地从网站中提取有价值的数据，并将其转换为可在 LLM 应用程序和数据分析中使用的格式。"}

Markdownify Result (first 200 chars): [![ScrapeGraphAI Logo](https://scrapegraphai.com/images/scrapegraphai_logo.svg)ScrapeGraphAI](https://scrapegraphai.com/)

PartnersPricingFAQ[Blog](https://scrapegraphai.com/blog)DocsLog inSign up

Op
LocalScraper Result: {'company_name': 'Company Name', 'description': 'We are a technology company focused on AI solutions.', 'contact': {'email': 'contact@example.com', 'phone': '(555) 123-4567'}}

Credits Info: {'remaining_credits': 49679, 'total_credits_used': 914}
```

```python
# SmartCrawler 示例
from scrapegraph_py.logger import sgai_logger
import json

from langchain_scrapegraph.tools import SmartCrawlerTool

sgai_logger.set_logging(level="INFO")

# 将自动从环境获取 SGAI_API_KEY
tool = SmartCrawlerTool()

# 基于提供的代码片段的示例
url = "https://scrapegraphai.com/"
prompt = (
    "公司做什么？我需要从他们的隐私和条款中获取文本内容"
)

# 使用带爬网参数的工具
result = tool.invoke(
    {
        "url": url,
        "prompt": prompt,
        "cache_website": True,
        "depth": 2,
        "max_pages": 2,
        "same_domain_only": True,
    }
)

print(json.dumps(result, indent=2))
```

### [使用 ToolCall 调用](/oss/python/langchain/tools)

我们也可以使用模型生成的 ToolCall 来调用工具：

```python
model_generated_tool_call = {
    "args": {
        "user_prompt": "提取主标题和描述",
        "website_url": "https://scrapegraphai.com",
    },
    "id": "1",
    "name": smartscraper.name,
    "type": "tool_call",
}
smartscraper.invoke(model_generated_tool_call)
```

```text
ToolMessage(content='{"main_heading": "从任何网站获取您需要的数据", "description": "只需几行代码即可轻松提取和收集信息，使用简单的 API。将网站转换为干净且可用的结构化数据。"}', name='SmartScraper', tool_call_id='1')
```

## 链式调用

让我们将工具与 LLM 结合使用来分析网站：

<ChatModelTabs customVarName="llm" />

```python
# | output: false
# | echo: false

# pip install -qU langchain langchain-openai
from langchain.chat_models import init_chat_model

model = init_chat_model(model="gpt-4.1", model_provider="openai")
```

```python
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnableConfig, chain

prompt = ChatPromptTemplate(
    [
        (
            "system",
            "您是一个有用的助手，可以使用工具从网站提取结构化信息。",
        ),
        ("human", "{user_input}"),
        ("placeholder", "{messages}"),
    ]
)

model_with_tools = model.bind_tools([smartscraper], tool_choice=smartscraper.name)
model_chain = prompt | model_with_tools


@chain
def tool_chain(user_input: str, config: RunnableConfig):
    input_ = {"user_input": user_input}
    ai_msg = model_chain.invoke(input_, config=config)
    tool_msgs = smartscraper.batch(ai_msg.tool_calls, config=config)
    return model_chain.invoke({**input_, "messages": [ai_msg, *tool_msgs]}, config=config)


tool_chain.invoke(
    "ScrapeGraph AI 做什么？从他们的网站 https://scrapegraphai.com 提取此信息"
)
```

```text
AIMessage(content='ScrapeGraph AI 是一个由 AI 驱动的网络抓取工具，可通过简单的 API 高效提取和转换网站数据为结构化格式。它面向开发人员、数据科学家和 AI 研究人员，提供易于集成、支持动态内容以及针对大型项目的可扩展性等功能。它支持各种网站类型，包括商业、电子商务和教育网站。联系方式：contact@scrapegraphai.com。', additional_kwargs={'tool_calls': [{'id': 'call_shkRPyjyAtfjH9ffG5rSy9xj', 'function': {'arguments': '{"user_prompt":"提取有关 ScrapeGraph AI 提供的产品、服务和关键功能的详细信息，以及网站上提到的任何独特卖点或创新。","website_url":"https://scrapegraphai.com"}', 'name': 'SmartScraper'}, 'type': 'function'}], 'refusal': None}, response_metadata={'token_usage': {'completion_tokens': 47, 'prompt_tokens': 480, 'total_tokens': 527, 'completion_tokens_details': {'accepted_prediction_tokens': 0, 'audio_tokens': 0, 'reasoning_tokens': 0, 'rejected_prediction_tokens': 0}, 'prompt_tokens_details': {'audio_tokens': 0, 'cached_tokens': 0}}, 'model_name': 'gpt-4o-2024-08-06', 'system_fingerprint': 'fp_c7ca0ebaca', 'finish_reason': 'stop', 'logprobs': None}, id='run-45a12c86-d499-4273-8c59-0db926799bc7-0', tool_calls=[{'name': 'SmartScraper', 'args': {'user_prompt': '提取有关 ScrapeGraph AI 提供的产品、服务和关键功能的详细信息，以及网站上提到的任何独特卖点或创新。', 'website_url': 'https://scrapegraphai.com'}, 'id': 'call_shkRPyjyAtfjH9ffG5rSy9xj', 'type': 'tool_call'}], usage_metadata={'input_tokens': 480, 'output_tokens': 47, 'total_tokens': 527, 'input_token_details': {'audio': 0, 'cache_read': 0}, 'output_token_details': {'audio': 0, 'reasoning': 0}})
```

---

## API 参考

有关所有 ScrapeGraph 功能和配置的详细文档，请前往 [LangChain API 参考](https://python.langchain.com/docs/integrations/tools/scrapegraph)。

或前往 [官方 SDK 仓库](https://github.com/ScrapeGraphAI/langchain-scrapegraph)。

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/tools/scrapegraph.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [通过 MCP 将这些文档连接到 Claude、VSCode 等](/use-these-docs) 以获取实时答案。
</Callout>
</div>