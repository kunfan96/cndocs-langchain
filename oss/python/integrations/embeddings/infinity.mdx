---
title: "Infinity 集成"
description: "使用 LangChain Python 与 Infinity 嵌入模型进行集成。"
---

`Infinity` 允许使用采用 MIT 许可证的嵌入服务器来创建 [`Embeddings`](https://reference.langchain.com/python/langchain-core/embeddings/embeddings/Embeddings)。

本笔记本将介绍如何在 [Infinity GitHub 项目](https://github.com/michaelfeil/infinity) 中使用 LangChain 配合 Embeddings。

## 导入

```python
from langchain_community.embeddings import InfinityEmbeddings, InfinityEmbeddingsLocal
```

# 选项 1：从 Python 使用 infinity

#### 可选：安装 infinity

要安装 infinity，请使用以下命令。更多详细信息请查看 [GitHub 上的文档](https://github.com/michaelfeil/infinity)。
安装 torch 和 onnx 依赖项。

```bash
pip install infinity_emb[torch,optimum]
```

```python
documents = [
    "Baguette is a dish.",
    "Paris is the capital of France.",
    "numpy is a lib for linear algebra",
    "You escaped what I've escaped - You'd be in Paris getting fucked up too",
]
query = "Where is Paris?"
```

```python
embeddings = InfinityEmbeddingsLocal(
    model="sentence-transformers/all-MiniLM-L6-v2",
    # 版本
    revision=None,
    # 建议保持为 32
    batch_size=32,
    # 通过 torch 用于 AMD/Nvidia GPU
    device="cuda",
    # 在执行前预热模型
)


async def embed():
    # TODO：此函数仅用于展示您的调用可以异步运行。

    # 重要：在 `async with` 语句内使用引擎以启动/停止批处理引擎。
    async with embeddings:
        # 避免频繁关闭和启动引擎。
        # 最好让它持续运行。
        # 您可以调用 `await embeddings.__aenter__()` 和 `__aexit__()`
        # 如果您能更精细地控制手动启动/停止执行的时机

        documents_embedded = await embeddings.aembed_documents(documents)
        query_result = await embeddings.aembed_query(query)
        print("成功创建 embeddings")
    return documents_embedded, query_result
```

```text
/home/michael/langchain/libs/langchain/.venv/lib/python3.10/site-packages/tqdm/auto.py:21: TqdmWarning: 未找到 IProgress。请更新 jupyter 和 ipywidgets。请参阅 https://ipywidgets.readthedocs.io/en/stable/user_install.html
  from .autonotebook import tqdm as notebook_tqdm
BetterTransformer 实现不支持训练期间的填充，因为融合的内核不支持注意力掩码。请注意，在训练期间传递填充后的批次数据可能会导致意外输出。有关更多详细信息，请参阅 https://huggingface.co/docs/optimum/bettertransformer/overview。
/home/michael/langchain/libs/langchain/.venv/lib/python3.10/site-packages/optimum/bettertransformer/models/encoder_models.py:301: UserWarning: 嵌套张量的 PyTorch API 处于原型阶段，将在不久的将来发生变化。（在内部触发于 ../aten/src/ATen/NestedTensorImpl.cpp:177。）
  hidden_states = torch._nested_tensor_from_mask(hidden_states, ~attention_mask)
```

```python
# 按您喜欢的方式运行异步代码
# 如果您在 Jupyter Notebook 中，可以使用以下内容
documents_embedded, query_result = await embed()
```

```python
# （演示）计算相似度
import numpy as np

scores = np.array(documents_embedded) @ np.array(query_result).T
dict(zip(documents, scores))
```

# 选项 2：运行服务器并通过 API 连接

#### 可选：确保启动 infinity 实例

要安装 infinity，请使用以下命令。更多详细信息请查看 [GitHub 上的文档](https://github.com/michaelfeil/infinity)。

```bash
pip install infinity_emb[all]
```

# 安装 infinity 包

pip install -qU  infinity_emb[all]

启动服务器——最好在单独的终端中执行，而不是在 Jupyter Notebook 内部执行

```bash
model=sentence-transformers/all-MiniLM-L6-v2
port=7797
infinity_emb --port $port --model-name-or-path $model
```

或者您也可以直接使用 Docker：

```bash
model=sentence-transformers/all-MiniLM-L6-v2
port=7797
docker run -it --gpus all -p $port:$port michaelf34/infinity:latest --model-name-or-path $model --port $port
```

## 使用您的 infinity 实例嵌入文档

```python
documents = [
    "Baguette is a dish.",
    "Paris is the capital of France.",
    "numpy is a lib for linear algebra",
    "You escaped what I've escaped - You'd be in Paris getting fucked up too",
]
query = "Where is Paris?"
```

```python
#
infinity_api_url = "http://localhost:7797/v1"
# 模型目前未经过验证。
embeddings = InfinityEmbeddings(
    model="sentence-transformers/all-MiniLM-L6-v2", infinity_api_url=infinity_api_url
)
try:
    documents_embedded = embeddings.embed_documents(documents)
    query_result = embeddings.embed_query(query)
    print("成功创建 embeddings")
except Exception as ex:
    print(
        "请确保 infinity 实例正在运行。通过点击以下链接进行验证："
        f"{infinity_api_url.replace('v1', 'docs')} 异常信息：{ex}。 "
    )
```

```text
请确保 infinity 实例正在运行。通过点击 http://localhost:7797/docs 进行验证。异常信息：HTTPConnectionPool(host='localhost', port=7797): 超出最大重试次数，URL: /v1/embeddings（由 NewConnectionError('<urllib3.connection.HTTPConnection object at 0x7f91c35dbd30>: 无法建立新连接：[Errno 111] 连接被拒绝') 引起）。
```

```python
# （演示）计算相似度
import numpy as np

scores = np.array(documents_embedded) @ np.array(query_result).T
dict(zip(documents, scores))
```

```json
{'Baguette is a dish.': 0.31344215908661155,
 'Paris is the capital of France.': 0.8148670296896388,
 'numpy is a lib for linear algebra': 0.004429399861302009,
 "You escaped what I've escaped - You'd be in Paris getting fucked up too": 0.5088476180154582}
```

---

<div className="source-links">
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/embeddings/infinity.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
<Callout icon="terminal-2">
    [将这些文档连接到](/use-these-docs) Claude、VSCode 等，以便通过 MCP 获取实时答案。
</Callout>
</div>