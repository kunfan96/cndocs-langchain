---
title: "Metal 集成"
description: "使用 LangChain Python 与 Metal 集成。"
---

本页介绍如何在 LangChain 中使用 [Metal](https://getmetal.io)。

## 什么是 Metal？

Metal 是一个为生产环境构建的托管检索与记忆平台。您可以轻松地将数据索引到 `Metal` 中，并对其进行语义搜索和检索。

## 快速开始

首先，[创建一个 Metal 账户](https://app.getmetal.io/signup)。

然后，您可以轻松利用 `MetalRetriever` 类来开始检索数据，用于语义搜索、提示上下文等。该类接受一个 `Metal` 实例和一个传递给 Metal API 的参数字典。

```python
from langchain_classic.retrievers import MetalRetriever
from metal_sdk.metal import Metal


metal = Metal("API_KEY", "CLIENT_ID", "INDEX_ID");
retriever = MetalRetriever(metal, params={"limit": 2})

docs = retriever.invoke("search term")
```

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档](/use-these-docs)通过 MCP 连接到 Claude、VSCode 等，以获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/metal.mdx) 或 [提交问题](https://github.com/langchain-ai/docs/issues/new/choose)。
</Callout>
</div>